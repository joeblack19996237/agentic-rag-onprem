# 02 — Requirements (seeded outline)

> Stage 2 seed: REQ-### identifiers, one-line statement, one-line acceptance criterion each.
> Stage 7 (`spec-writer`) deepens each into full acceptance criteria and links to architecture, test plan, build plan.
> Each REQ traces to a value-prop differentiator (§4 of `01-product-brief.md`) or a non-functional constraint.

Status legend: **MVP** = ship in MVP; **V2+** = roadmap; **NFR** = non-functional, applies to MVP.

## Functional requirements

| ID | Status | Statement | Acceptance seed | Traces to |
|---|---|---|---|---|
| **REQ-001** | MVP | The system shall accept document uploads via API in supported formats (PDF text-extractable, Word .docx, Markdown, plain text) | Upload returns a document ID and a parseable processing status within 1 second; rejected formats return 415 with the supported list | Diff 1 (on-prem ingest) |
| **REQ-002** | MVP | The system shall parse, chunk, embed, and index uploaded documents into a vector store | A 100-page born-digital PDF reaches "indexed" status in ≤ 60 s on reference hardware; failure modes emit structured errors with retry guidance | Diff 1 |
| **REQ-003** | MVP | The system shall perform hybrid retrieval (dense + sparse + rerank) over the indexed corpus, **English document retrieval only at MVP per DEC-052** (multi-language schema extensions deferred; schema must not block future extension). **Embedding model is `bge-m3` per DEC-086** (reverted from `bge-large-en-v1.5`), which natively produces the dense **and** sparse vectors this requirement depends on — a dense-only embedding model cannot satisfy "hybrid dense+sparse" | English-only query returns top-K reranked chunks; reranking measurably improves NDCG over dense-only on the golden set; **both a dense-vector score and a sparse-vector score are present and independently contribute to the fused ranking (verifiable via `bge-m3`'s dense + sparse output), not just dense-vector-plus-rerank** | DEC-014 (embedding portion reinstated by DEC-086); DEC-052 (generation/NLI/language-scope portions still in force); DEC-086 |
| **REQ-004** | MVP | The system shall generate natural-language answers with inline citations linking to source chunks | Every assertion-bearing sentence in the answer carries at least one citation token; citation tokens resolve to specific chunk IDs in this turn's retrieval set | Diff 3 |
| **REQ-005** | MVP | The system shall perform runtime citation verification before delivering an answer | Every emitted citation is verified to (a) reference a chunk in the **Layer-2-authorized, reranked chunk set actually used to construct the generation prompt** (mechanical — per DEC-088, not the raw pre-authorization retrieval candidate pool), and (b) pass an NLI entailment check against that chunk; verification failures cause refusal or stripped citation per policy | Diff 3; DEC-088 |
| **REQ-006** | MVP | The system shall refuse to answer when grounding confidence is below configured threshold | Below-threshold queries receive an explicit refusal response (no fabricated answer); refusal threshold is admin-configurable; refusal events are logged | Diff 4 |
| **REQ-007** | MVP | The system shall persist an append-only audit record for every query | Each record contains: query, timestamp, user/session ID, **`conversation_id` (non-null, per RC-T3-02)**, retrieved chunk IDs, answer text, **`citations` — the same citation objects returned in the API response, including verbatim cited-span snippet text captured at answer time, not merely `chunk_id` pointers (per DEC-087)** — so the record remains evidentiary-complete after the source chunk is later physically deleted under retention policy (DEC-046), verification results, 5-class refusal flag (per DEC-042/DEC-058) with both `refusal_reason_actual` and `refusal_reason_shown`, **`context_fingerprint` (non-null, per DEC-060, extended by DEC-089)** containing `model_adapter`, `model_version`, `embedding_model_version`, `reranker_version`, `prompt_template_id`, `verify_thresholds`, **`safety_input_adapter`, `safety_input_version`, `safety_output_adapter`, `safety_output_version`, `policy_ruleset_version`**, latency. Records are immutable after write per DEC-070 | Diff 5; DEC-060; DEC-070; DEC-087; DEC-089 |
| **REQ-008** | MVP | The system shall expose a stable HTTP API for vendor integrators | OpenAPI 3.x spec published; semantic versioning; integration test suite exercises every documented endpoint | Diff 2 |
| **REQ-009** | MVP | The system shall ship an embeddable chat widget (iframe) deployable into a host vendor UI | Widget loads from system origin via iframe tag; host can pass theme tokens (color, font) via URL params; widget never reads cross-origin host state | Diff 2, §8 trend research |
| **REQ-010** | MVP | The system shall provide admin APIs for document lifecycle: upload, list, delete, per-document ACL | Delete is soft (configurable retention); ACL is a flat tag set in MVP (V2: group-based); list supports pagination | Diff 5 |
| **REQ-011** | MVP | The system shall ship as a single-host installable artifact (docker-compose) | `docker compose up` plus a documented config file brings the system from cold to a working `/ready` endpoint in ≤ 30 minutes on reference hardware | DEC-008 (demo) |
| **REQ-012** | MVP | The system shall be runnable in air-gapped mode | With egress blocked at the OS network namespace, MVP eval suite passes 100%; no required outbound calls; optional cloud-LLM adapters disabled by default | DEC-003 |
| **REQ-013** | MVP | The system shall ship an offline eval harness using RAGAS metrics | `cli eval run --suite golden` produces a report with faithfulness, answer relevancy, context precision, context recall; pass/fail per §9.1 thresholds | DEC-017 |
| **REQ-014** | MVP | The system shall ship with a sample CCM-style corpus and golden Q&A set | A 50-question hand-curated golden set is included; first-run `eval` produces a baseline report; documented as "the smoke test for new installs" | RISK-009 |
| REQ-015 | V2 | The system shall support multi-step ReAct-style agent reasoning over the same retrieval primitives | Agent decomposes a multi-part question into sub-queries; final answer carries citations from each sub-query; degrades to single-step when single-step recall is sufficient | (V2-α) |
| REQ-016 | V2 | The system shall surface a review queue for routed categories | Admin defines category routing rules; matching answers pause for reviewer action (approve / edit / reject); reviewer verdict captured | (V2-α) |
| REQ-017 | V2 | The system shall support pluggable document connectors (SharePoint, file share, generic REST) | Connector framework documented; at least one reference connector ships | (V2-γ) |
| REQ-018 | V2 | The system shall provide a JS widget (non-iframe) for deeper vendor theming | Widget renders in host DOM; host CSS cascades; sandbox isolation preserved at API layer | (V2-β) |
| **REQ-019** | **V2** | The system shall render each citation as a highlighted span inside its source chunk and a clipped page snapshot for PDF sources | Click a citation → modal opens the source chunk with the cited span highlighted; for PDF sources, a page-region image is shown | DEC-022, brief §7 V2-citation-deep |
| **REQ-020** | **V2** | The system shall decompose an answer into atomic claims and verify each claim independently | Each claim carries its own NLI check + citation; failed-claim spans are visually downgraded (greyed) while passing claims render normally | DEC-022, brief §7 V2-citation-deep |
| **REQ-021** | **V2** | The system shall support document lifecycle states (`authoritative` / `draft` / `deprecated`) and surface potential contradictions | Admin can flip state via API or admin console; `authoritative` boosts retrieval ranking; the system flags document pairs whose contents conflict on the same query | DEC-022, brief §7 V2-governance-deep |
| **REQ-022** | **V2** | The system shall ship a per-customer prompt template registry with versioning | Customer admin saves system prompt, refusal phrasing, citation format; templates are immutable on version pin; rollback supported | DEC-022, brief §7 V2-governance-deep |
| **REQ-023** | **V2** | The system shall allow admins to curate a customer-specific golden Q/A set and run RAGAS against it | Admin UI for adding/editing/deleting Q/A pairs; CLI runs the customer set as a regression gate; deltas vs prior run reported | DEC-022, brief §7 V2-tuning-deep |
| **REQ-024** | **V2** | The system shall support A/B traffic split across prompts or model adapters | Admin defines a split (e.g., 50/50); per-arm RAGAS metrics over a sampling window are reported; admin promotes the winner | DEC-022, brief §7 V2-tuning-deep |
| **REQ-033** | **V2** | The system shall implement a model adapter abstraction so swapping generation model (e.g. Llama-3.1 → Llama-3.2 / Mistral-Small → Mistral-Medium / commercial API per DEC-052 English-only stack) requires only a config change with no service restart | Switching adapter via admin API takes effect for new turns within ≤ 10 seconds; pre- and post-switch RAGAS reports run against the same golden set produce comparable structured deltas | DEC-028 LCC enabler |
| **REQ-034** | **MVP-promoted (schema) / V2 (automation)** | The system shall version embedding model identity (**schema portion in MVP per DEC-059**: double-collection naming convention + `embedding_model_version` column on `chunks`) and support single-host blue/green corpus re-embedding (**automation V2**) | MVP: each chunk persists `embedding_model_version`; collections are named `<corpus_id>_<embedding_model_version>`. V2: bumping the embedding model spawns a re-embedding job into a shadow collection; admin triggers cutover; rollback returns to old collection within ≤ 60 seconds | DEC-028 LCC Tier 3 enabler; DEC-059 |
| **REQ-035** | **MVP-promoted (schema) / V2 (services)** | Every audit record (REQ-007) shall carry a "context fingerprint": model adapter + model version + embedding model version + reranker version + prompt template version + verify thresholds **+ safety-rail adapter/version + policy ruleset version (per DEC-089)** active at answer time (**fingerprint columns mandatory and non-null in MVP per DEC-060, extended by DEC-089**; LCC-grade fingerprint services V2) | MVP: an auditor selecting any historical answer — **including a `policy_blocked` refusal** — can reconstruct exactly which stack components (including which safety-rail model/version/ruleset) produced it; fingerprint fields are non-null. V2: LCC migration-evidence pipeline reads fingerprints across time windows | DEC-028 LCC forensics; DEC-060; DEC-089 |
| **REQ-006a** | **MVP** | `no_recall` refusals shall return up to 3 distinct doc_ids from the **post-Layer-2 retained** low-score retrieval set as "you might also be looking for" suggestions. Because chunks have already passed Layer 2 ACL trim, no second ACL check is required | Neighbors come from the already-fetched, already-ACL-trimmed retrieval set (no extra retrieval, no second filter); UX clearly labels them as "not a confident answer" | DEC-043 (acceptance clarified per RC-T3-04) |
| **REQ-006d** | **MVP** | The refusal taxonomy shall include 5 typed reasons: `no_recall`, `low_grounding`, `access_denied`, `policy_blocked`, `verification_unavailable`. `acl_denial_mode` config (`transparent` default / `opaque`) controls whether `access_denied` is shown as-is or masked as `no_recall` to the user. **Both modes are shippable in MVP with no schema change required to flip** (DEC-069). **Audit log always records the real reason** regardless of mode | All 5 types are returned as HTTP 200 with typed `refusal_reason` (not as errors); audit log fields `refusal_reason_actual` and `refusal_reason_shown` differ when `opaque`; mode change takes effect without restart and without schema migration | DEC-042 + DEC-069 |
| **REQ-006b** | **V2** | The system shall maintain a query log index and surface ACL-filtered similar past questions when `no_recall` is returned | Suggestions are filtered to the current user's ACL on **both** sides — the suggested question's asker context and the current user's access to the source documents | DEC-043 |
| **REQ-006c** | **V2** | The system shall provide an access-request workflow when `access_denied` is returned in `transparent` mode | `POST /v1/access-requests {document_ids[], justification}`; request routed to `documents.approver_user_ids[]`; notification emitted; approval grants ACL update via ECMAdapter | DEC-043, DEC-044 |
| **REQ-036** | **MVP** | The system shall implement a **two-layer authorization** pipeline via `ECMAdapter` interface, with two reference adapters (`LocalAdapter` + `OIDCAdapter`) | Layer 1: ingest stores `acl.allow_principals[]`, `acl.deny_principals[]`, `security_label`, `retention_state`, `document_id`, `version_id`, `repository_id` as Qdrant payload; query-time filter is pushed into Qdrant HNSW (filter-then-search). Layer 2: post-retrieval, distinct doc_ids go through `batch_check_access(user, doc_ids)` + `get_retention_state(doc_ids)` via adapter; chunks failing either are dropped before rerank. Identity does NOT enter the embedding vector | DEC-045, DEC-046, §7B |
| **REQ-037** | **MVP** | Document retention state shall be captured at ingest via `ECMAdapter.get_retention_state()` and enforced as a Qdrant filter at retrieval | `retention_state` field on every chunk; query filter excludes non-`active` states; Layer 2 re-checks current state to catch lag | DEC-046 |
| **REQ-041** | **MVP** | A CDC consumer (HTTPS webhook receiver + periodic re-poll fallback) shall handle ECM change events: `acl_changed`, `version_added`, `version_deleted`, `retention_expired`, `legal_hold_added`, `legal_hold_released` | Webhook endpoint authenticated via HMAC; re-poll runs every 30 min (admin-configurable); `retention_expired` triggers physical delete of chunks + embeddings (not soft flag); `legal_hold_added` freezes chunks (immutable + non-reindexable) until released | DEC-046, DEC-051 |
| **REQ-043** | **MVP** | The system shall expose an audit-events pull API for vendor SIEM forwarding | `GET /v1/admin/audit/events?from=...&to=...&user_id=...&format=ndjson&cursor=...`; idempotent paginated read; vendor SIEM pulls on their schedule | DEC-047 |
| **REQ-045** | **MVP** | Every query that **retrieved or attempted to retrieve** ECM-sourced documents shall trigger an async write-back to ECM audit log via `ECMAdapter.write_audit_access()`. **Both successful access (`intent="granted"`) and `access_denied` refusals (`intent="denied"`) are written** per DEC-064 | Best-effort async write (does not block query); failures retried with exponential backoff; final failure logged for ops review but does not fail the user query; both granted and denied paths appear in the ECM audit trail | DEC-047; DEC-064 |
| **REQ-038** | **V2** | M-Files identity/ACL adapter implementation against M-Files REST API | All `ECMAdapter` methods implemented; integration test suite against an M-Files sandbox; `get_effective_acl()` matches M-Files-side evaluation on a curated test set | DEC-050 V2-β |
| **REQ-039** | **V2** | SharePoint adapter via Microsoft Graph API | All `ECMAdapter` methods implemented; honors per-item unique permissions + sensitivity labels | DEC-050 V2-β |
| **REQ-040** | **V2** | Hyland Alfresco adapter | All `ECMAdapter` methods implemented; honors permission inheritance and dynamic groups | DEC-050 V2-β |
| **REQ-044** | **V2** | **Documentum + OpenText adapters** (V2-α priority) | All `ECMAdapter` methods implemented; honors Documentum `dm_acl + r_accessor_name` and OpenText permission ACL + inheritance + dynamic groups | DEC-050 V2-α |
| **REQ-042** | **V3** | Partner adapter SDK + certification kit so customers and partners can implement adapters for regional CCM/ECM vendors | Documented adapter interface; reference implementation walk-through; certification test suite that adapter passes before being marked "production" | DEC-050 V2-γ partner pattern |
| REQ-025 | V3 | The system shall ship as React / Vue component SDKs for embedding | Vendor adds `<GroundedDocsChat />` to a React app; CSS isolation guaranteed; identical API surface to widget | (V3) |
| REQ-026 | V3 | The system shall support multi-host deployment via Helm chart with blue/green reindex. **V3 trigger thresholds (Round 3, S0.b)**: any one of (a) corpus size > 10 M chunks (Qdrant single-node filter performance degrades); (b) sustained concurrency > 8 in-flight queries (DEC-066 warm-cache cap exceeded); (c) > 3 customer-pilot deployments concurrently active (operationally requires HA) | Helm install on a Kubernetes cluster; reindex pipeline runs against shadow collection with cutover trigger; trigger-threshold metrics (chunk count, concurrency p95, active pilot count) are observable via §12.3 | (V3) |
| REQ-027 | V3 | The system shall integrate TruLens for production observability + drift monitoring | Live trace per query; drift detection on RAGAS metrics over a configurable window | DEC-016 V3 step |
| REQ-028 | V3 | The system shall support OCR + complex table extraction during ingest | Scanned-PDF page becomes searchable text; tables in contracts emit cell-structured chunks | (V3) |
| REQ-029 | V3 | The system shall ship a compliance audit prep package for the first regulated buyer's standard (SOC2 / HIPAA / MLPS L3) | Audit-mode export of audit log; documentation matrix mapping controls to system features | Triggered by first regulated buyer |
| **REQ-030** | **V3** | The system shall support customer-corpus LoRA fine-tuning of **both reranker and generator** (two independent adapters, trained separately) | Each training job runs on customer hardware in ≤ 24 h on reference rig; emits adapter weights; per-customer adapter swap at serving time (REQ-033 abstraction) | DEC-023, DEC-030 |
| **REQ-031** | **V3** | The system shall expose a corpus-health dashboard for admin | Dashboard shows: stale-document candidates, contradicting-pair candidates, low-citation-coverage documents, top high-refusal query clusters | DEC-022 (sticky support deliverable) |
| **REQ-032** | **V3** | The system shall ship a tuning playbook as a versioned product artifact | Playbook is markdown + executable scripts; "5 steps after first install" covers golden-set seeding, threshold calibration, prompt-template seed, A/B baseline, first weekly review | DEC-022 |

## Non-functional requirements (NFR-### — MVP)

| ID | Statement | Acceptance seed |
|---|---|---|
| **NFR-001** | The system shall run end-to-end on a single host meeting the **revised English-only customer-hardware floor**: single GPU ≥ 24 GB VRAM + 64 GB system RAM + 200 GB SSD (DEC-041 + DEC-079, **supersedes the 16 GB figure this row previously carried** — DEC-052's 16 GB was itself superseded by DEC-079's layered-safety-rail floor recalculation, and further nudged by DEC-086's `bge-m3` embedding revert; see `04-architecture.md` §4.2.2). Floor reference generation model = `Llama-3.1-8B-Instruct` int4; Comfort tier = `Mistral-Small-24B-Instruct` int4 at 32 GB VRAM. Below-floor runs are supported in **degraded mode** (`Qwen2.5-7B-Instruct` int4 or `Phi-4`) with documented quality + latency disclaimers | Reference rig spec documented in `09-deployment-ops.md`; eval suite passes RAGAS thresholds (DEC-017) on the floor rig; degraded-mode rig passes only a published lower-bound suite |
| **NFR-002** | Make no required outbound network calls at runtime | Air-gap test (REQ-012) passes |
| **NFR-003** | **English baseline only at MVP per DEC-052** (supersedes prior "Chinese + English" baseline); design must not block future language extensions (column-level schema neutral on language). **Embedding model is `bge-m3` per DEC-086** — English-only *document scope* is unaffected by this; DEC-086 only reverses the *model choice* to restore hybrid retrieval, it does not reopen multilingual document support | English-only golden set passes MVP thresholds; schema review confirms no English-only field names or hard-coded language assumptions in `chunks` / `documents` / `audit_events` |
| **NFR-004** | Citation-hit-rate (every emitted citation lands in current retrieval set) shall be 100% | Hard gate; verified by REQ-005 mechanical check |
| **NFR-005** | Query latency p95 ≤ 8 s on reference hardware. **Concurrency targets revised under Round 2 (DEC-066 + DEC-076 + DEC-079)**: **cold-cache floor** ≥ 2 in-flight queries (was DEC-066 hard cap); **warm-cache demo target** 5-8 in-flight queries when Redis cache hit ratios (prompt / answer / ACL / embedding per DEC-076) ≥ 60%. Beyond warm-cache cap, queries queue with `queue_depth` exposed. SLO holding requires KV-cache reuse + prompt-cache + reranker/NLI batching per DEC-054, plus Redis-backed answer / ACL / embedding cache per DEC-076 | Measured in eval harness latency suite at 1 / 2 / 5 / 8 concurrent users with cache priming sweep; per-line-item budgets stated in `04-architecture` §7B.12 + §9.4 |
| **NFR-006** | Ingest throughput ≥ 100 pages/min on reference hardware | Measured in eval harness ingest suite |
| **NFR-007** | All durable state survives container restart | Persist DB + vector store on host volume; documented backup procedure |
| **NFR-008** | The system shall not log query content or document content at INFO level by default | Sensitive payload at DEBUG only; INFO logs contain IDs, status, latency, sizes |
| **NFR-009** | The HTTP API surface shall declare auth on every endpoint | Default: bearer token; integrations document AuthN/AuthZ hand-off |
| **NFR-010** | The system shall expose a configurable refusal threshold (REQ-006) via admin config | Threshold change takes effect without restart |
| **NFR-011** | The MVP shall ship a `dev profile` documenting cloud-GPU rental settings (RunPod / Vast.ai recommended images, ports, volumes, model cache layout) | A new contributor following only the dev profile reaches a working eval run in ≤ 2 hours; profile validated by user (DEC-021) |
| **NFR-012** | Identity in the embedding vector is **forbidden**; ACL data lives only as Qdrant payload metadata | Static check in CI: embedding service inputs may not contain user / role / group / principal identifiers (DEC-046 §7B.3) |
| **NFR-013** | ECM audit write-back failure (REQ-045) shall not affect query SLO | Write-back runs async; query response latency budget unchanged by ECM availability; persistent failure surfaces as ops alert not user error |
| **NFR-014** | Retention expiry SLA is **split by transport path per DEC-056**: **webhook path** ≤ 60 s end-to-end (event delivery → physical delete of chunks + embeddings); **re-poll fallback path** ≤ 30 min when webhook delivery fails. NFR-016 alerts ops when webhook health degrades. V2 with event bus targets ≤ 5 s | Verified by integration test: send `retention_expired` event via webhook, query the doc, confirm no chunks returned within 60 s; verify Qdrant contains no orphan vectors. Second test: disable webhook, advance time 30 min, run re-poll, confirm chunks gone |
| **NFR-015** | Legal hold shall freeze affected chunks: not deletable, not updatable, not re-indexable, until release | Verified by integration test: send `legal_hold_added`, attempt re-ingest of the doc → operation refused with hold-active reason |
| **NFR-016** | ECM PDP timeout / failure circuit breaker per DEC-063: configurable threshold (default 500 ms); breaker trip returns `verification_unavailable` refusal — never silent skip. Webhook delivery health is monitored; sustained failure triggers ops alert | Integration test: stub PDP with 1 s latency → breaker trips after configurable window → queries return `verification_unavailable`; metric `pdp_breaker_state` is observable |
| **NFR-017** | Rate limiting at API surface: default 60 queries / token / minute, burst 10 / 5 s, admin-configurable. Per-IP secondary limit for unauthenticated demo widget. Over-limit returns HTTP 429 with `Retry-After` | Load test: 100 QPS to a single token → 429 responses observed; abuse alert emitted when sustained over-limit |
| **NFR-018** | Widget security configuration: Content-Security-Policy with `frame-ancestors` allowlist, postMessage origin allowlist validation, `X-Frame-Options` legacy header, Subresource Integrity hashes on widget JS bundles | Security scan (mozilla observatory / similar) against demo widget instance passes baseline grade B+ or better; no cross-origin host state reads in widget JS source review |
| **NFR-019** | Offline model bundle per DEC-067: single tarball with SHA-256 manifest; total size ≤ 20 GB; install verification checks all hashes before activation | Bundle build script produces tarball + manifest; install on air-gap host succeeds with no network access; manifest verification catches corrupted weights |
| **NFR-020** | AU Privacy Act 1988 + NZ Privacy Act 2020 data-residency posture: on-prem by default; no required outbound calls to non-customer-controlled services; documentation lists every potential cross-border data path with disable mechanism. NDB-scheme breach-notification readiness reference is in `09-deployment-ops` | Reviewer can trace every model / config / external call to either "customer-controlled" or "explicitly disabled in air-gap"; audit retention configurable per customer policy |
| **NFR-021** | **Layered safety rails (DEC-077)** must be present in MVP shipping artifact: `safety_input/` (Llama Prompt Guard 2 int4) + `safety_output/` (Llama Guard 3 8B int4) + `policy/` (NeMo Guardrails declarative DSL). Each rail's verdict is captured in `audit_events` per query | docker-compose includes all three rail services; integration tests show injection-trigger query → `policy_blocked` refusal; toxicity-trigger draft → `policy_blocked`; audit record includes per-rail verdict |
| **NFR-022** | **Per-rail latency budget (DEC-077)**: `safety_input` ≤ 150 ms; `safety_output` ≤ 250 ms; `policy/` ≤ 30 ms additive overhead. Per-rail latency contributes to the NFR-005 p95 ≤ 8 s budget — see `04-architecture` §7B.12 revised | Eval harness latency suite measures per-rail span via OTEL GenAI conventions per §12.3 |
| **NFR-023** | **Mid-flight rewriting iteration cap (RC-R2-T8-04 + DEC-075)**: MVP value N=1 (single pass, no feedback edge enabled); V2 value N≤2 mid-flight rewrites per turn before refusal | Integration test (V2): seed an answer with 1 ungrounded claim → 1 feedback iteration → second answer passes verify; seed with permanently-ungrounded claim → after 2 iterations system refuses with `low_grounding` |
| **NFR-024** | **Cost ceiling per turn (RC-R2-T8-04)**: ≤ ¥0.10 per turn at the customer-chosen open-weights configuration (Llama-3.1-8B int4 on the floor tier); configurable; ceiling is **observability-driven**, not a hard runtime block in MVP (V2 adds runtime block) | Eval harness reports cost per turn; alert when 95th percentile exceeds configured ceiling for 5 consecutive minutes |
| **NFR-025** | **ACL cache TTL discipline (RC-R2-T4-03 + DEC-076)**: per-user `effective_principals[]` cache TTL ≤ 60 s; per-document Layer 2 PDP cache TTL ≤ 30 s; force-refresh mandatory before Layer 2 evaluation when TTL exceeded. Silent stale-cache use is forbidden | Integration test: inject `acl_changed` event for a user; verify next query for that user re-fetches the principal set within 60 s; verify per-document cache invalidates within 30 s of `acl_changed(doc_id)` |
| **NFR-026** | **OpenTelemetry GenAI semantic conventions (RC-R2-T5-03)**: per-query span structure includes `gen_ai.system`, `gen_ai.request.model`, `gen_ai.response.model`, `gen_ai.usage.prompt_tokens`, `gen_ai.usage.completion_tokens`, `gen_ai.response.finish_reasons`; LangGraph node spans nest under the top-level request span | Trace inspection shows GenAI-conformant attributes; OTLP forwarding to standard collectors recognises the attributes |
| **NFR-027** | **TEI rerank latency p95 ≤ 100 ms per query (top-K = 50) (Round 3, S2.2)**. **Backend: ONNX Runtime is the MVP-shipped default (DEC-093, supersedes this row's prior "recommended for AU/NZ public-sector deployments" framing)** — not an optional tuning step. TensorRT-LLM acceptable when customer hardware supports it. Plain TEI default backend (150-200 ms) is a **documented fallback only**, not the shipped default, because it individually risks exceeding the ≤150 ms rerank line item the `§7B.12` latency SLO is calculated against | Eval harness latency suite measures rerank span; deployment-ops documents the backend-choice procedure with measured numbers per tier; **default docker-compose install uses the ONNX Runtime backend out of the box (DEC-093 acceptance criterion)** |
| **NFR-028** | **vLLM optimization flags (Round 3, S2.3, DEC-083)**: `--enable-chunked-prefill` is **mandatory** in MVP shipping config. `--speculative-decoding` is **conditional on measured VRAM headroom** — only enabled after `09-deployment-ops` validation run shows ≥ 4 GB free VRAM at peak concurrency; draft model `Llama-3.2-1B-Instruct` int4 (~1 GB) is skipped if headroom is below threshold. Default flag values: floor tier `false`; comfort tier `true` | Eval harness shows TTFT improvement on long-context prompts when chunked-prefill is enabled; when speculative decoding is enabled, token throughput increases 1.5-2× per benchmark §Topic 7; flag values verified by `vllm.config` introspection |
| **NFR-029** | **Parallel-node execution budget (Round 3, S1.1, DEC-082)**: the `safety_input ∥ retrieve` parallel fan-out + `acl/` join latency ≤ max(`safety_input`, `retrieve`) + 20 ms join overhead. The `safety_input` budget (≤ 150 ms warm) is masked under the `retrieve` budget (≤ 250 ms warm); join overhead measured in `acl/` node prologue | Eval harness latency suite confirms `safety_input ∥ retrieve` p95 ≤ 270 ms warm-cache; OTEL trace shows parallel span structure (two child spans starting simultaneously under the same parent) |

**Functional requirements added under Round 2 (extension to MVP):**

| ID | Phase | Requirement | Acceptance criteria | Trace |
|---|---|---|---|---|
| **REQ-046** | **MVP** (Round 2) | The system shall orchestrate the query pipeline as a **LangGraph 0.2.x typed-state graph** with explicit node boundaries (`safety_input`, `retrieve`, `acl`, `rerank`, `generate`, `safety_output`, `verify`, `audit`) and a reserved feedback edge from `verify/` to `generate/` (V2-enabled) | The MVP build includes LangGraph 0.2.x as a pinned dependency; `query_graph` is defined under `api/`; integration test shows graph traversal matches the canonical sequence; the feedback edge exists but is disabled in MVP config | DEC-075 |
| **REQ-047** | **MVP** (Round 2) | The system shall use Redis (or Valkey, OSS fork) as the hot-path cache for prompt cache, answer cache, ACL cache, and embedding cache; with TTL discipline per NFR-025 | docker-compose includes a Redis 7.x or Valkey 8.x container; integration test shows answer-cache hit on repeated identical query within the model_version + ACL window; integration test shows TTL force-refresh on `acl_changed` | DEC-076 |
| **REQ-048** | **MVP** (Round 2) | The system shall apply layered safety rails (input rail: Llama Prompt Guard 2; output rail: Llama Guard 3 8B; orchestration rail: NeMo Guardrails) on every query, with per-rail verdicts captured in `audit_events` | Integration test: prompt-injection query → `policy_blocked` refusal with `safety_input` verdict in audit; harmful draft → `policy_blocked` refusal with `safety_output` verdict in audit; orchestration-rail policy escalation → refusal with the escalating rule recorded in audit | DEC-077 |
| **REQ-049** | **MVP** (Round 2) | Eval golden set shall be expanded to 150-200 prompts with the original 50-prompt set preserved as the smoke-test subset | `cli eval run --suite golden-smoke` covers the 50-prompt smoke set ≤ 5 min; `cli eval run --suite golden-full` covers the 150-200 prompt full set; documented in `23-evals-guardrails.md` §2.2 | DEC-078 |
| **REQ-050** | **MVP** (Round 3) | The system shall expose a `SafetyRailAdapter` Protocol for input/output rails (`classify(text: str) -> SafetyVerdict`, `health() -> bool`, `model_version() -> str`). Default MVP implementation wraps `Llama Prompt Guard 2` (input) and `Llama Guard 3 8B int4 AWQ` (output). Customers may swap to alternative models (ShieldGemma 2B, BERT-class fine-tune) via configuration without code change, subject to RAGAS-validated regression on the goldset | Integration test: swap the output adapter to a stub returning `flagged` for all inputs → every query refuses with `policy_blocked` and audit captures `safety_output_verdict`; second swap to a BERT-class fine-tune passes the goldset regression with documented metric deltas | DEC-082; mirror pattern of REQ-033 |
| **REQ-051** | **V2** (Round 3) | The system shall ship an admin-configurable query intent classifier. Per-intent-class policy may downgrade `verify/.nli_slow_path` to advisory mode, but **must never bypass `verify/.mechanical_fast_path` or audit logging**. Per-customer SLA waiver required for any intent class with NLI downgraded; the waiver is captured in `audit_events.policy_waiver_id` per query; audit always records `nli_performed: bool` and `intent_class` | Integration test (V2): admin defines `intent: chitchat → nli: advisory` and signs the SLA waiver; queries classified as chitchat skip `nli_slow_path` and complete in ≤ 1 s; audit shows `nli_performed: false, intent_class: "chitchat", policy_waiver_id: <uuid>`; mechanical-fast-path still runs | DEC-084 |
| **REQ-052** | **V2** (Round 3) | The V2 verification pipeline shall support **sentence-level streaming NLI** parallel to vLLM token streaming. `generate/` emits sentence-boundary signals; `verify/` consumes them in parallel; on first failed claim, `verify/` emits an early-stop token back to vLLM and routes the partial output to the rewrite feedback edge | Integration test (V2): seed a query where sentence 2 contains an ungrounded claim; vLLM is stopped on sentence-boundary-after-claim-2; failed-claim list contains the offending sentence; feedback edge fires; revision_count increments; corrected answer passes verify | DEC-075 + DEC-082 |
| **REQ-053** | **V2** (Round 3) | Widget UI shall support two-pass streaming display: first-pass output rendered with `Correcting in progress…` indicator on the first failed claim; corrected output rendered diff-style against the first pass. Pairs with REQ-052 streaming verification; out-of-scope if customer disables the feedback edge | UX test (V2): trigger a feedback-edge case → widget shows initial draft → "Correcting…" indicator appears → corrected answer renders with diff highlights; UX recording stored for vendor demo material | DEC-075 |
| **REQ-054** | **V3** (Round 3) | V3 introduces an **advisory NLI mode** for intent classes deemed low-risk (paired with REQ-051 V2 intent classifier). In advisory mode, the answer is emitted immediately; NLI runs asynchronously and posts a compliance flag to the audit event; widget surfaces a delayed disclaimer if NLI fails. Requires explicit per-customer SLA waiver. **Mechanical citation check + audit are NEVER advisory** | Integration test (V3): admin sets intent `lookup → advisory_nli`; query answered immediately; async task posts NLI verdict to `audit_events.nli_advisory_verdict` within 5 s; if NLI fails, the widget receives a follow-up websocket message displaying the disclaimer | DEC-084 |
| **REQ-055** | **V3** (Round 3) | V3 introduces an alternative retrieval path: for documents tagged `legal_hold` or `sensitivity: high`, GroundedDocs **bypasses pre-ingestion** and queries the customer's ECM Search API at runtime via a new `ECMSearchAPIAdapter`. ECM enforces ACL natively; returned passages are run through TEI rerank in-memory then handed to `generate/`. **No pre-ingested copies retained** — RAG-side TTL 0 for these documents | Integration test (V3): document tagged `sensitivity: high` is queried; no pre-ingested chunks exist in Qdrant for the doc; `ECMSearchAPIAdapter.search(query, user_context)` returns text + chunk IDs; result is reranked in-process; answer is generated; no chunks written to Qdrant or Postgres for this query class | DEC-085 |

**Functional requirements added under Round 4 (2026-07-03, independent architecture review):**

| ID | Phase | Requirement | Acceptance criteria | Trace |
|---|---|---|---|---|
| **REQ-056** | **MVP** (Round 4) | The system shall run a periodic full-reconciliation crawl comparing the ECM's document inventory against the RAG `documents` table, flagging orphaned or missing documents. MVP scope is **detect + ops alert only** (no auto-remediation) | Integration test: seed a document in the RAG `documents` table with no corresponding ECM document (simulated drift); reconciliation job run flags it in an ops-facing report/alert; default schedule is weekly, admin-configurable | DEC-090 |

## Traceability Quick Map (will deepen at Stage 7)

| Value-prop differentiator (§4 of brief) | Requirements |
|---|---|
| On-prem deployment | REQ-011, REQ-012, NFR-001, NFR-002, NFR-007 |
| Vendor-embeddable | REQ-008, REQ-009 |
| Verified citation | REQ-004, REQ-005, NFR-004, DEC-088 (verification-target correctness) |
| Refusal as a product feature | REQ-006, NFR-010 |
| Audit-ready | REQ-007, REQ-010, NFR-008, REQ-035, REQ-056, DEC-087 (verbatim citation snapshot), DEC-089 (safety-rail fingerprint) |
| Eval / RAGAS thresholds | REQ-013, REQ-014, NFR-005, NFR-006 |
| English baseline (multilingual deferred per DEC-052) | REQ-003, NFR-003 |

## Out of MVP (named, traced to Non-Goals §6 of brief)

- ReAct agent → REQ-015 (V2)
- Review queue → REQ-016 (V2)
- Connectors → REQ-017 (V2)
- JS widget → REQ-018 (V2)
- SDK, Helm, HA, blue/green, OCR, certs, IM channels — see §6 / §7 of brief
