# Spec Audit Report

## Verdict
READY

## Re-audit Note (2026-07-15, PDF-primary-parser correction, DEC-143)

**Scope**: `update-specs` run to record DEC-143, which corrects DEC-036's PDF-primary-parser claim (`04-architecture.md`'s Doc-parsing row, module-dependency table, and decision-summary table; `03-workflows.md`'s workflow diagram and error-path table; `06-api-contracts.md`'s failure example; `10-build-plan.md`'s TASK-008/009 description) — Unstructured.io's PDF module cannot be imported without a full `torch`/OCR dependency stack, so PDF's primary parser moves to `pdfminer.six`, called directly; PyMuPDF stays the rescue path; Word (`python-docx`) is unaffected. Re-checks **Gate 5** (Architecture Readiness — the doc-parsing mechanism in `04-architecture.md` §4.1 changed) and **Gate 8** (Cross-Spec Consistency — propagation across the workflow doc, API contract examples, build plan, and one in-flight `.scratch/` issue's own implementation notes). Gates 1-4, 6-7, 9 are **not** re-checked — no product-brief scope, UX, AI-agent-behavior, requirements-level, or SaaS-tenancy content was touched — and carry forward as PASS/N/A from the prior audit.

**Context**: found while implementing `document-ingest-pipeline` Issue 02 (2026-07-15) — not a routine re-audit trigger, a real implementation-time discovery, the same shape as DEC-142 two days earlier (row below). Issue 02 needed to actually call `unstructured.partition.pdf` for the first time; importing it raised `ModuleNotFoundError: No module named 'pdfminer'` in this sandbox. Reading the installed package's source (`unstructured/partition/pdf.py` lines 19-20) found an unconditional, module-level `from unstructured_inference... import ...` — this import fires regardless of which strategy (`fast`/`hi_res`/`ocr_only`) is requested at call time, since Python evaluates module-level imports before any function body runs. Checked `unstructured_inference` and its own dependency `effdet`'s PyPI metadata live: both pull in `torch`, `torchvision`, `transformers`, `onnxruntime`, `opencv-python`, `pycocotools`, `accelerate` — the full `unstructured[pdf]` extra, none of it needed for plain text extraction from a born-digital PDF.

**What was checked before deciding**: grepped `DEC-036` and `Unstructured` (the underlying entity name, not just the ID, per this skill's Step 1 guidance) across `specs/` and `.scratch/` — found 5 spec files with real citations (`03-workflows.md`, `04-architecture.md` ×4 locations, `06-api-contracts.md`, `10-build-plan.md`) plus 2 in-flight `.scratch/` references, not just the two files anticipated at the start of this task. Also checked `unstructured.partition.docx`'s own import chain separately, confirming DOCX was unaffected (only imports `python-docx` and pure-Python `unstructured` internals) — and re-read DEC-036's own original text, confirming Word was always specified as `python-docx`-direct, never routed through Unstructured at all, so the DOCX half of DEC-036 needed no correction. Verified the replacement (`pdfminer.six`) live rather than assuming it would be lighter: real PyPI metadata for its standalone dependency list, a real non-dry-run `pip install`, and a real extraction test against an in-memory PDF built with PyMuPDF.

**Fix applied (DEC-143)**: `13-decision-log.md` gains the DEC-143 row (corrects DEC-036's PDF-primary-parser clause only; the PyMuPDF-rescue and python-docx-for-Word clauses are untouched and not re-litigated). `04-architecture.md` gains: a corrected Doc-parsing row in §4.1's tech-stack table, a corrected `ingest/` diagram box and module-dependency-table row, and a new DEC-143 row in the §14 decision-summary table (DEC-036's own row left unedited, append-only). `03-workflows.md`'s workflow diagram and error-path table corrected to name `pdfminer.six`. `06-api-contracts.md`'s failure-example JSON string corrected. `10-build-plan.md`'s TASK-008/009 TDD-Green line and Related-Requirements/Decision-References citations corrected. `00-index.md`'s `13-decision-log.md` row updated (through DEC-143, summary blurb added); `04-architecture.md`'s own Key-ids row left unchanged, matching the same pre-existing selective-list pattern DEC-142's note already established (that list didn't carry DEC-036 either, before or after). `confirmed-context.md`'s Drift Log gains the corresponding row (below). `.scratch/document-ingest-pipeline/issues/02-pdf-word-parsing-retry-resilience.md` and `PRD.md` updated in place (Issue 02 is in-flight, not yet closed, per this skill's implemented-vs-not-yet-implemented rule). `requirements.txt` drops `unstructured==0.18.32` entirely (zero remaining role in any currently-supported format once DOCX-direct and PDF-via-`pdfminer.six` are both accounted for) and adds `pdfminer.six==20260107`.

**Gate 5 — PASS, no open gap.** Unlike DEC-142's VRAM-budget gap, this correction has no comparable loose end: `pdfminer.six` and PyMuPDF are both already-installed, already-lightweight libraries with no model-loading/hardware-budget dimension to leave open. The one deliberately-accepted-and-recorded cost (loss of Unstructured's structured-element output for PDFs) is a design trade-off stated in DEC-143's own row, not an unresolved unknown.

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TASK`/`VG` id was needed or minted — DEC-143 corrects which library performs one already-required capability (PDF text extraction), not what's required. Checked `docs/adr/` — still does not exist, nothing to supersede. The one in-flight `.scratch/` feature affected was updated in place, not just flagged, since it hasn't been implemented yet.

**Final verdict: READY, unchanged.** Same pattern DEC-142's note already flagged as worth watching, recurring a second time within the same week: a spec-level claim about a third-party library's exact behavior held up only until code actually had to call it. `requirements.txt`'s own prior "confirmed via a real install" comment for `unstructured==0.18.32` was true as far as it went (the base package does import cleanly) but didn't specifically exercise the one capability this issue needed — a reminder that "the package imports" and "the specific code path I need works" are different claims, even when a real (non-dry-run) verification already happened once.

## Re-audit Note (2026-07-15, TEI/bge-m3 sparse-embedding correction, DEC-142)

**Scope**: `update-specs` run to record DEC-142, which corrects DEC-086's dense+sparse-single-model claim (REQ-003, `04-architecture.md`'s Embedding row(s), `05-data-model.md`'s `embedding_sparse` field, `07-database.md`'s Qdrant payload description) — TEI cannot serve `bge-m3`'s sparse output, only its dense output; sparse embedding now comes from a separate, dedicated SPLADE model, also via TEI. Re-checks **Gate 5** (Architecture Readiness — the embedding-serving mechanism in `04-architecture.md` §4.1/§4.2.2 changed, including an unresolved VRAM-budget gap this correction introduces) and **Gate 8** (Cross-Spec Consistency — propagation across REQ-003, the data model, the physical schema, and one in-flight `.scratch/` issue's own implementation notes). Gates 1-4, 6-7, 9 are **not** re-checked — no product-brief scope, UX, AI-agent-behavior, build-plan-acceptance-criteria, or SaaS-tenancy content was touched — and carry forward as PASS/N/A from the prior audit.

**Context**: found while implementing `document-ingest-pipeline` Issue 01's embedding client (2026-07-15) — not a routine re-audit trigger, a real implementation-time discovery. `ingest/embedding.py` needed to call TEI's actual `/embed_sparse` endpoint for `bge-m3`; verifying that endpoint's real behavior (TEI's own Rust source, `router/src/http/server.rs`/`types.rs`, plus GitHub issue #141's comment thread) found it only works for genuine SPLADE-architecture models (`--pooling splade`, MaskedLM-only) — `bge-m3`'s own sparse mechanism needs the model's raw `last_hidden_state`, which TEI's serving architecture never exposes for any model.

**This also corrects an earlier correction, not just DEC-086 directly.** The 2026-07-13 cross-model review (DEC-136/137/138's batch) already touched this exact area — its R.26 finding, citing the same GitHub issue #141, concluded "TEI serves dense + sparse only" (ColBERT unsupported, sparse assumed fine) and `04-architecture.md`'s Embedding row was edited to say so at the time. Re-reading issue #141's full comment thread this session (not just confirming the issue exists, which is as far as the 2026-07-13 pass appears to have gone) found a contributor stating plainly that sparse and ColBERT share the identical root cause — neither is exposed. A same-family (Claude) mistake corrected by a cross-model review, which itself only partially corrected it, closed for real only once the actual code needed to call the endpoint and its behavior got checked directly — worth naming since it's a concrete instance of the layered-verification principle DEC-130/DEC-135's own cross-model-review discipline exists for, not a criticism of that review's diligence.

**What was checked before deciding**: grepped `DEC-086` (14 hits in `specs/`, 4 in `.scratch/`) and `"natively produces"` / `"single forward pass"` / `bge-m3` across `specs/` and `.scratch/` per this skill's Step 1, rather than trusting a partial list of "files I remember citing this." Distinguished files asserting the specific single-model *mechanism* (REQ-003, `04-architecture.md` lines ~11/84/110/139, `05-data-model.md`, `07-database.md` — all corrected) from files citing DEC-086 only as "why `bge-m3` is the embedding model" at an outcome level, which remains true and unchanged (`01-product-brief.md`, `09-deployment-ops.md`, `10-build-plan.md`, `23-evals-guardrails.md` — left as-is). Historical review-memo/audit snapshots (`92-stage5-review-memos.md`, `93-confirmed-context.md`) were **not** edited — they are dated records of what was believed/decided at the time, not living cross-references, matching this project's own append-only-history convention applied to review artifacts as well as the decision log itself. Checked `.scratch/` for in-flight citations: `data-foundation/issues/02-qdrant-collection-payload-indexes.md` (already implemented — added an addendum note per this skill's "don't silently edit history" rule, code itself is unaffected since the Qdrant collection schema doesn't encode which model produces the sparse vector) and `document-ingest-pipeline/PRD.md` + its Issue 01 (both updated in place — Issue 01 was implemented in the same session this gap was found, before it was resolved, so its own "gap found, not yet resolved" note is completing the loop it explicitly left open, not rewriting history).

**Fix applied (DEC-142)**: `13-decision-log.md` gains the DEC-142 row (corrects DEC-086, doesn't edit it; also explicitly names and corrects the 2026-07-13 review's own partial fix). `02-requirements.md` REQ-003's mechanism claim and acceptance criterion reworded to attribute dense to `bge-m3` and sparse to the new dedicated SPLADE model. `04-architecture.md` gains: a corrected reference-stack summary line (§1), a split Embedding (dense) / Embedding (sparse) row pair replacing the single unified row (§4.1's tech-stack table), a VRAM-budget open-gap note at both the summary level (§4.2.2's supersede-note list) and inside the allocation table itself (new unsized SPLADE row, headroom figures flagged as understated rather than silently left looking complete). `05-data-model.md`'s `embedding_sparse` field description and Decision References footer corrected. `07-database.md`'s payload description corrected, plus a new open-question note on `embedding_model_version` now covering two independently-versionable models (not resolved — flagged for whoever builds the V2 blue/green re-embedding automation). `00-index.md`'s `13-decision-log.md` row updated (through DEC-142, summary blurb added) — `04-architecture.md`'s own Key-ids row left unchanged, matching the pre-existing pattern that this curated list was already selective and didn't carry DEC-086 either. `confirmed-context.md`'s Drift Log gains the corresponding row (below). `ingest/embedding.py` and its tests updated in the same work session (code side of this same fix, tracked in `document-ingest-pipeline` Issue 01's own file, not duplicated here).

**Gate 5 — PASS, with a real, explicitly-open gap.** The embedding-serving mechanism correction itself is complete and internally consistent. What is **not** resolved: §4.2.2's VRAM allocation table has carried a ~1.7 GB warm-cache headroom figure since DEC-109 that never accounted for a second TEI-served model — this note adds the missing row as a flagged unknown rather than estimating a number to fill it in, since no SPLADE model has been chosen yet and inventing a figure would repeat the exact "verified summary vs. actually measured" gap DEC-141 named for a different dependency. Whoever picks the production SPLADE model owes a real VRAM measurement before this ships to a customer deployment; this is recorded as an open item, not silently assumed to fit.

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TASK`/`VG` id was needed or minted — DEC-142 corrects which model produces one of REQ-003's two required vector types, not what REQ-003 itself requires. Checked `docs/adr/` — still does not exist, nothing to supersede. Both in-flight `.scratch/` references handled per this skill's implemented-vs-not-yet-implemented rule (see "What was checked" above) rather than uniformly rewritten.

**Final verdict: READY, unchanged.** A same-project instance of a pattern worth watching, not just this one fix: a claim traveled from DEC-086 (original, 2026-07-03) through a cross-model review specifically designed to catch same-family blind spots (2026-07-13) and still carried a partial error 12 days later, closed only when the code that actually depended on the claim being true needed to call the real API. Cross-model review narrows the same-family blind spot; it does not replace actually running the thing.

## Re-audit Note (2026-07-14, DEC-140 payload-index correction, DEC-141)

**Scope**: `update-specs` run to record DEC-141, a correction to DEC-140 (recorded earlier the same day). Re-checks **Gate 8** (Cross-Spec Consistency — corrects the same set of files DEC-140 just touched, so they don't carry forward an overclaim). Gates 1-7, 9 are **not** re-checked — same reasoning as DEC-140's own note: no product/scope/architecture/AI-agent-behavior/build-plan-task/SaaS content was touched, and TASK-007's acceptance criteria themselves are unchanged, only which tier can satisfy one of them narrows back.

**Context**: found while gating TASK-007's draft acceptance criteria through `verifiable-acceptance-criteria`, before publishing it as an issue via `to-issues` — i.e., before DEC-140's claim was ever acted on downstream. Actually running `QdrantClient(":memory:")`'s `create_payload_index()` against the installed `qdrant-client` 1.18.0 (rather than resting on DEC-140's WebSearch-sourced summary) produced a real `UserWarning: Payload indexes have no effect in the local Qdrant` and an empty `payload_schema` on introspection — a stronger gap than DEC-140's "not guaranteed production-grade" framing implied. The other two claims in DEC-140 (dense+sparse vector config; Layer 1 filter/query semantics) were independently confirmed correct by an actual passing assertion in the same test run, so they're unaffected.

**What was checked before deciding**: re-grepped `DEC-140` across the same 8 files it touched a few minutes earlier in this same session (`00-index.md`, `11-test-plan.md`, `13-decision-log.md`, `14-spec-audit-report.md`, `confirmed-context.md`, `docs/agents/dev-environment.md`, `docs/testing.md`, `.scratch/document-ingest-pipeline/PRD.md`) to correct each one's carve-out language rather than assume the propagation list was identical without checking.

**Fix applied (DEC-141)**: `13-decision-log.md` gains the DEC-141 row (correcting, not editing, DEC-140 — same append-only pattern DEC-138 used against DEC-134). All 8 files DEC-140 touched are corrected to the three-way split: vector config and filter semantics genuinely Tier-1/agent-executable; payload-index *existence* stays Tier 3, with a new Tier-2 static-call-inspection option added as a middle ground (confirm the setup code calls `create_payload_index` for all 5 fields, without claiming the index empirically exists).

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TASK`/`VG` id needed — DEC-141 corrects how TASK-007's existing acceptance criteria get verified, same relationship DEC-140 itself has to those criteria. Checked `docs/adr/` — still does not exist, nothing to supersede. TASK-006/TASK-007 still have no issue published yet, so no `.scratch/` issue needed updating beyond the PRD note already covered under DEC-140's propagation.

**Final verdict: READY, unchanged.** A same-day self-correction, caught by actually running the check before anything downstream relied on the uncorrected version — the discipline `verifiable-acceptance-criteria`'s "during implementation, not just at drafting time" section exists for, applied here at drafting time itself, which is the cheaper place to catch it.

## Re-audit Note (2026-07-14, Qdrant local-mode verification narrowing, DEC-140)

**Scope**: `update-specs` run to record DEC-140 (narrows DEC-135's Tier-3 manual-verify bucket specifically for Qdrant, given `qdrant-client`'s embedded local-mode capability). Re-checks **Gate 8** (Cross-Spec Consistency — new DEC citation, `11-test-plan.md` Test Environments row amendment, `00-index.md` propagation). Gates 1-7, 9 are **not** re-checked — no product/scope/architecture/AI-agent-behavior/build-plan-task/SaaS content was touched, and no existing `TASK-###` acceptance criterion changed in substance (TASK-007's criteria are unchanged; only which verification tier can satisfy them narrows) — and carry forward as PASS/N/A from the prior audit.

**Context**: found while scoping TASK-006 (Postgres schema) and TASK-007 (Qdrant collection + payload indexes) for Phase 2 kickoff, before either had a PRD or issue drafted. Verifying `qdrant-client`'s testing capabilities live (WebSearch against the qdrant-client GitHub repo and maintainer Discussion #683) surfaced its embedded/in-memory local mode — not previously named anywhere in `specs/` — which changes what DEC-135's Tier-3 bucket actually needs to contain for this one dependency.

**What was checked before deciding**: grepped `DEC-135` across `specs/` (`00-index.md`, `11-test-plan.md`, `13-decision-log.md`, `14-spec-audit-report.md`, `confirmed-context.md`) and `.scratch/` (`document-ingest-pipeline/PRD.md`, the 2026-07-13 cross-model review report, `session-feedback.md`), and `qdrant-client` across `specs/`/`docs/` (`13-decision-log.md`, `14-spec-audit-report.md`, `confirmed-context.md`, `docs/agents/dev-environment.md`) per this skill's Step 1. Confirmed the local-mode claim against a live source rather than asserting from training-data recall, per `CLAUDE.md`'s dependency-version-claims discipline — the same discipline that caught the LangGraph version staleness (DEC-131).

**Fix applied (DEC-140)**: `13-decision-log.md` gains the DEC-140 row (a narrow amendment to DEC-135, append-only, same pattern DEC-138 used against DEC-134). `11-test-plan.md`'s "Agent execution sandbox" row gains a Qdrant-specific carve-out sentence, citing DEC-140; its Decision References footer gains DEC-140. `00-index.md`'s Key-ids rows updated for both `13-decision-log.md` (through DEC-140, corrected line/size count) and `11-test-plan.md` (Qdrant carve-out mention). `docs/agents/dev-environment.md` and `docs/testing.md` updated with the same carve-out (outside `specs/` but within this skill's propagation responsibility, since `docs/testing.md` is the live "how to write a test here" doc this finding directly changes). `confirmed-context.md`'s Drift Log gains the corresponding row. `.scratch/document-ingest-pipeline/PRD.md`'s Further Notes DEC-135 citation updated — its own acceptance-test line cites Qdrant retrieval, and while that PRD covers TASK-008/009 (not TASK-007 itself), the underlying Qdrant-verification-strength claim it made is now narrower/stronger and worth correcting since the PRD hasn't been implemented yet.

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TASK`/`VG` id was needed or minted — DEC-140 constrains how TASK-007's existing acceptance criteria get verified, not what they assert, the same relationship DEC-135 already has to the `TEST-###`/`VG-###` rows it governs. Checked `docs/adr/` — still does not exist, nothing to supersede. Checked `.scratch/` for in-flight issues: only Phase 1 (closed) and the un-issued `document-ingest-pipeline` PRD exist; the PRD's citation updated above, no issue needed updating since TASK-006/TASK-007 have no issue yet.

**Final verdict: READY, unchanged.** A verification-strategy amendment for a not-yet-drafted Phase 2 task is closed ahead of its issue being written, following the same "decide before an agent guesses wrong" discipline DEC-135 and DEC-139 already established — applied here to a single-dependency carve-out rather than an environment-wide default or a whole test category.

## Re-audit Note (2026-07-14, performance-test regression-detection requirement, DEC-139)

**Scope**: `update-specs` run to record DEC-139 (performance tests TEST-032/033/034/041 must implement baseline-regression detection, not just fixed-threshold gating, once Phase 2+ builds them). Re-checks **Gate 8** (Cross-Spec Consistency — new DEC citation, `11-test-plan.md` amendment, two pre-existing `00-index.md` citation gaps found and closed in the same pass). Gates 1-7, 9 are **not** re-checked — no product/scope/architecture/AI-agent-behavior/build-plan-task/SaaS content was touched, and no acceptance criterion for any existing `TASK-###` changed — and carry forward as PASS/N/A from the prior audit.

**Context**: found while drafting `CLAUDE.md`'s "Closing out a work cycle" checklist (a separate, non-spec change) — writing "run performance tests" as a checklist step surfaced the question of what that would actually mean once TEST-032/033/034/041 exist, and the existing spec text only answers "clear this fixed threshold," not "hasn't regressed from where it was." Not a bug in existing content — NFR-005/006/022/027 and their paired TEST-### rows are internally consistent as written — but a real methodology gap for whoever implements these tests in Phase 2+.

**What was checked before deciding**: grepped `TEST-032`/`TEST-033`/`TEST-034`/`TEST-041` across `specs/` (found in `11-test-plan.md` and one `12-verification.md` row, `VG-023`) and `.scratch/` (found only in the 2026-07-13 cross-model review report, not in any in-flight, not-yet-implemented PRD/issue — no `.scratch/` propagation needed). Checked `specs/10-build-plan.md` for a `TASK-###` implementing any of these four tests — none exists yet, consistent with this being genuinely unscheduled Phase 2+ work, so no build-plan task needed updating either. Confirmed directly (not assumed) that every module beyond `api/` (`acl/`, `retrieve/`, `rerank/`, `generate/`, etc.) contains only an empty `__init__.py` — there is no perf-sensitive code anywhere in the repository yet, which is why this note pins a requirement rather than building or testing anything now.

**Fix applied (DEC-139)**: `13-decision-log.md` gains the DEC-139 row. `11-test-plan.md`'s Performance Tests section gains a "Threshold gate vs. regression detection" note directly under the TEST-032/033/034/041 table, citing DEC-139; its Decision References footer gains both DEC-139 and DEC-135 (the latter was already cited in this file's Test Environments table but had never been propagated to the footer — a pre-existing gap closed while this footer was already being touched). `00-index.md`'s Key-ids rows updated for both `13-decision-log.md` (through DEC-139, corrected stale line/size count) and `11-test-plan.md` (DEC-139's note, plus **TEST-041 itself**, which existed in `11-test-plan.md` since the 2026-07-13 cross-model review but had never been added to this row's `TEST-001..TEST-040` range).

**Gate 8 — PASS, with two pre-existing citation gaps closed while this row was already being touched.** `00-index.md`'s `11-test-plan.md` row still read `TEST-001..TEST-040` despite `TEST-041` existing in the actual file since 2026-07-13 (cross-model review R.17), and the same row's Decision References footer never cited `DEC-135` despite `11-test-plan.md`'s own Test Environments table citing it directly — both the same index-goes-stale-relative-to-content shape the cross-model review's R.27 finding named for `DEC-135` itself. Fixed alongside DEC-139's own propagation rather than left standing now that they were noticed, per this file's own "fix it when found" precedent (see DEC-135's re-audit note below, which did the same for `00-index.md`'s stale size figure). No new `REQ`/`NFR`/`TASK`/`VG` id was needed or minted — DEC-139 constrains how `TEST-032/033/034/041` get implemented, not what they assert, the same relationship DEC-135 has to the `TEST-###`/`VG-###` rows it governs. Checked `docs/adr/` — still does not exist, nothing to supersede.

**Final verdict: READY, unchanged.** A test-methodology gap for not-yet-built Phase 2+ performance tests is now closed ahead of time, following the same "decide before an agent guesses wrong" discipline DEC-135's own re-audit note already established — applied here to a narrower, single-section gap rather than an environment-wide default.

## Re-audit Note (2026-07-13, first cross-model review, DEC-136/137/138)

**Scope**: this is not a routine `update-specs` re-audit — it's the first time `specs/` itself has been reviewed by a model outside the Claude family (DeepSeek, `.scratch/review-reports/specs-cross-model-review-2026-07-13.md`), closing a gap this audit report's own history demonstrates: ~10 self-audit iterations since 2026-07-06, every one returning READY/PASS, all conducted by the same model family that wrote the content being checked — the exact same-evaluation-bias pattern `DEC-130` names for the product's RAG judge model, never before applied to this audit process itself (the cross-model review's own R.14 finding). The review used its own 5-dimension rubric (cross-reference integrity, unverified technical claims, requirement/architecture soundness, overconfidence/groupthink markers, domain plausibility) rather than this file's 9-gate framework, so this note maps findings onto gates after the fact rather than re-running the original checklist verbatim. Re-checks **Gate 1** (Confirmation Integrity — R.5 touched an "open question" claim), **Gate 5** (Architecture Readiness — R.4/R.10/R.18/R.19/R.26/R.29 touch `04-architecture.md` content directly), **Gate 6** (AI Agent Readiness — R.20 touched `20-agent-behavior.md` refusal UX), and **Gate 8** (Cross-Spec Consistency — the majority of the 30 findings). Gates 2, 4, 7, 9 are **not** re-checked — no research-methodology, build-plan-acceptance-criteria, or SaaS-tenancy content was touched — and carry forward as PASS/N/A from the prior audit.

**What the review found**: 30 findings (6 CRITICAL, 9 HIGH, 10 MEDIUM, 5 LOW), verdict BLOCK at review time. All 30 triaged with the user the same day; full detail and per-finding reasoning is in the review report itself and `confirmed-context.md`'s Drift Log row for this date — not re-narrated here. Three new decisions minted: **DEC-136** (closed DEC-131's long-deferred benchmark version-sweep; found and evaluated Llama Guard 3-vs-4, kept Guard 3 8B for MVP), **DEC-137** (concrete mechanism for DEC-091's KV-cache legal-hold invalidation, generalizing DEC-109's existing answer-cache pattern), **DEC-138** (softened DEC-134's Python 3.14 verification-confidence framing, added a `[manual-verify]` item for the Linux/`manylinux`-wheel gap the original verification didn't cover).

**Gate 1 — PASS, with a correction.** R.5 found `23-evals-guardrails.md` §8's Open Question 3 phrased as if NFR-004's "100% citation hit-rate" were itself in doubt — it isn't; NFR-004 is a narrow, mechanically-guaranteed invariant (verified by construction via REQ-005's strip/refuse check), not an empirical accuracy claim. Reworded to ask what's actually open (refusal-rate/usability on uncurated corpora) instead. This file's original Gate 1 evidence ("no open questions found in any final spec") is narrowly still accurate — the question wasn't a genuine unresolved decision, it was mis-scoped — but the mis-scoping itself is exactly the kind of internal-consistency gap a same-family reviewer is prone to miss, per the finding's own analysis.

**Gate 5 — PASS, with real gaps closed.** R.19 is the most substantive: DEC-091's KV-cache legal-hold invalidation had never had an implementable mechanism specified (now DEC-137). R.10 named a genuine, previously-uncovered TOCTOU window between Layer 2 authorization and response delivery (extended into RISK-017, not a new risk row — same risk family, different mechanism). R.4/R.18/R.26/R.29 were narrower cross-reference/staleness fixes (a stale NLI model name in the §14 summary table, a genuine 100ms-vs-150ms rerank latency contradiction reconciled across 5 downstream figures, a misleading ColBERT-serving claim corrected via live source, an unvalidated batching-latency assumption flagged for hardware-rig rehearsal). None of these change the architecture's fundamental soundness — DEC-082's layered-safety-rail design, the two-layer ACL model, and the verification pipeline all held up under review — but Gate 5's original "PASS" evidence didn't account for any of them, since none were checked by the original architecture-readiness pass.

**Gate 6 — PASS.** R.20 found `04-architecture.md` §12.1's `policy_blocked` UX text didn't distinguish MVP's immediate-refusal behavior from V2's review-routing language, which `20-agent-behavior.md` §6 already correctly separates. Fixed by splitting the collapsed row to match §6's existing per-trigger granularity. Narrow, UX-text-only; doesn't affect the agent-behavior design this gate evaluates.

**Gate 8 — PASS, with the review's central finding fully absorbed.** The majority of the 30 findings are exactly what Gate 8 exists to catch: stale cross-references and propagation gaps a prior DEC didn't fully reach (R.4's DEC-037/DEC-034 table staleness, R.7's third recurrence of the "50-question" figure, R.8's DEC-082 AWQ propagation miss, R.13's NFR-033 attribute-table gap, R.27's own DEC-135 citation gap in `00-index.md`, which is a case of *this file's own prior re-audit note* claiming a propagation that hadn't actually happened). That every one of these existed after ~10 self-audit rounds, each of which nominally checks cross-spec consistency, is the review's central point: a same-family Gate 8 check verifies "the ids referenced exist," not "every file that should have been updated actually was" — the two are different questions, and this round's fixes close the specific instances found, not the structural gap that let them accumulate. No new `REQ`/`NFR`/`TEST`/`VG` id collisions found. `docs/adr/` still does not exist, nothing to supersede. `.scratch/document-ingest-pipeline/PRD.md` (the one in-flight, not-yet-implemented feature) checked directly against every DEC/NFR it cites (DEC-036, DEC-038, DEC-046, DEC-065, DEC-086, NFR-012) — none touched by any of the 30 findings' fixes, confirmed by direct grep, no update needed.

**Final verdict: READY, with an explicit caveat this note is adding rather than silently carrying forward.** Every CRITICAL and HIGH finding from this review is fixed — nothing in either tier remains open. But R.14's own point stands on its own merits regardless of this round's fixes: a verdict of READY from a self-audit process that has never once returned otherwise is weak evidence of correctness on its own, and this round's 30 findings (found by the first reviewer without that shared blind spot) are the proof. This is the first READY verdict on record that isn't purely self-graded — treat it as more trustworthy for that reason, not as confirmation the spec set is now exhaustively correct. The secondary patterns the review named but didn't file as individual findings (drift-log diffusion in `confirmed-context.md`'s §3-vs-drift-log split-truth surface, "precision theater" in unmeasured latency/VRAM figures presented with false precision, and a persistent gap between the spec's production-grade scope and a solo developer's delivery capacity) are not closed by this round and aren't gate failures — they're named here so a future reader doesn't mistake their absence from the Gates table for resolution.

## Re-audit Note (2026-07-13, Phase 2+ verification strategy, DEC-135)

**Scope**: `update-specs` run to record DEC-135 (Phase 2+ verification-tiering strategy for the agent execution environment's confirmed no-Docker/no-GPU/no-C++-toolchain ceiling, per `docs/agents/dev-environment.md`). Re-checks Gate 5 (Architecture Readiness — operational implications of the environment ceiling) and Gate 8 (Cross-Spec Consistency — new DEC citation, `11-test-plan.md` amendment). Gates 1-4, 6-7, 9 are **not** re-checked — no product/scope/UX/AI-agent-behavior/build-plan-acceptance-criteria/SaaS content was touched — and carry forward as PASS/N/A from the prior audit.

**Context**: this is not a bug fix, and it does not change any existing `TASK-###`'s acceptance criteria. Phase 1's only real endpoint (`/ready`, DEC-117) was deliberately schema-only with no live backend calls (Issue 03), so the question of how an agent verifies work against services it can't run never came up. Phase 2 onward genuinely needs to prove behavior against Qdrant/Postgres/vLLM/etc., and `docs/agents/dev-environment.md`'s 2026-07-11 probe already established that this agent's own execution sandbox has none of those available. DEC-135 answers that question ahead of the first Phase 2 issue being drafted, rather than after an agent has already guessed wrong on a specific acceptance criterion.

**What was checked before deciding**: whether the gap was already addressed elsewhere in `specs/` (it wasn't — `docs/agents/dev-environment.md` records the environment probe but no spec file connected that probe to a verification strategy) and whether the proposed tiering matches or diverges from `11-test-plan.md`'s existing Integration Tests table. It matches: the large majority of that table's `TEST-###` rows (`TEST-006`..`TEST-020`, `TEST-035`..`TEST-040`) already exercise stubs/fixtures rather than live services (`SafetyRailAdapter` stub swap, `ECMAdapter` stub, seeded/synthetic corpora), so DEC-135 formalizes an existing pattern as the default rather than inventing new architecture. The one real inconsistency found: `11-test-plan.md`'s Test Environments table's "Local/CI" row asserted "dockerized Postgres/Qdrant/Redis test instances" as if that were reachable by whoever runs the tests, without distinguishing a Docker-provisioned human/CI machine from the agent's own execution sandbox (which has no Docker at all, confirmed directly rather than assumed).

**Fix applied (DEC-135)**: `13-decision-log.md` gains the DEC-135 row (three-tier verification model: fake-backed unit/integration, contract/schema validation, live-service integration deferred to `[manual-verify]` with a named owner). `11-test-plan.md`'s Test Environments table gains a new "Agent execution sandbox" row, distinct from "Local/CI," citing DEC-135. `00-index.md`'s Key ids updated for both `13-decision-log.md` and `11-test-plan.md`; its `13-decision-log.md` size/line-count figure ("56K / 92 lines") was also found stale (predated DEC-109 onward) and corrected to the current figure while this row was already being touched. `confirmed-context.md`'s Drift Log gains the corresponding row. `CLAUDE.md`'s "Running and verifying this repo" section (added earlier this session, pre-dating this DEC) updated with a `(DEC-135)` citation now that the informal description it carried has a formal decision behind it.

**Gate 5 — PASS.** No architecture component, module map entry, or operational runbook changed — this is a verification-methodology decision layered on top of the existing architecture, not a change to it. `09-deployment-ops.md`'s Environments table (Dev/RunPod, Customer production, CI) is unaffected; DEC-135 only clarifies what subset of "Local/CI"-tier verification the agent itself can perform, which is a `11-test-plan.md` concern, not a deployment-architecture one.

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TEST`/`VG` id was needed or minted — DEC-135 is a verification-strategy decision, not a new requirement or a new test case; it constrains *how* existing and future `TEST-###`/`VG-###` entries get executed by an agent, not what they assert. Grepped both `DEC-135`'s underlying topic (`docker`, `manual-verify`, `dockerized`) and the specific entities it touches (Qdrant/Postgres/Redis/vLLM/TEI) across `specs/` and `.scratch/` — no other file asserts a conflicting verification-environment claim that would need reconciling. Checked `.scratch/` for in-flight issues: none open beyond Phase 1 (closed, `ready-for-human` on all three issues) — no issue needed updating. Checked `docs/adr/` — still does not exist, nothing to supersede there.

**Final verdict: READY, unchanged.** A verification-methodology gap for the upcoming build phase is now closed ahead of time, following the same "decide before an agent guesses wrong" discipline `verifiable-acceptance-criteria` already established for individual acceptance criteria — applied here one level up, to the environment-wide default those criteria will be checked against.

## Re-audit Note (2026-07-11, Python version re-pin, DEC-134)

**Scope**: `update-specs` run to propagate DEC-134 (Python 3.12 → 3.14, superseding DEC-033's version literal only — not its FastAPI/language-family choice). Re-checks Gate 4 (testable acceptance criteria — `TASK-002`'s pin list changed), Gate 7 (Build Plan Verification Compliance, scoped to `TASK-002`), and Gate 8 (Cross-Spec Consistency, scoped to every file citing DEC-033's Python version). Gates 1-3, 5-6, 9 are **not** re-checked — no product/scope/UX/AI-agent-behavior/SaaS content was touched — and carry forward as PASS/N/A from the prior audit.

**Context**: this is not a bug fix. DEC-133's re-audit (2026-07-08, immediately above) explicitly checked the Python 3.12 pin against the "false current-version claim" bug shape that hit LangGraph (DEC-075/131) and found no such defect — DEC-033 was, and remains, a deliberate, correct-at-the-time choice. DEC-134 supersedes it for an unrelated reason: the actual Phase 1 execution environment (this maintenance session's agent sandbox) has only Python 3.14.3 installed, with no 3.12 runtime available, and the user chose to re-pin rather than install a second runtime purely to match the spec.

**Verification performed before the decision was made** (not assumed): all 7 CPU-installable dependencies `TASK-002` pins (fastapi, uvicorn, langgraph, qdrant-client, psycopg[binary], redis, PyYAML) were confirmed via a live `pip install --dry-run` to resolve with zero conflicts under Python 3.14.3, cross-checked against live PyPI JSON metadata (each ships a `cp314` or `py3-none-any` wheel). vLLM and llama-cpp-python — the two pinned dependencies not verifiable in this environment — were confirmed to be blocked by reasons unrelated to the Python version (vLLM: `manylinux`-only wheels, GPU/Linux-only regardless of interpreter version; llama-cpp-python: missing local C/C++ build toolchain, already recorded as `[manual-verify]` before this DEC), so the re-pin does not newly break either.

**Fix applied (DEC-134)**: Python version updated from 3.12 to 3.14 in `04-architecture.md` (stack summary line, tech-stack table, §14 decision summary — DEC-033's own row left unedited, annotated instead), `07-database.md` (Alembic framework note), `10-build-plan.md` (`TASK-002`'s Verification Plan bullet + Decision References footer, which also picked up two pre-existing missing citations — DEC-131, DEC-133 — found while confirming the footer's list was exhaustive per this skill's Step 1), `00-index.md` (Key ids row for `04-architecture.md`, Stage-7-files row for `10-build-plan.md`), and the one affected, not-yet-implemented `.scratch/` issue (`01-project-scaffold-dependencies.md`).

**Gate 4 — PASS.** `TASK-002`'s acceptance criteria ("`pip install` succeeds with zero conflicts," "every pinned dependency importable") remain testable as written; only the target interpreter version named in the Verification Plan changed, not the criteria's shape or its command/expected-output pairing.

**Gate 7 — PASS for TASK-002 (the only task this round touched).** TASK-002 remains TDD-Exempt with complete Why/Verification Plan/Rollback Plan/Acceptance Criteria/Verification Evidence; the version change is a substitution within the existing Verification Plan bullet, not a structural change.

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TEST`/`VG` id was needed or minted — this is a `DEC`-only change. Checked `.scratch/` for other affected in-flight issues — grepped both "Python 3.12" and "DEC-033" across `.scratch/`; only Issue 01 (`01-project-scaffold-dependencies.md`) matched, and it's updated. Checked `docs/adr/` — still does not exist, nothing to supersede there. Checked for any other spec file asserting "Python 3.12" that this round might have missed — `02-requirements.md`, `01-product-brief.md`, `06-api-contracts.md`, `20-agent-behavior.md` were grepped and do not cite the version number directly (only `04-architecture.md`, `07-database.md`, `10-build-plan.md` did, all three now updated).

**Final verdict: READY, unchanged.** A deliberate, verified re-pin — not a defect fix — fully propagated across every file that asserted the old version number.

## Re-audit Note (2026-07-08, general re-audit — llama-cpp-python coverage gap, DEC-133)

**Scope**: User requested a general re-audit independent of any specific known change, modeled on DEC-129's NFR→TASK sweep — specifically checking whether any other recently-added DEC (127-132) had the same failure shape as the LangGraph version gap: a decision introducing a capability without full build-plan coverage. This re-checks Gate 7 (Build Plan Verification Compliance, scoped to TASK-002/TASK-029) and Gate 8 (Cross-Spec Consistency). Gates 1-6, 9 are **not** re-checked — no product/scope/UX/architecture-alternative/AI-agent-behavior/SaaS content was touched — and carry forward as PASS/N/A from the prior audit.

**Finding**: DEC-130 (2026-07-08) introduced `llama.cpp` as the RAGAS judge model's runtime. `TASK-029`'s Green step correctly references it, but `TASK-002` — the task that pins and verifies every core dependency — was never updated, and `TASK-029` had no acceptance criterion for the judge model's GGUF weights actually being present/loadable. Found by grepping the underlying entity (`llama.cpp`/`llama_cpp`) across `specs/` rather than trusting DEC-130's own citation list, per `update-specs`'s Step 1 discipline.

**Fix applied (DEC-133)**: `llama-cpp-python` added to `TASK-002`'s pin list (`10-build-plan.md`) and to `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` (not yet implemented, updated in place). New `TASK-029` acceptance criterion + verification-evidence line added for judge-model weight presence (fail-fast, not silent fallback).

**Gate 7 — PASS for TASK-002 and TASK-029 (the only tasks this round touched).** TASK-002 remains TDD-Exempt with complete Why/Verification Plan/Rollback Plan/Acceptance Criteria/Verification Evidence; the new pin is an addition within the existing Verification Plan bullet, not a structural change. TASK-029 remains full-TDD-template-compliant (Red/Green/Refactor/Acceptance Criteria/Verification Evidence all present before and after this addition).

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TEST`/`VG` id was needed — the addition extends `TASK-029`'s existing acceptance-criteria/verification-evidence structure, the same pattern DEC-129 used for its traceability-only gaps. Checked `.scratch/` for other affected in-flight issues — only Issue 01 (`01-project-scaffold-dependencies.md`) cites `TASK-002`; updated. Checked `docs/adr/` — still does not exist. Also checked (and did **not** find equivalent gaps in): Postgres 16 / Redis 7.x / Valkey 8.x / Python 3.12 version pins — none of their governing DECs (033, 034, 076) assert a "current/2026-default" claim the way DEC-075 falsely did for LangGraph, so these are deliberate stable-version choices, not the same bug shape; not fixed, not flagged as a gap.

**Final verdict: READY, unchanged.** A genuine build-plan coverage gap (decision made, dependency never pinned) is now closed — the same category of gap this project has closed before (DEC-129), applied to a different id.

## Re-audit Note (2026-07-08, LangGraph API-compatibility follow-up closed — DEC-132)

**Scope**: Closes the follow-up DEC-131 explicitly left open — whether LangGraph's 0.x→1.x transition breaks the typed-state/feedback-edge design in `04-architecture.md`. Searched LangGraph's GitHub PR/issue history directly (not re-derived from training data) for breaking-change reports around and since the 1.0 release (2025-10-17): every hit found is in `langgraph-prebuilt`, `langgraph-checkpoint-postgres`, `aiosqlite` (upstream), or `stream_mode='values'` interrupt semantics — none touch `StateGraph`, `add_node`, `add_edge`, `add_conditional_edges`, typed state schemas, or cyclic/feedback edges. No gate is affected — no spec content changed, only a previously-flagged open question was checked and closed. Gates 1-9 all carry forward unchanged from the prior audit.

**Fix applied (DEC-132)**: `04-architecture.md` §2's Stack line and Round 3 framing blockquote gain a pointer to this confirmation; `13-decision-log.md` DEC-132 records the evidence and the honest caveat that no single official migration-guide document was located (two guessed URLs 404'd/redirected) — this is strong circumstantial evidence, not document-confirmed certainty.

**Also closed in this round**: `confirmed-context.md`'s Drift Log was missing a row for DEC-130 (the RAGAS judge model decision) — noticed while adding DEC-131's row, backfilled retroactively rather than left as a standing gap. Both DEC-130's and DEC-131's rows now sit correctly in the Drift Log's chronological sequence.

**Final verdict: READY, unchanged.** Both items closed this round are verification/bookkeeping closures — a risk question answered and a missing record backfilled — not new architecture decisions or reopened scope.

## Re-audit Note (2026-07-08, LangGraph version correction — DEC-131)

**Scope**: User cross-checked LangGraph's actual current version against `reference.langchain.com/python/langgraph` and `gh api repos/langchain-ai/langgraph/releases` directly, finding the spec's pinned "LangGraph 0.2.x" (DEC-075, DEC-082) was already stale the day it was decided — GitHub shows `1.2.5` published 2026-06-12, seventeen days before DEC-075's 2026-06-29 date, and `1.2.8` published 2026-07-06. Fix scope was explicitly narrowed by the user to the version number only — not a re-verification of whether LangGraph's 0.x→1.x API changes affect the specific typed-state/feedback-edge usage described, and not a broader re-audit of `92a-stage5r2-benchmark.md` for other version-sensitive claims (both flagged as separate, not-yet-done follow-ups). This note re-checks the gates this fix could plausibly affect: Gate 4 (Executable Quality), Gate 5 (Architecture Readiness), Gate 7 (Build Plan Verification Compliance, scoped to TASK-002/TASK-011), and Gate 8 (Cross-Spec Consistency). Gates 1-3, 6, 9 are **not** re-checked — this fix introduced no new product/scope confirmation, no AI-agent-behavior change, and no SaaS-multi-tenancy content — and carry forward as PASS/N/A from the prior audit below.

**Fix applied (DEC-131)**: "LangGraph 0.2.x" corrected to "LangGraph 1.2.x" in `02-requirements.md` (REQ-046), `01-product-brief.md`, `04-architecture.md` (every tech-stack/module-map/pipeline citation), `10-build-plan.md` (TASK-002's dependency-pin bullet, TASK-011's Green step), `20-agent-behavior.md`, and `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` (the one published, not-yet-implemented issue citing this pin). DEC-075's and DEC-082's own decision-log rows are left unedited, per this log's append-only convention — they accurately record what was decided/reasoned on 2026-06-29; DEC-131 is a new, separate row correcting only the living spec files' current-state assertions.

**Gate 4 — PASS.** The corrected acceptance criteria (REQ-046, TASK-002, TASK-011) remain concrete and testable (pinned-version smoke-import, graph-traversal integration test) — only the version literal changed, not the shape of what's verified.

**Gate 5 — PASS, unaffected.** The architecture's chosen orchestration framework (LangGraph) and DEC-082's "typed execution graph; runtime is a swappable detail" framing are unchanged — this fix corrects a version number the framing already anticipated would need to move, not the architectural decision itself.

**Gate 7 — PASS for TASK-002 and TASK-011 (the only tasks this round touched).** Both already had complete Red/Green/Refactor/Acceptance-Criteria/Verification-Evidence (TASK-002 is TDD-Exempt with its own Verification Plan/Rollback Plan) before this round; the version-string edit did not remove or restructure any required section.

**Gate 8 — PASS, re-verified by direct grep.** A full sweep (`grep -rn "0\.2\.x" specs/`) after all edits confirms zero remaining "LangGraph 0.2.x" references in any living spec file — the only surviving matches are DEC-075's own historical decision-log row and the Stage 5 process artifacts (`92-stage5-review-memos.md`, `92a-stage5r2-benchmark.md`), both intentionally left as an accurate historical record rather than rewritten. Checked `docs/adr/` — directory does not yet exist, so no ADR to supersede. Checked `.scratch/` for citations of the changed pin — found `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` (not yet implemented, updated in place per the update-specs skill's Procedure B step 6) and this skill-proposals gap-analysis draft under `.scratch/skill-proposals/` (not a published PRD/issue, left as-is).

**Final verdict: READY, unchanged.** This was a factual correction to a version literal, not a reopened architectural decision — the framework choice, its rationale, and DEC-082's version-drift-resistant framing all stand. The 0.x→1.x API-compatibility question is explicitly **not** resolved by this note and remains an open follow-up, not a hidden assumption.

## Re-audit Note (2026-07-08, RAGAS judge model gap — DEC-130)

**Scope**: `update-specs` skill closed a design gap where `92a-stage5r2-benchmark.md` §Topic 5's `high`-confidence non-negotiable ("LLM-as-judge with a *distinct* judge model") had no corresponding spec — no file named which model scores RAGAS's faithfulness/answer_relevancy metrics. Compounding the gap, `92-stage5-review-memos.md` R2-T6-02 and `11-test-plan.md`'s "AI Evals" section had both marked this `[CONFIRMED]`/satisfied on the strength of the NLI verifier's decoupling (DEC-037) — a different mechanism (runtime grounding classification) from the RAGAS LLM-as-judge the benchmark actually discusses. This note re-checks the gates this fix could plausibly affect: Gate 4 (Executable Quality), Gate 6 (AI Agent Readiness), Gate 7 (Build Plan Verification Compliance, scoped to `TASK-029`), and Gate 8 (Cross-Spec Consistency). Gates 1-3, 5, 9 are **not** re-checked — this fix introduced no new product/scope confirmation, no new architecture alternative, and no SaaS-multi-tenancy content — and carry forward as PASS/N/A from the prior audit below.

**Fix applied (DEC-130)**: RAGAS judge model = `Qwen2.5-14B-Instruct` int4 (GGUF), CPU inference via `llama.cpp`, invoked only by `cli eval run` (never GPU-resident, never a persistent service) — a different vendor/family from both generation-model candidates (Meta `Llama-3.1-8B-Instruct`, Mistral AI `Mistral-Small-24B-Instruct`). Specified in `04-architecture.md` §4.1 (new tech-stack row), §4.2.2 (explicit VRAM-budget exclusion note), §9.1 (docker-compose comment explaining the omission); `23-evals-guardrails.md` §2.1/§2.3 (judge identity + invocation lifecycle); `10-build-plan.md` TASK-029 (Green step + acceptance criterion + verification evidence for judge-model independence); `12-verification.md` VG-013 (extended to check judge/generation-model distinctness). `92-stage5-review-memos.md` R2-T6-02 split into R2-T6-02a (NLI decoupling, remains CONFIRMED) and R2-T6-02b (RAGAS judge decoupling, was the real gap, now resolved). `11-test-plan.md`'s AI Evals section corrected to describe both mechanisms as distinct, not one substituting for the other.

**Gate 4 — PASS.** DEC-130's acceptance criteria are concrete and testable: TASK-029's new bullet ("config/code inspection confirms the judge invocation targets a distinct local model... and makes no outbound network call") names a specific, checkable artifact (judge-model identifier vs. generation-model identifier per run), not vague language.

**Gate 6 — PASS, evidence corrected.** The original Gate 6 evidence line cited only NLI-model decoupling for "judge-model decoupling." Corrected to name both mechanisms explicitly: the RAGAS LLM-as-judge (`Qwen2.5-14B-Instruct`, DEC-130) and the NLI grounding verifier (`deberta-v3-base-mnli`, DEC-037) — both independently satisfy the gate's underlying criterion (`quality-gates.md`: "the eval judge is a different model/provider than the agent's active LLM"), and now for the right reason on the RAGAS side.

**Gate 7 — PASS for `TASK-029` (the only task this round touched).** TASK-029 already had all five required sections (Red/Green/Refactor/Acceptance Criteria/Verification Evidence) before this round; the new Green step, acceptance-criteria bullet, and verification-evidence line were added to the existing sections, not as a new task or a structural change. No TDD-Refactor gap was introduced or found.

**Gate 8 — PASS.** No new `REQ`/`NFR`/`TASK`/`TEST`/`VG` id was minted — this is a design-specification gap closure (naming a previously-unnamed model), not a new requirement, so it correctly reuses `VG-013`'s existing umbrella verification gate for `TASK-029` rather than manufacturing a new id for a sub-detail of already-covered scope (consistent with how DEC-129's NFR-003 golden-set half was folded into `TASK-029`/`VG-013` rather than given its own id). `DEC-130` is the new id minted, correctly placed in `13-decision-log.md` immediately after `DEC-129` with no renumbering. Checked `.scratch/` for any published PRD/issue citing RAGAS, judge models, or the touched files — none found, so no downstream reconciliation was needed. Checked `docs/adr/` — directory does not yet exist, so no ADR to supersede.

**Final verdict: READY, unchanged.** This was a genuine MUST-ALIGN gap (a `high`-confidence benchmark non-negotiable with no spec coverage, previously masked by an incorrect `[CONFIRMED]` label) now closed with a concrete, air-gap-compliant, resource-budgeted design — not a new architecture alternative or a reopened settled decision.

## Re-audit Note (2026-07-08, NFR→TASK traceability sweep)

**Scope**: this is the first audit pass to check NFR→TASK coverage specifically — every prior Gate 8 pass (original audit + DEC-121..126) checked REQ→TASK coverage only. Requested independently of any specific spec change, as a general re-audit of this file and the codebase; re-verified twice across two separate sessions with identical results before treating findings as confirmed.

**Gate 8 — found 6 previously-undetected gaps, now closed.** Grep-based sweep of all 33 `NFR-###` ids against `10-build-plan.md`, `11-test-plan.md`, `12-verification.md`: **3 traceability-only gaps** — `NFR-021` (layered safety rails, a near-duplicate of REQ-048, already fully built by TASK-018/019/020/021 but never cross-cited), `NFR-020` (AU/NZ data-residency, already substantially satisfied by TASK-030's air-gap check but never cross-cited), `NFR-003` (English-only schema neutrality, half-covered by TASK-029's golden-set run with the schema-review half uncovered) — fixed by adding the NFR id to the relevant task(s)' Related Requirements, plus one new acceptance-criteria bullet on TASK-007 for NFR-003's schema-review half. **3 real functional gaps** (zero task, test, or verification-gate coverage) — `NFR-024` (cost-per-turn: the ¥0.10 ceiling existed since Stage 4 with no computation formula ever defined, making it unimplementable; DEC-129 defines one, grounded in DEC-068's already-cited GPU-hour reference rate and NFR-026's already-captured span duration, and extends TASK-027 + TASK-029 to implement it), `NFR-028` (mandatory/conditional vLLM flags, fully specified in the NFR's own text but never wired into a task; extends TASK-014), `NFR-007` (durable-state-survives-restart, distinct from TASK-003's model-cache check and TASK-010's ingest-job-resume check; new TASK-039 in Phase 6). New ids: **TEST-039, TEST-040** (`11-test-plan.md`); **VG-034, VG-035, VG-036** (`12-verification.md`); **TASK-039** (`10-build-plan.md`); **DEC-129** (`13-decision-log.md`).

**Cross-check — is this now exhaustive?** Re-ran the sweep after applying the fixes, and caught an overclaim in an earlier draft of this note in the process: **not** every `NFR-###` resolves via a `TASK-###`'s literal Related Requirements field — 17 NFRs (NFR-005, 006, 008-010, 013, 016-019, 022, 023, 025, 027, 029-031) still have zero literal citation there. What *is* true, re-verified directly rather than assumed: every one of the 33 `NFR-###` ids has **some** verification-step coverage — a Related Requirements citation, a `VG-###`, a `TEST-###`, or an architecture cross-reference in `04-architecture.md` — with zero having none of the four. That is the actual bar `10-build-plan.md`'s "Definition of Done" checklist item sets ("a verification step somewhere in Phases 1-6," not "cited in Related Requirements specifically"), and it now holds. This round's scope was closing the 6 NFRs with **zero** coverage of any kind, not adding a formal Related Requirements citation to every NFR that already had indirect coverage — the latter would be a documentation-completeness pass, not a functional gap closure, and is a reasonable candidate for a future dedicated pass but was not this round's job.

**A note on how this was found, for future audits**: the gap existed because Gate 8's re-audits (DEC-121..126) were each scoped to specific named findings from a specific prior report, not a from-scratch sweep of every ID namespace — a reasonable scoping choice per-round, but it meant no round ever asked "does NFR→TASK coverage hold" as its own question, only "are the reported REQ orphans fixed." Recorded here so a future full-namespace sweep (REQ, NFR, RISK, DEC, TASK, TEST, VG, all pairwise) is treated as a distinct, periodically-worthwhile check, not assumed subsumed by narrower re-audits.

**Not found in this pass, still worth a dedicated future pass (not fixed here — out of this round's scope)**: `TASK-014` had zero `TEST-###`/`VG-###` coverage before this round (now covered for NFR-028 specifically via TEST-040/VG-035, but its own `REQ-004` acceptance criterion — "`reranked_set` correctly derived..." — still has none). `TASK-030`/`TASK-031` similarly have no `TEST-###`/`VG-###` backing beyond what this round added to TASK-030 for NFR-020. Whether every `TASK-###`'s own primary acceptance criterion (not just the NFR/REQ cross-references layered onto it) has TEST/VG backing was not swept this round — a natural next audit question.

**Final verdict: READY, unchanged.** All 6 gaps were spec-completeness/cross-reference gaps inside already-decided scope (NFR-024's cost formula is the one exception — a genuine missing decision, resolved by reusing an already-established reference figure rather than inventing a new one, consistent with this file's own "no invented decisions" discipline). None required reopening a settled DEC or a new product/scope confirmation from the user beyond confirming the cost-formula approach.

## Re-audit Note (2026-07-08, Gate 7 batch fix)

An independent Gate 7 re-audit (programmatic sweep of every `10-build-plan.md` task tagged "Verification Pattern: TDD" for Red/Green/Refactor/Acceptance Criteria/Verification Evidence completeness) found 13 tasks missing "TDD Refactor" — TASK-012, TASK-014, TASK-015, TASK-016, TASK-017, TASK-019, TASK-021, TASK-022, TASK-023, TASK-024, TASK-025, TASK-026, TASK-029 — and reported TASK-027 as already fixed in a separate same-day observability-spec round. Applying the fix found that second claim did not hold: opening `10-build-plan.md` directly showed TASK-027 in the same incomplete state as the other 13 (Red/Green/Acceptance-Criteria/Verification-Evidence present, Refactor absent), and no commit, branch, or decision-log entry corroborated a prior TASK-027 fix or an existing DEC-127. TASK-027 was folded into this fix round rather than left on a false paper trail (DEC-127).

**Gate 7 — re-verified PASS.** Post-fix, a full re-parse of `10-build-plan.md` (not a re-count of the reported list) confirms all 26 strict-TDD tasks now have exactly one "TDD Refactor" section each — 0 tasks with Red but no Refactor, 0 duplicates. The 12 TDD-Exempt tasks (unaffected by this fix, since "Why TDD-Exempt"/"Rollback Plan" is their own section pair, not "TDD Refactor") were spot-checked to confirm this edit did not touch them. Each added Refactor entry names a task-specific refactor (shared-logic extraction, boundary tightening, or duplication removal tied to that task's own implementation), not a generic placeholder line.

**Scope note**: Gates 1-6, 8, 9 were not re-checked in this pass — this re-audit is scoped to Gate 7's TDD-Refactor-completeness defect only, per the same narrow-scope convention the 2026-07-06 re-audits used. They carry forward as PASS/N/A from the note below.

**Reconciliation note (added at merge time)**: this round ran as an isolated background task in a separate git worktree and had no visibility into the concurrently-running DEC-128 observability round's uncommitted work in the main working tree — which had, in fact, already added TASK-027's own Refactor section before this round started. This round's conclusion ("no prior commit... corroborated a prior TASK-027 fix") was accurate given what was actually committed at the time; the discrepancy was a session-isolation artifact, not an error by either round. TASK-027's final Refactor section merges both rounds' content — the generic span-lifecycle wrapper from this round plus the domain-specific attribute-collection helper from DEC-128 — see `10-build-plan.md`. Both DEC-127 (this round) and DEC-128 (renumbered from a colliding DEC-127 at merge time) are preserved as separate, real decisions in `13-decision-log.md`.

## Re-audit Note (2026-07-08, post-Stage-8 content addition)

Scope: DEC-128 added `NFR-033` (`02-requirements.md`), amended `TASK-027` (`10-build-plan.md`), and added `TEST-038`/`VG-033` (`11-test-plan.md`/`12-verification.md`), closing an observability design gap (domain-specific span attributes, `nli_entailment_score` histogram, a trace-sampling wording fix, and an MVP-scoped trace-to-regression path) via the `update-specs` skill. This note re-checks only the gates this addition could plausibly affect — Gate 4 (Executable Quality), Gate 7 (Build Plan Verification Compliance, scoped to `TASK-027`), and Gate 8 (Cross-Spec Consistency). Gates 1-3, 5-6, and 9 are **not** re-checked — this addition introduced no new product/scope confirmation, no new architecture alternative or Stage 5 review topic, and no AI-agent-loop or SaaS-multi-tenancy content — and carry forward as PASS/N/A from the prior audit below.

**Gate 4 — PASS.** `NFR-033`'s statement names concrete attributes per span (not vague language), ties each to the specific node that emits it, and its acceptance seed is directly testable (span inspection against the same query's `audit_events` row). `TEST-038`/`VG-033` give it a real, non-generic test and verification gate, not a placeholder.

**Gate 7 — PASS for `TASK-027` (the only task this round touched), with a separately-flagged pre-existing gap found and not fixed.** `TASK-027` was missing its "TDD Refactor" section before this round (present: Red, Green, Acceptance Criteria, Verification Evidence; absent: Refactor) — a real, pre-existing Gate 7 defect that predates DEC-128 and was not introduced by it. Added a substantive Refactor section as part of this round's edit (extracting shared per-node attribute-collection instrumentation) since the task was already being edited. **Independent verification found this is not isolated**: a programmatic sweep of all 26 strict-TDD (non-exempt) tasks in `10-build-plan.md` found **13 other tasks also missing "TDD Refactor"** — TASK-012, 014, 015, 016, 017, 019, 021, 022, 023, 024, 025, 026, 029. This was not part of DEC-128's scope and is **not fixed in this round** — flagged as a follow-up (a targeted Gate 7 remediation pass across these 13 tasks), not silently carried forward as if it doesn't exist. **Resolution (added at merge time)**: this follow-up was spawned as a background task and completed independently as the "Gate 7 batch fix" round above (DEC-127) — all 13 tasks plus TASK-027 are now fixed. This does not change the overall `READY` verdict below, following this file's own established precedent of naming a real, out-of-scope gap explicitly rather than either fixing everything unscoped or hiding the finding (see the CI-check recommendation in the prior audit's Final Notes for the same pattern).

**Gate 8 — PASS.** Full repo-wide ID sweep re-run after this round's additions, and again after the merge-time DEC-127/DEC-128 renumbering: DEC 128/128 (128 unique rows, max id DEC-128, zero duplicates post-renumber), NFR max 033 (33 unique in `02-requirements.md`), TASK max 038 (unaffected by this round), TEST 38/38, VG 33/33 — all resolve, zero orphans, zero duplicate definitions. Traced `NFR-033` → `TASK-027` (`10-build-plan.md`) → `TEST-038` (`11-test-plan.md`) → `VG-033` (`12-verification.md`) end-to-end by opening all four files directly; chain is real and specific, not a generic placeholder. Cross-file propagation independently confirmed: `04-architecture.md` §12.3, `08-observability-logs.md` (Span Structure, Metrics, Traces, Privacy Rules, Dependencies), `23-evals-guardrails.md` (§2.2 sampling-wording correction, §6 trace-promotion path, §7 histogram cross-reference, §9 cross-reference table), `24-prompt-registry.md` (trace-attribute cross-reference), `09-deployment-ops.md` (sampling re-evaluation trigger, reusing the existing REQ-026 V3 concurrency threshold rather than minting a new one), and `00-index.md` (Group C reassignment + Stage 6 table's `08-observability-logs.md` row, which previously read "None new" and would have been stale had it not been updated). Checked `.scratch/` for any published PRD/issue citing the touched IDs — none found, so no downstream reconciliation was needed this round. Checked `05-data-model.md`/`07-database.md` — `otel_spans.attributes` is already JSONB, so no schema migration was needed or introduced; confirmed by direct inspection, not assumed. Checked `42-compliance-security.md`'s `otel_spans` classification (`Internal/operational, No customer content`) — still holds, since the new attributes are scores/counts/version strings, not content; no edit needed there.

**Final verdict: READY, unchanged.** The new content (`NFR-033`, `TASK-027` amendment, `TEST-038`, `VG-033`) passes every gate it was checked against. The pre-existing, out-of-scope Gate 7 gap found on 13 other tasks does not retroactively fail this verdict — consistent with this file's own precedent of naming real findings without expanding an audit's scope to fix everything discovered along the way — but should not be left un-actioned indefinitely either.

## Prior Re-audit Note (2026-07-06, superseded by the three 2026-07-08 notes above)

Gates 7 and 8 were re-run independently after `spec-writer` claimed fixes (DEC-121 through DEC-124). Gates 1-6 and 9 were **not** re-checked, per the coordinator's scoped re-audit instruction, and are carried forward unchanged from the original audit below.

**Gate 7 — still FAIL, different reason.** Spec-writer's claim that all 12 TDD-Exempt tasks now have both "Why TDD-Exempt" and "Rollback Plan" sections is **confirmed true** — I found the same 12 exempt tasks independently (TASK-001, 002, 003, 004, 006, 007, 010, 028, 030, 031, 032, 037, matching spec-writer's list including the 2 extra beyond my original 6) and every one now has both sections with substantive, non-placeholder content. However, a new structural defect exists that spec-writer's summary did not mention: **TASK-037's "Acceptance Criteria" and "Verification Evidence" sections are missing from TASK-037's own block** — they were misplaced (duplicated) at the tail end of TASK-038's block instead (`10-build-plan.md` lines 468-474, which read "Seeded orphan document correctly flagged..." and "TEST-020 output (reconciliation-crawl integration test)" — content that belongs to TASK-037's reconciliation crawl, not TASK-038's generation-model swap). This leaves TASK-037 incomplete and TASK-038 with two duplicate "Acceptance Criteria"/"Verification Evidence" headings. This is an editing/formatting defect introduced during the fix pass, not present in my original audit.

**Gate 8 — still FAIL, narrower scope.** Spec-writer's task-level claims are confirmed: all 30 MVP-tagged `REQ-###` (re-derived independently from `02-requirements.md`, correcting my own original list to exclude REQ-033, which is actually tagged **V2** not MVP — spec-writer built TASK-038 for it anyway, correctly framed as closing a different gap: a test/VG chain existed for it with no task) now resolve to at least one `TASK-###` via a direct "Related Requirements" grep. REQ-005a/REQ-005b are confirmed resolved in `23-evals-guardrails.md` (now documented as informal shorthand, not independent IDs). However, the **TASK → TEST → VG chain is still broken for three requirements** that spec-writer's fix pass did not touch, because it only edited `10-build-plan.md` (plus `02-requirements.md`/`23-evals-guardrails.md`/`13-decision-log.md`) and never updated `11-test-plan.md` or `12-verification.md`: REQ-050 (`SafetyRailAdapter` Protocol, new TASK-036) has a VG-020 row but **zero `TEST-###` entries** anywhere — VG-020's evidence column names only a vague, unnamed "Adapter-swap integration test," not a real test ID; REQ-037 (retention capture, TASK-009's cross-reference fix) has **zero VG row and zero TEST row**; REQ-043 (audit-pull API, new TASK-035) has **zero VG row and zero TEST row** despite now having a real implementing task. The new tasks (TASK-033 through TASK-038) were opened and read in full: each genuinely builds what it claims (TASK-033 wires real FastAPI routes against `06-api-contracts.md`'s schemas; TASK-034 implements the actual widget with CSP/postMessage/SRI per NFR-018; TASK-035 implements both the NDJSON pull endpoint and the ECM write-back call; TASK-036 correctly builds the `SafetyRailAdapter` Protocol as a refactor-in-front-of-TASK-018/020, not a reimplementation; TASK-037's Verification Plan is substantively about the reconciliation crawl; TASK-038 correctly implements REQ-033's swap-gate abstraction). The tasks are real and correct — the test-plan and verification-matrix layers simply were never brought into sync with them.

**Verdict**: still NEEDS REVISION. Both gates improved materially (the original 10-requirement build-task gap is fully closed; TDD-Exempt template compliance is now 12/12 on the two originally-checked sections) but each has one new or surviving concrete defect, detailed in the Gates table and Required Fixes below.

## Summary

*(Original audit summary below, from the first pass. Superseded where it discusses Gates 7/8 by the "Re-audit Note" above — kept for full history rather than rewritten, since Gates 1-6/9 findings in this section are still current and unchanged.)*

GroundedDocs's spec package (21 canonical/process files under `specs/`) is exceptionally disciplined in its decision-tracking, cross-referencing, and educational writing. Six architecture-review rounds plus two spec-generation phases produced a document set where nearly every cross-document claim I independently spot-checked actually resolved to real, correctly-placed content — including several deep chains (DEC-088's citation-verification-target fix, DEC-091/DEC-106/DEC-116's legal-hold cache-invalidation chain across three files, DEC-117's `/ready` full-dependency contract propagated through architecture, API contract, and requirements). The package's own repeated self-correction discipline (DEC-109 through DEC-120 alone fixed six distinct staleness/consistency bugs missed by five prior rounds) is a genuinely good sign, not a red flag — it means the process catches its own drift.

However, my own independent verification — which this audit's brief specifically required rather than trusting the prior rounds' self-reported sweeps — found a real, previously uncaught gap: **`10-build-plan.md`'s own "Definition of Done" claims "every REQ-### tagged MVP has at least one TASK-### tracing to it," and this claim is false.** Ten MVP-tagged requirements (REQ-008, REQ-009, REQ-010, REQ-033, REQ-037, REQ-043, REQ-045, REQ-050, REQ-056, and the REQ-006a/REQ-006d refusal-taxonomy specifics) have **no** `TASK-###` whose "Related Requirements" field names them — some of these (REQ-008 HTTP API, REQ-009 widget, REQ-010 admin API, REQR-043 audit-pull API, REQ-045 ECM write-back) have no implementing task at all, not even implicitly. Several of these same requirements *do* have a `VG-###` verification-gate row in `12-verification.md` and/or a `TEST-###` in `11-test-plan.md` pointing at evidence a task was supposed to produce — meaning the verification layer references work products the build layer never actually schedules. This is a Gate 8 (Cross-Spec Consistency) failure with a Gate 7 (Build Plan Verification Compliance) component.

Separately, and independently of the above, `10-build-plan.md`'s own TDD-Exempt task template compliance is incomplete: of 12 tasks marked `TDD-Exempt`, only 3 include the template-mandated "Why TDD-Exempt" section and only 5 include the mandated "Rollback Plan" section (`spec-templates.md` requires both on every exempt task). TASK-004, TASK-007, TASK-028, TASK-030, TASK-031, and TASK-032 are each missing one or both sections — this is systemic, not a single slip.

A smaller, cosmetic Cross-Spec Consistency issue: `23-evals-guardrails.md` cites `REQ-005a` and `REQ-005b` as if they were minted sub-requirement IDs; `02-requirements.md` never defines these — REQ-005's own acceptance criterion already describes the (a)/(b) mechanical/NLI split in prose, but no `REQ-005a`/`REQ-005b` ID exists to resolve to.

All other gates pass on independent verification, including a full independent ID-resolution sweep (REQ, NFR, DEC, RISK, TASK, TEST, VG namespaces) that found zero other orphaned or unresolved references, and direct spot-checks of five `RC-###` "Applied" claims (RC-T3-01, RC-T4-02, RC-T6-02, RC-T8-01, RC-T4-01) that all actually resolved to the claimed content in the claimed location.

## Gates

| Gate | Result | Evidence | Required Fix | Routed To |
|---|---|---|---|---|
| 1. Confirmation Integrity | PASS | `confirmed-context.md` read in full; scope (§3), risk-time (§5-6), product type incl. SaaS flag (§2), authority (§7) all confirmed with explicit user sign-off dates. Single canonical decision log (`13-decision-log.md`, 120 DECs) — no parallel decision documents found. Every specialist stage's output cites `confirmed-context.md` or `13-decision-log.md` as source. No open questions found in any final spec (see forbidden-token sweep below). | — | — |
| 2. Research Integrity | PASS | `90-stage1-trend-research.md` (skimmed per brief, Group E) carries dated claims, confidence tags (e.g. "[M, inferred]"), and explicit "unconfirmed" flags (e.g. `01-product-brief.md` §2 AU Privacy Act row: "[unconfirmed exact 2026 status — verify before vendor pitch]"). Stage 1 was run, not skipped, with documented rationale in `confirmed-context.md` §8. Competitor claims map to specific research sections and are revisited/corrected at Stage 5 (D2-01 finding, "empty quadrant" framing retired). | — | — |
| 3. Educational Quality | PASS | Every file opens with a "Plain-English Summary." Rationale is project-specific throughout, not generic "best practice" — e.g. `07-database.md` explains *why* Postgres/Qdrant/Redis split this way for *this* product's ACL/audit/cache needs, not abstract database theory. Tradeoffs and rejected alternatives are explicit and dated (DEC entries name what was rejected and why). Junior-developer test: `09-deployment-ops.md`'s runbooks, `04-architecture.md`'s worked pipeline pseudocode (§8.1), and `06-api-contracts.md`'s request/response examples all give a 1-2yr developer enough concrete detail to build without inventing behavior. | — | — |
| 4. Executable Quality | PASS | Concrete builder contracts throughout: `06-api-contracts.md` gives exact JSON schemas + examples per endpoint; `07-database.md` gives exact table/column/index definitions; `05-data-model.md` gives full field lists with source-decision citations. Edge cases are named and handled (e.g. malformed citation tokens → strict-mode rejection, `11-test-plan.md`; compound-document ACL resolution, DEC-114). Acceptance criteria are testable and measurable (e.g. NFR-005's exact latency budget table in `04-architecture.md` §7B.12). | — | — |
| 5. Architecture Readiness | PASS | Full tech stack (§4), module map (§5), diagrams (Mermaid ERD, flowcharts, sequence diagrams across `04-architecture.md`, `03-workflows.md`, `05-data-model.md`), 4 alternatives compared with scored rationale (§3.1), rejected mature architectures with reasons (LlamaIndex/Haystack/RAGFlow-fork, §3.2-3.3), failure modes (`20-agent-behavior.md` §6), operational implications (`09-deployment-ops.md`). Every Stage 5 review topic (T1-T8) has a memo entry and closure record in `92-stage5-review-memos.md`'s topic coverage matrix (all 8 CLOSED). T7 (multi-tenant) skip verified: `SKIP-NOT-APPLICABLE`, formally labeled in both `confirmed-context.md` §2 and `92-stage5-review-memos.md` §R5.4, with rationale (single-tenant on-prem, DEC-003). Spot-checked 5 `RC-###` "Applied"/"Fixed" pointers by opening the named file/section directly — all 5 resolved to real content: RC-T3-01 (measured-VRAM methodology) confirmed in `09-deployment-ops.md`; RC-T4-02 (encryption-at-rest) confirmed as a full runbook section in the same file; RC-T6-02 (driver matrix + ingest-resume) confirmed as two distinct subsections; RC-T8-01's remaining half (parser strict-mode + known-limitations appendix) confirmed in `11-test-plan.md`; RC-T4-01 confirmed as a legitimately-tracked V2 deferral, not a silent drop. | — | — |
| 6. AI Agent Readiness | PASS | Agent goals/non-goals (`20-agent-behavior.md` §2.2-2.3), autonomy boundary and allowed/forbidden actions (same, explicit "what the model may/may not do" lists), agent loop with hard iteration cap (`revision_count < 2`, NFR-023) and loop-detection (illegal-transition exceptions, §5.1.1), tool contracts (`sub_query_retrieve`, V2, one tool only), memory/context policy (`22-memory-context.md`, dedicated file), conversation-history provenance (server-reconstructed by default, client history unconditionally ignored — stronger than the gate's baseline "signed or justified" exception, `22-memory-context.md`'s own "Conversation History Provenance" section), judge-model decoupling — **two distinct mechanisms, both decoupled from the generation model**: the RAGAS LLM-as-judge (`Qwen2.5-14B-Instruct`, DEC-130, `23-evals-guardrails.md` §2.1) and the NLI grounding verifier (`deberta-v3-base-mnli`, DEC-037, confirmed in `11-test-plan.md`'s "AI Evals" section) — system-prompt version registry with rollback path (`24-prompt-registry.md`, dedicated file, versioned + immutable + config-pointer rollback), prompt-injection test set as executable cases (`23-evals-guardrails.md` §2.2's "Layered-rail trigger prompts" +15, "Parallel-edge join-correctness" +10), refusal-rate baseline + drift monitor (`01-product-brief.md` §9.1 baseline/target, `08-observability-logs.md`'s refusal-rate alert), citation/grounding verification at runtime (REQ-005, `verify/` node, DEC-088's ACL-bypass-closed target), cost ceiling per turn (NFR-024) and per session (implied via concurrency/queue caps — no explicit per-session ceiling found, but per-turn is present and this is not gate-failing on its own), human handoff (V2 review queue, `20-agent-behavior.md` §4, correctly deferred not silently dropped), logs and audit events (`08-observability-logs.md`, `05-data-model.md`'s `audit_events`). | — | — |
| 7. Build Plan Verification Compliance | **PASS** (final re-audit 2026-07-06) | Programmatically parsed all 38 tasks in `10-build-plan.md`: every task has exactly one "Acceptance Criteria" and exactly one "Verification Evidence" section (zero duplicates anywhere in the file). TASK-037 now correctly owns its reconciliation-crawl Acceptance Criteria + Verification Evidence; TASK-038 ends cleanly at its own single Verification Evidence with no trailing content. All 12 TDD-Exempt tasks (TASK-001, 002, 003, 004, 006, 007, 010, 028, 030, 031, 032, 037) independently re-confirmed to have both "Why TDD-Exempt" and "Rollback Plan." All 5 exempt-type labels map to the allowed types in `spec-templates.md`. | — | — |
| 8. Cross-Spec Consistency | **PASS** (final re-audit 2026-07-06) | All 3 previously-broken chains independently traced end-to-end by opening all four files directly: REQ-050 (`02-requirements.md` L108) → TASK-036 (`10-build-plan.md`) → TEST-036 (`11-test-plan.md`, real `SafetyRailAdapter` swap test) → VG-020 (`12-verification.md`, now correctly cites "TEST-036" by ID). REQ-037 (L45) → TASK-009 → TEST-035 (retention-capture-at-ingest test) → VG-031. REQ-043 (L47) → TASK-035 → TEST-037 (NDJSON-pull idempotency test) → VG-032. Full repo-wide ID sweep re-run: TASK 38/38, TEST 37/37, VG 32/32, DEC 126/126 — zero orphans. REQ sweep's apparent gaps (REQ-015/016/025/027/etc.) were confirmed to be a regex false-positive (unbolded V2/V3 rows, not missing definitions), not real orphans. REQ-005a/REQ-005b confirmed resolved via `REQ-005(a)`/`REQ-005(b)` parenthetical notation in `23-evals-guardrails.md` §3.2, with an explicit note that these are not independent IDs. | — | — |
| 9. SaaS Multi-Tenant Readiness | NOT APPLICABLE | Confirmed single-tenant on-prem per `confirmed-context.md` §2/§3 and DEC-003. Skip is not merely assumed — independently verified as formally recorded with the correct category (`SKIP-NOT-APPLICABLE`) in three places: `confirmed-context.md` §2, `00-index.md`'s Stage 6 skip table (slots 30-33), and `92-stage5-review-memos.md`'s Stage 5 topic-coverage matrix (T7 row, with a dedicated §R5.4 "bookkeeping, not re-litigation" formalization note dated 2026-07-05). Rationale is substantive (each install is a dedicated single-customer deployment; no tenancy concern arises at the architecture level), not a placeholder. | — | — |

Also verified: the five additional conditional-skip slots named in the audit brief (`03a-ui-design.md`, `21-tools-and-mcp.md`, `40-migration.md`, `50-analytics-events.md`, `51-data-pipeline.md`) each carry a stated skip category and rationale in `00-index.md`'s Stage 6 skip table, cross-referenced to a supporting DEC or memo (`91-stage3-ux-skip.md` for 03a; DEC-039 for 21; the greenfield-vs-migration distinction for 40; the no-growth-funnel rationale for 50; the ingest/CDC-pipeline-already-covered rationale for 51). All findable, none silently assumed.

## Traceability Matrix

| Goal | Requirement | Architecture Component | Build Task | Test | Verification Gate |
|---|---|---|---|---|---|
| Citation/grounding — "no answer without verified citation" | REQ-005 (runtime citation verification) | `04-architecture.md` §8.1 `verify/.mechanical_fast_path` + `.nli_slow_path`; DEC-088 (checks against `reranked_set`, not `retrieval_set`) | TASK-022 (`mechanical_fast_path`), TASK-023 (`nli_slow_path` + refusal decision) | TEST-001, TEST-002 (unit), TEST-021/022 (parser strict-mode) | VG-005 |
| Citation/grounding — hard 100% hit-rate gate | NFR-004 | `04-architecture.md` §8.5 (hardcoded, non-tunable); `08-observability-logs.md` (Critical alert on any deviation) | TASK-022 | TEST-001, TEST-002 | VG-022 |
| Legal-hold / audit — freeze does not leave stale content servable | DEC-091 (KV-cache invalidation), DEC-116 (answer-cache invalidation) | `04-architecture.md` §7B.6; `05-data-model.md` `legal_hold_invalidation_events` entity; `03-workflows.md` Workflow 5 | TASK-017 (retention+legal hold), TASK-026 (cross-layer cache invalidation) | TEST-011, TEST-012 | VG-025, VG-029 |
| Legal-hold / audit — remediation itself is provable | DEC-106 (KV-cache invalidation audit write) | `05-data-model.md` `legal_hold_invalidation_events`; `07-database.md` transaction-consistency note | TASK-026 | TEST-012 | VG-025 |
| Audit-ready by default — every turn traceable to exact model/prompt/threshold state | REQ-007, REQ-035 | `04-architecture.md` §5.1.1 (`audit_fingerprint` reducer), DEC-060/DEC-087/DEC-089; `05-data-model.md` `audit_events.context_fingerprint` (verbatim citation snapshot) | TASK-024 (`audit/` + context fingerprint) | (fingerprint-completeness test, described in TASK-024's own Verification Evidence) | VG-007 |
| Two-layer authorization — ACL never trusted from cache alone | REQ-036 | `04-architecture.md` §7B.1-§7B.4; `41-integration-contracts.md` | TASK-012 (Layer 1 filter), TASK-013 (Layer 2 JIT trim) | TEST-006, TEST-007 | VG-014 |
| Refusal as a product feature, not an error | REQ-006, REQ-006d (5-class taxonomy) | `04-architecture.md` §12.1; DEC-042/DEC-058 | TASK-023 | TEST-005, TEST-024 | VG-006 |
| Layered safety rails (anti-hallucination + injection defense) | REQ-048 | `04-architecture.md` §4.3 (`SafetyRailAdapter`), §8.1 pipeline | TASK-018 (`safety_input/`), TASK-019 (retrieval rail), TASK-020 (`safety_output/`), TASK-021 (`policy/`) | TEST-013, TEST-014, TEST-015 | VG-019 |
| Safety-rail swappability (vendor lock-in avoidance) | REQ-050 (`SafetyRailAdapter` Protocol) | `04-architecture.md` §4.3 | TASK-036 (Protocol abstraction, distinct from TASK-020's hardcoded adapter) | TEST-036 (real `SafetyRailAdapter` swap test) | VG-020 (now correctly cites TEST-036 by ID) |
| Audit pull for vendor SIEM forwarding | REQ-043 | `06-api-contracts.md` API-A-04 | TASK-035 (NDJSON pull endpoint) | TEST-037 (NDJSON-pull idempotency test) | VG-032 |
| Vendor-embeddable HTTP API surface | REQ-008 | `06-api-contracts.md` (full contract) | TASK-033 (real FastAPI route wiring) | (contract tests exist in `06-api-contracts.md` itself) | VG-008 |
| Embeddable chat widget | REQ-009 | `04-architecture.md` §5 (`widget/` module), DEC-040 | TASK-034 (real widget build, CSP/SRI/postMessage) | TEST-029 (widget CSP scan) | VG-009 |
| Reconciliation crawl | REQ-056 | `04-architecture.md` §7B.5 (DEC-090) | TASK-037 (own Acceptance Criteria/Verification Evidence confirmed correctly placed) | TEST-020 | VG-021 |
| Retention capture at ingest | REQ-037 | `04-architecture.md` §7B.3 | TASK-009 (cross-reference fix confirmed) | TEST-035 (retention-capture-at-ingest test) | VG-031 |

All fourteen sampled chains now hold fully end-to-end (corrected from a stale "fifteen" count, 2026-07-08 — the table itself always had 14 rows; this is a wording fix, not a row change). The three chains that were broken in the prior re-audit pass (REQ-050, REQ-043, REQ-037) are now complete with real, specific `TEST-###`/`VG-###` entries — no vague or generic evidence descriptions remain.

## Required Fixes

All required fixes identified across the original three audit passes were confirmed resolved as of 2026-07-06. **Two further rounds have since found and closed additional gaps (items 6-7) — this section is updated each time, rather than left standing as a stale "none outstanding" claim once a later round finds something.**

1. ~~Add missing `TASK-###` entries for REQ-008/009/010/033/037/043/045/050/056/006a/006d.~~ **Resolved** — TASK-033 through TASK-038 confirmed built correctly; TASK-009/023 cross-reference fixes confirmed.
2. ~~Complete the TDD-Exempt template on 6 existing tasks.~~ **Resolved** — all 12 exempt tasks (including the 2 spec-writer found beyond the original 6) now have both required sections.
3. ~~Resolve the REQ-005a/REQ-005b orphaned reference.~~ **Resolved** — now documented as `REQ-005(a)`/`REQ-005(b)` shorthand, explicitly not independent IDs.
4. ~~Fix the TASK-037/TASK-038 section-misplacement defect.~~ **Resolved** — confirmed programmatically: every one of the 38 tasks in `10-build-plan.md` has exactly one Acceptance Criteria and one Verification Evidence section, correctly scoped to its own task.
5. ~~Close the TASK→TEST→VG chain for REQ-050, REQ-037, and REQ-043.~~ **Resolved** — TEST-035/036/037 and VG-020(corrected)/031/032 all added and independently confirmed real, specific, and consistent across `02-requirements.md`, `10-build-plan.md`, `11-test-plan.md`, and `12-verification.md`.
6. ~~Add missing "TDD Refactor" sections to 14 strict-TDD tasks (TASK-012, 014, 015, 016, 017, 019, 021, 022, 023, 024, 025, 026, 027, 029).~~ **Resolved** (2026-07-08, DEC-127) — see the "Gate 7 batch fix" Re-audit Note above; all 26 strict-TDD tasks now have exactly one Refactor section each.
7. ~~Close 6 NFR→TASK traceability gaps found by the first NFR-scoped Gate 8 sweep (NFR-003, NFR-007, NFR-020, NFR-021, NFR-024, NFR-028) — these 6 had zero coverage of any kind (no Related Requirements citation, no TEST/VG, no architecture cross-reference).~~ **Resolved** (2026-07-08, DEC-129) — see the "NFR→TASK traceability sweep" Re-audit Note above; every `NFR-###` now has verification-step coverage of at least one kind (17 others remain uncited in Related Requirements specifically but have TEST/VG/architecture coverage — not a gap, not fixed further this round; see the Re-audit Note's cross-check for the corrected claim and why).

**None outstanding as of 2026-07-08.** This claim is only as good as the last full sweep — item 7 exists precisely because "none outstanding" had gone stale once before without anyone noticing until a dedicated NFR sweep was requested.

## Final Notes

- No item across any of the three audit passes required a new product/business/scope decision from the user — every finding was a spec-completeness or cross-reference gap inside already-decided scope, correctable by `spec-writer` without reopening any DEC. None remains open.
- The original gap pattern (verification gates and tests referencing work with no corresponding build task) most plausibly arose because `10-build-plan.md`'s Phase structure was organized around the *hot-path query pipeline* (Phases 2-4) and *cross-cutting infra* (Phases 1, 5-6), which left several **surface-level MVP deliverables** (the HTTP API itself, the widget itself, the admin API itself, the audit-pull endpoint) implicitly assumed as "obviously covered by the API scaffold" rather than separately scheduled. This is now fully remediated, but worth keeping in mind for any future spec-writer pass adding new MVP-tagged requirements.
- This audit independently re-verified rather than trusted self-reported consistency claims at every stage — the original Stage 7/8 sweeps, and each of `spec-writer`'s two subsequent fix-pass summaries. Two of the three re-checks found something the summary hadn't mentioned (the TASK-037/038 misplacement on the second pass); the third re-check found the fixes exactly as claimed. The finding that a six-round, two-phase process, plus two rounds of "fixed" claims, still needed a third independent pass to fully close is not a criticism of any one round or fix — it is evidence that automated/independent ID-resolution and section-structure sweeps (like the ones this audit ran by hand) should be a standing CI check per `09-deployment-ops.md`'s existing "architecture import-graph check" pattern, not a one-time audit action. Recommended as a process improvement for this project's next spec-generation cycle, not a required fix for this package.
- **Final verdict: READY.** All 9 gates now resolve to PASS or the correctly-justified NOT APPLICABLE (Gate 9). Gates 1-6 and 9 were verified once, in the original pass, and are carried forward unchanged per the coordinator's explicit re-audit scoping in each subsequent round. Gates 7 and 8 were independently re-verified three times across this session, with the final pass finding zero outstanding defects.
