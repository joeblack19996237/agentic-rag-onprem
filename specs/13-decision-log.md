# Decision Log — GroundedDocs

> Canonical append-only log of durable decisions. Every specialist writes here.
> Stable IDs: `DEC-###`. Reference from any spec with the ID.

| ID | Date | Stage | Owner | Decision | Rationale | Alternatives rejected |
|---|---|---|---|---|---|---|
| DEC-001 | 2026-06-25 | 0 | User | Project codename = **GroundedDocs** | Internal name only; conveys the anti-hallucination positioning ("answers stay grounded") | "VerifiedRAG" (too generic), "CiteFirst" (too narrow to one feature) |
| DEC-002 | 2026-06-25 | 0 | User | Output path = `D:\AI\claude_code\agentic-rag-onprem\specs\` (new directory) | Earlier multi-tenant SaaS specs in `agentic-rag\specs\` are a different product line; keep separated to avoid contamination | Reusing or archiving the old directory |
| DEC-003 | 2026-06-25 | 0 | User | Deployment model = **single-tenant on-premise only** for MVP | Vendor-embeddable + private deployment is the core differentiator vs SaaS RAG offerings; enterprise data sovereignty is the buying reason | SaaS (rejected: defeats positioning); hybrid (rejected: too expensive for personal-project demo) |
| DEC-004 | 2026-06-25 | 0 | User | Sales channel = **B2B2B via CCM/ECM vendor add-on** | Matches user's professional network and lowers go-to-market cost; vendor owns the customer relationship | Direct enterprise sales (rejected: no sales motion as a solo project); marketplace listing (deferred) |
| DEC-005 | 2026-06-25 | 0 | User | MVP scope = **retrieval + cited Q&A only**; ReAct multi-step agent moves to V2; Wiki Mode is out of scope permanently for MVP | Anti-hallucination guarantee is harder to keep under multi-step reasoning; Wiki Mode auto-synthesizes content, which creates a new hallucination surface that contradicts positioning | MVP-with-agent (rejected: scope risk); MVP-with-wiki (rejected: contradicts positioning) |
| DEC-006 | 2026-06-25 | 0 | User | Compliance baseline = **general B2B** for MVP; no certification committed | First buyer profile is non-regulated; design must not block future certification, but no audit prep yet | Targeting regulated buyers from day 1 (rejected: scope) |
| DEC-007 | 2026-06-25 | 0 | User | Tech stack = **open at Stage 0**; chosen with alternatives in Stage 4 | No incumbent codebase; freedom to pick the most-evidenced stack from trend research | Pre-pinning a stack now (rejected: would short-circuit Stage 1 + 4) |
| DEC-008 | 2026-06-25 | 0 | User | Time horizon = **personal project → demo** | Defines spec depth: production-readiness specs target demo-deployable, not 24/7 SLA | Pretending to be GA-track (rejected: dishonest scope) |
| DEC-009 | 2026-06-25 | 0 | User | Run **Stage 1 trend research** | Contested category with multiple mature alternatives; need explicit competitor map and pattern survey before PM and architecture | Skip with rationale (rejected: trend research justified) |
| DEC-010 | 2026-06-25 | 2 | PM | Positioning anchor = **on-prem + vendor-embeddable + verified citation + refusal + audit-ready** | Stage 1 trend research §2.2 identifies this as an empty quadrant in 2026 competitor map; five differentiators converge on a single coherent buyer message | Generic "self-hosted RAG" (too crowded); narrow-vertical RAG (too small a TAM for first wave) |
| DEC-011 | 2026-06-25 | 2 | PM | **Wiki Mode permanently out of scope** | Auto-synthesized content is a new hallucination surface that directly contradicts the anti-hallucination positioning; trend research §13.3.2 confirms this is a real differentiation vs WeKnora | Wiki Mode as V2 (rejected: positioning conflict, not just scope) |
| DEC-012 | 2026-06-25 | 2 | PM | Reference LLM serving = **vLLM**; demo bootstrap = **Ollama**; **TGI excluded** | vLLM is the 2026 production default with widest hardware support; Ollama is acceptable for sub-5-minute demo install only; TGI is in HuggingFace maintenance mode since Dec 2025 | SGLang (rejected for MVP: deferred to V2 as upgrade path); TensorRT-LLM (rejected: ultra-high-volume only, NVIDIA-only, high engineering overhead); TGI (rejected: maintenance mode) |
| DEC-013 | 2026-06-25 | 2 | PM | Reference generation model = **Qwen3 family (Apache 2.0)** | Strong multilingual (Chinese + English mandatory for CCM market); MoE variants enable on-prem single-host deployment; Apache 2.0 is safest commercial license; trend research §5 | Llama 4 (deprioritized: license has scale clauses); DeepSeek V3 (deprioritized: hardware footprint heavy for single-host demo); Phi-4 (kept as CPU/edge fallback only); Gemma 4 (kept as alternative Apache 2.0 option) |
| DEC-014 | 2026-06-25 | 2 | PM | Reference embedding = **bge-m3**; reference rerank = **bge-reranker-v2-m3** | 2026 OSS production safe default per trend research §6; multilingual; unified dense+sparse+ColBERT in one model | No serious 2026 OSS contender; commercial APIs rejected by on-prem positioning |
| DEC-015 | 2026-06-25 | 2 | PM | Vendor integration surface MVP = **iframe widget + HTTP API**; V2 = **JS widget**; V3 = **SDK** | Industry pattern: ship iframe first for speed, migrate to richer surfaces as adoption justifies (trend research §7.3) | SDK-first (rejected: too much engineering for demo); API-only (rejected: many CCM vendors will want a drop-in widget) |
| DEC-016 | 2026-06-25 | 2 | PM | Eval stack MVP = **RAGAS offline harness**; V2 = **add CI gate (DeepEval or Promptfoo)**; V3 = **TruLens production observability** | Three-framework parallel pattern is industry-standard (trend research §3.3); MVP needs at least offline RAGAS to validate quality gates | TruLens-first (rejected: production-only value, not demo-relevant); no eval at all (rejected: defeats the entire positioning) |
| DEC-017 | 2026-06-25 | 2 | PM | **Adopt industry RAGAS thresholds as MVP starting baseline**: faithfulness ≥ 0.75, answer relevancy ≥ 0.80, context precision ≥ 0.70, context recall ≥ 0.80; refusal ≥ 95%; hallucination ≤ 2%; citation-hit-rate = 100% hard gate | Confirmed by user 2026-06-25 in response to Stage 1 follow-up question. Industry-cited thresholds (trend research §3.2) for B2B production. CCM-specific re-baselining flagged as RISK-009 — these are a starting line, not the finish line | Inventing custom thresholds at Stage 2 (rejected: no data); skipping numeric gates (rejected: defeats positioning) |
| DEC-018 | 2026-06-25 | 2 | PM | **First-wave vendor target list = Smart Communications, M-Files, Hyland, regional CCM/ECM**; **OpenText prospects deprioritized in first wave**; Quadient relationship = partnership via BYOAI framework | Trend research §13.5: OpenText Content Aviator has high overlap when buyer is already on OpenText; entering that conversation without a wedge is uphill. Smart Comm / M-Files / Hyland / regional vendors do not have Aviator-grade AI suites — clearest field | Going at OpenText first (rejected: high overlap, weak wedge); going direct enterprise (rejected: solo project, no sales motion); all-vendors-equal (rejected: dilutes messaging) |
| DEC-019 | 2026-06-25 | 2 | PM | **Stage 3 (UX) skip with rationale**: MVP has no standalone UI beyond the embeddable iframe widget; widget UX is scoped inside the architecture stage when the integration contract is defined. Admin console is V2. Skip category = `SKIP-NOT-APPLICABLE` for the standalone-product UX topic; widget UX handled in arch stage | Per workflow Stage 3 rules: skippable for headless APIs / pure backends / embedded components | Run Stage 3 fully (rejected: MVP UX surface is the iframe widget; bigger UX work is V2 admin console) |
| DEC-020 | 2026-06-27 | 2 | User | Business model = **open-source / source-available core + paid deployment service + annual support subscription** (RedHat / RAGFlow-style) | User has no hardware to ship a self-validated boxed product; CCM consulting background makes deployment + tuning a natural revenue source; OSS positioning builds credibility cheaper than a sales motion for a solo project | Closed-source low-license + mandatory services (rejected: closed-source kills the OSS-community first-wave channel from DEC-025); pure project consulting (rejected: no reusable software asset means no leverage); pure license sale (rejected: no validated hardware path) |
| DEC-021 | 2026-06-27 | 2 | User | Dev/eval hardware = **cloud GPU rental** (RunPod / Vast.ai), 24 GB-class single GPU, monthly budget target ~¥200 | User has no local 24 GB+ GPU; one-off purchase not justified at personal-project scale; rental at $0.3–0.6/h matches intermittent dev cadence | Buy 3090 (rejected: ¥6–8k sunk cost for unproven project); CPU-only Phi-4 (rejected: violates NFR-005 latency and tanks demo quality) |
| DEC-022 | 2026-06-27 | 2 | PM | **V2 scope expanded** to include all of REQ-019..024 (span highlighting, claim decomposition, doc lifecycle/authority, prompt template registry, customer golden set, A/B prompt+model) in addition to the previously committed REQ-015..018 | Service-heavy business model (DEC-020) means each V2 capability is a billable deployment / tuning engagement; vertical depth in the citation+governance+tuning triple raises per-customer revenue without expanding product surface to compete with WeKnora/RAGFlow | Picking only 2–3 V2 items (rejected: user explicitly chose full set); horizontal expansion into Wiki / IM connectors / general agent (rejected: see brief §6 non-goals) |
| DEC-023 | 2026-06-27 | 2 | User | **REQ-030 (customer-corpus reranker LoRA fine-tuning) attempted in-house first**; partner subcontract is fallback if effort exceeds capacity | User wants the skill-building experience; if practical complexity blocks, business arrangement with ML partner is acceptable Plan B | Outsource from day 1 (rejected: user wants the learning); commit to in-house only (rejected: real schedule risk for solo project) |
| DEC-024 | 2026-06-27 | 2 | User | **Pricing/packaging direction deferred**; vendor conversations may surface a shape before we pin one | No pilot data yet; premature pricing is more harmful than no pricing at this stage; service-rate framing in `01-product-brief.md` §8 used for internal sizing only | Pre-pinning a number now (rejected: zero data) |
| DEC-025 | 2026-06-27 | 2 | User | **First-wave go-to-market = OSS community + industry events**; direct CCM/ECM vendor outreach moves to second wave | Solo project has no sales motion; OSS reputation is cheaper to build than vendor introductions; once the project has community traction, vendor conversations become inbound rather than cold | Direct vendor outreach first wave (per DEC-018; now **superseded** for first wave — DEC-018 retained as the recorded *vendor priority order* for second wave) |
| DEC-026 | 2026-06-27 | 2 | User | **Demo deadline = 2026-09-27 (3 months from today, 2026-06-27)**; MVP scope is fixed at REQ-001..014 + NFR-001..011 and may be cut further if delivery slips | Honest schedule for solo developer learning a new stack and renting cloud GPU; deadline is a forcing function for scope discipline, not a quality commitment | No deadline (rejected: scope creep risk); 6-month deadline (rejected: user choice + market window pressure from EU AI Act timing) |
| DEC-027 | 2026-06-27 | 2 | User | **Metric ownership during demo = user runs RAGAS golden-set evals manually pre-demo**; MVP ships the runner (REQ-013) but does not automate CI evaluation | Solo project at demo stage; manual eval is acceptable and matches §9.1 measurement-window cadence; CI gate is V2 (DEC-016) | Automated CI eval in MVP (rejected: scope inflation); no eval at all (rejected: defeats positioning) |
| DEC-028 | 2026-06-27 | 2 | User | **"Model Lifecycle Care" (LCC) as a standard service package** with four tiers: T1 Monitoring (included in annual support), T2 Advisory (add-on), T3 Migration Execution (per-engagement), T4 LTS (1.5× annual support multiplier). Architecturally enabled by REQ-033/034/035 | Open-source model lifecycle risk (RISK-014) is the largest single buyer hesitation against the open-source-first model (DEC-020); LCC converts the hesitation into recurring revenue and matches the proven RedHat/Canonical pattern | Verbal-only commitment (rejected: leaves risk on the buyer); fewer tiers (rejected: user explicitly chose all four); no LCC at all (rejected: leaves DEC-020 incomplete) |
| DEC-029 | 2026-06-27 | 2 | User | **Stance on customer-trained models**: L1 (pretrain-from-scratch) refused; L2 (continued pretraining) and L3 (SFT/DPO) accepted as advisory + ML-partner subcontract per DEC-023; L4 (LoRA adapters) executed in-house | L1 is wrong question for any CCM/ECM customer (wrong scale; we are not an LLM lab); L2/L3 are rare-fit and best partnered (we own the customer relationship, partner does ML); L4 is bounded enough to own and is already in scope (REQ-030) | Accept all four levels in-house (rejected: solo capacity does not fit L1/L2/L3 ML work); outsource everything (rejected: loses skill-building per DEC-023; loses L4 margin) |
| DEC-030 | 2026-06-27 | 2 | PM | **REQ-030 scope extended** from "reranker LoRA fine-tuning" to "reranker AND generator LoRA fine-tuning" — two independent adapters trained separately | Customer-specific answer-format and citation-format tuning (the most common in-vertical request) requires generator LoRA, not just reranker LoRA; covering both is the natural shape of REQ-030; user attempts both in-house per DEC-023 | Keep REQ-030 reranker-only (rejected: leaves the most common customer-tuning request unaddressed); split into two REQs (rejected: same training infrastructure, redundant) |
| DEC-031 | 2026-06-27 | 2 | PM | **"Architecture for Insulation" white paper** added as a V2-cycle marketing deliverable; published into the OSS community + industry-event channel per DEC-025 | LCC sells best when buyers already understand why architecture+contract beats self-training; the white paper inoculates buyers against the "train your own model" misframing (RISK-015); aligned with OSS-first GTM | Defer to vendor-pilot stage (rejected: by then the buyer has already been pitched by competitors); skip entirely (rejected: leaves RISK-015 unmitigated) |
| DEC-032 | 2026-06-27 | 4 | Architect | Build approach = **from-scratch with OSS primitives** (vLLM + TEI + Qdrant + Postgres + FastAPI directly wired) | Differentiation lives in `verify/` (citation + NLI + claim decomposition + refusal) on the hot path; frameworks fight the implementation. Surface area ~10 modules each 200–500 LOC — fits solo 3-month timeline (DEC-026). Owning every line minimizes LCC service coordination cost | LlamaIndex scaffolding (rejected: framework abstractions block verify/ layering); Haystack scaffolding (rejected: pipeline DAG learning curve + heavier deps); Fork RAGFlow (rejected: dilutes positioning; you inherit upstream's roadmap; LCC adapter abstraction clashes with RAGFlow's) |
| DEC-033 | 2026-06-27 | 4 | Architect | Primary language **Python 3.12**; API framework **FastAPI** | OSS LLM ecosystem is Python-native; FastAPI is the de-facto async stack for this domain; CLAUDE.md will codify this | Go (rejected: ecosystem gap for ML libs); Flask (rejected: sync model); Litestar (rejected: smaller community) |
| DEC-034 | 2026-06-27 | 4 | Architect | Vector store = **Qdrant**; relational store = **Postgres 16**; **no Redis / no Celery / no Kafka in MVP** | Qdrant supports dense + sparse + payload filter in one collection (REQ-003); Postgres handles relational + audit + task queue (via `SKIP LOCKED`) + later observability; cutting Redis/Celery saves customer install footprint by 2 containers and eliminates 2 failure modes | pgvector (rejected: sparse weak); Weaviate (rejected: heavier deploy); Celery+Redis (rejected: V2 inflation); Kafka (rejected: way over-scoped for single-host) |
| DEC-035 | 2026-06-27 | 4 | Architect | Embedding and rerank served via **TEI** (text-embeddings-inference, HuggingFace) in **separate processes** from generation | Prevents VRAM contention with vLLM-served generation model; gives independent scaling + version pinning per model; aligns with REQ-034 embedding versioning | In-process sentence-transformers (rejected: VRAM contention; harder versioning); Ollama for embedding (rejected: scale ceiling) |
| DEC-036 | 2026-06-27 | 4 | Architect | Document parsing = **Unstructured.io** primary + **PyMuPDF** rescue path for PDFs + **python-docx** for Word | Unstructured covers 80% of formats with consistent chunk metadata; PyMuPDF as fallback for layouts Unstructured mangles; OCR + complex tables stay deferred (REQ-028, V3) | LlamaIndex parser (rejected: framework lock-in); RAGFlow's parser (rejected: tied to RAGFlow fork path we rejected); home-grown (rejected: 80/20 — not the differentiator) |
| DEC-037 | 2026-06-27 | 4 | Architect | NLI verification baseline = **deberta-v3-large-mnli**; **AutoGDA domain-adapted variant** introduced in V2 per trend research §3.1 | Lightweight enough for 1–2 s/check budget (trend research §3.1); strong publicly-evaluated multilingual fallback for mixed Chinese/English content; AutoGDA shown to improve domain grounding without inference cost increase | LLM-as-judge (rejected: cost, latency, drift); RoBERTa-NLI (rejected: weaker multilingual); skip NLI (rejected: defeats REQ-005) |
| DEC-038 | 2026-06-27 | 4 | Architect | Task queue = **Postgres rows + `SELECT ... FOR UPDATE SKIP LOCKED`** | Eliminates Redis/Celery dependency; Postgres is already there for relational + audit; pattern is battle-tested at scale appropriate for MVP/V2 single-host (REQ-026 V3 multi-host may reconsider) | Celery + Redis (rejected: 2 containers, 2 SPoFs); RQ (rejected: same as Celery, smaller); Dramatiq (rejected: smaller again) |
| DEC-039 | 2026-06-27 | 4 | Architect | **MVP has no agent loop**; ReAct multi-step (REQ-015) is a **V2 fallback** triggered only when single-step retrieval recall is insufficient | Reduces hallucination surface; ReAct adds verification complexity (every sub-call needs verify/ pass) that is V2 work; trend research §3.1 NLI verification is harder to reason about across multi-step | MVP with ReAct (rejected: scope + risk for 3-month deadline); permanent single-step (rejected: V2 needs the option) |
| DEC-040 | 2026-06-27 | 4 | Architect | Widget = **Web Component + iframe dual-mode** sharing one codebase; iframe is MVP default | Allows MVP iframe ship (REQ-009) and V2 JS-widget upgrade (REQ-018) without rewrite; iframe gives integration-day safety; Web Component is the future-proofing | iframe only (rejected: V2 widget rewrite cost); JS widget only (rejected: integration risk in MVP); React/Vue components (rejected: bundle size + framework lock-in) |
| DEC-041 | 2026-06-27 | 4 | Architect | Customer-deployment hardware floor = **single GPU ≥ 24 GB VRAM + 64 GB system RAM + 200 GB SSD**; reference generation model at this floor = **Qwen3-14B int4 or Qwen3-30B-A3B MoE int4**; below the floor we ship a documented **degraded mode** (Qwen3-7B int4 or Phi-4) with explicit quality + latency disclaimers | Below 24 GB VRAM, RAGAS thresholds (DEC-017) become unreliable on multilingual workloads; honest floor matters for vendor trust; degraded mode lets us truthfully say "runs on smaller boxes" without misleading | No floor (rejected: customer hardware surprises kill demos); higher floor — 48 GB (rejected: cuts the addressable customer base unnecessarily) |
| DEC-042 | 2026-06-28 | 4 | Architect | **5-class refusal taxonomy** (`no_recall` / `low_grounding` / `access_denied` / `policy_blocked` / `verification_unavailable`); `acl_denial_mode = transparent | opaque` config (MVP default = transparent); **audit log always records the real reason**, only the user-facing `refusal_reason` field is masked when `opaque` mode is on | Original 3-class taxonomy hid ACL denials inside `no_recall`, breaking audit forensics and UX dead-end. Transparent default fits general B2B (DEC-006); opaque mode supports sensitive corpora (HR/exec comp). Audit/user-view separation is correct compliance pattern | Keep 3 classes (rejected: ACL denials misclassified); always-opaque default (rejected: UX dead-end for normal customers); audit shows what user sees (rejected: defeats compliance) |
| DEC-043 | 2026-06-28 | 4 | Architect | **`no_recall` returns up to 3 neighboring documents as "you might also be looking for"** in MVP (REQ-006a); V2 adds query-log similar-question suggestions (REQ-006b); V2 adds access-request workflow for `access_denied` (REQ-006c). Neighboring docs **must be ACL-filtered before display** | Neighboring docs come from already-fetched low-score retrieval set — near-zero cost; high UX value. Query-log suggestions need new index + cross-user ACL safety logic → V2. Access-request workflow needs schema + notification + approver UI → V2 | No fallback (rejected: dead-end UX); query log in MVP (rejected: scope + safety complexity); always offer 3 even without ACL filter (rejected: ACL leak) |
| DEC-044 | 2026-06-28 | 4 | Architect | **MVP data model reserves `documents.owner_user_id` + `documents.approver_user_ids[]` columns** even though access-request workflow is V2 | V2 workflow without these fields requires full schema migration on existing deployments — friction at the worst time. Including null-allowed columns now costs nothing | Defer columns to V2 (rejected: migration friction); fully implement workflow MVP (rejected: scope) |
| DEC-045 | 2026-06-28 | 4 | Architect | **Identity & ACL = Federation pattern + ECMAdapter abstraction**; GroundedDocs **never** reimplements vendor's user / role / group / ACL hierarchy. ECM/CCM is sole authoritative source of truth | Reimplementing vendor ACL eval is perpetual divergence (Documentum `dm_acl + r_accessor_name`, OpenText permission + inheritance + dynamic groups, SharePoint per-item + sensitivity labels are non-trivial; user's external research (`reference.docx`) calls this out explicitly). Federation pattern matches industry consensus (Glean, Microsoft Graph connectors, AWS Q Business, Vectara) | Build user/role tables ourselves (rejected: perpetual ACL drift, compliance failure); pure live-RPC for everything (rejected: see DEC-046) |
| DEC-046 | 2026-06-28 | 4 | Architect | **Two-layer authorization** (per `reference.docx` research + 04-architecture §7B): Layer 1 = ACL/retention metadata synced to Qdrant payload + filter pushdown into HNSW (`filter-then-search`); Layer 2 = post-retrieval `batch_check_access` + retention re-check via ECMAdapter live RPC. **Retention: physical delete on expiry (no soft flag); freeze on legal hold (immutable + non-reindexable)** | Pure live RPC = 50 RPCs per top-K=50 query (p99 explodes; ECM ACL not designed for high fan-out) AND over-fetch+post-filter breaks HNSW recall at high filter rates. Pure sync = stale ACL during reorgs / offboarding / legal hold — leaks contaminate LLM context. Two-layer is industry consensus. Soft-delete on retention is auditor red line | Pure live RPC (rejected: perf + recall); pure sync (rejected: stale ACL + compliance); soft-delete retention (rejected: auditor red flag — rebuild/restore revives killed content) |
| DEC-047 | 2026-06-28 | 4 | Architect | **Audit dual-write enabled by default in MVP**: every query writes to (a) RAG local `audit_events` (REQ-007 + REQ-035 fingerprint), AND (b) ECM audit log via `ECMAdapter.write_audit_access()` async best-effort | Documentum DAR + OpenText Records Management compliance treat RAG-side traces as supplementary; only ECM audit is authoritative. Async best-effort write keeps query latency unaffected by ECM availability while still completing the compliance chain | MVP local-only audit (rejected: blocks ECM-required customers); synchronous ECM write (rejected: ECM availability becomes query SLO dependency); V2 dual-write (rejected: compliance-required customers can't sign up before V2) |
| DEC-048 | 2026-06-28 | 4 | Architect | **Token verification policy**: vendor tokens (OIDC bearer) must be validated against JWKS endpoint with algorithm whitelist; `none` algorithm + unsigned tokens rejected; key rotation supported via JWKS refresh | Unsigned/forged tokens are the prompt-injection sister vulnerability — they let attackers impersonate users in the federation model | Accept any presented token (rejected: trivial bypass); local key cache without rotation (rejected: stale signing keys) |
| DEC-049 | 2026-06-28 | 4 | Architect | **ECM-side `get_effective_acl(doc_id)` is a contract requirement** for vendor adapters; reference Python sidecar shipped only as fallback for ECMs with no such endpoint, with explicit "will drift" warnings | Vendor ACL eval engines (Documentum, OpenText, SharePoint) are battle-tested; reimplementing them on RAG side is the doc's named anti-pattern. Reference sidecar exists as escape hatch but is not recommended | Always reimplement on RAG side (rejected: drift; doc's anti-pattern); refuse adapters without endpoint (rejected: cuts addressable market) |
| DEC-050 | 2026-06-28 | 4 | Architect | **V2 per-vendor adapter priority**: V2-α = **Documentum + OpenText**; V2-β = SharePoint + M-Files + Hyland; V2-γ = regional CCM via partner per DEC-029 | User's external research scoped to Documentum + OpenText (those were the explicit examples). DEC-018 vendor priority listed Smart Comm + M-Files + Hyland for sales targeting but technical adapter order is driven by user's research context + integration partner availability — these don't have to match | SharePoint-first (rejected: research evidence prioritizes the other set); all V2 in parallel (rejected: solo capacity); all to partner (rejected: lose adapter margin and learning) |
| DEC-051 | 2026-06-28 | 4 | Architect | **CDC sync transport**: MVP = webhook receiver (HTTPS endpoint with HMAC verification) + periodic re-poll fallback (every 30 minutes, admin-configurable). V2 = event bus consumer (Kafka / EventGrid / Pub/Sub) | Documented webhook + re-poll keeps MVP simple while satisfying the doc's event-driven requirement for legal hold latency. V2 event bus addresses high-volume customers and reduces per-vendor webhook config burden. Re-poll fallback ensures no event loss during webhook outage | Re-poll only (rejected: violates doc's legal hold latency requirement); webhook only no fallback (rejected: webhook outage = silent ACL drift); event bus in MVP (rejected: container/dep footprint for single-host) |
| DEC-052 | 2026-06-28 | 5 | User | **English-only document retrieval is a hard MVP constraint**; multilingual baseline rationale in DEC-013 / DEC-014 / DEC-037 is superseded. Model stack is re-selected for English-only optimization (candidates: `Llama-3.1-8B-Instruct` int4 / `Mistral-Small-24B-Instruct` int4 generation; `bge-large-en-v1.5` or `e5-large-v2` embedding; `deberta-v3-base-mnli` or `roberta-large-mnli` NLI). Hardware floor (DEC-041) is re-evaluated. Multilingual schema extension stays as a non-blocking future option | First-wave market = AU + NZ (DEC-072) is English-speaking; multilingual model overhead consumes performance budget for capabilities the MVP market does not pay for. Smaller English-optimized models also lower hardware floor and ingestion cost | Keep multilingual baseline (rejected: out-of-scope cost); ship both multilingual + English-only stacks (rejected: solo scope) |
| DEC-053 | 2026-06-28 | 5 | User | **MVP demo form = with-ECM canonical path**. `04-architecture` §5 module diagram is upgraded to include `acl/` + `cdc/` modules as the default MVP shape. The no-ECM (`LocalAdapter`-only) path is demoted to an appendix variant for self-contained demos | Two coexisting MVP shapes confused readers and risked Stage 7 spec-writer producing a no-ECM default. AU/NZ first-wave market (DEC-072) is ECM-rich (Documentum / OpenText / M-Files / Hyland); with-ECM is the realistic canonical | Keep no-ECM as the default and with-ECM as opt-in (rejected: misaligned with target market); ship only with-ECM and drop no-ECM (rejected: loses the rapid-demo path) |
| DEC-054 | 2026-06-28 | 5 | User | **NFR-005 p95 ≤ 8 s is held**. The previously-tight §7B.12 budget is restructured via concurrency + caching primitives: KV-cache reuse across turns of the same conversation, prompt-cache for system prompts + per-customer templates, reranker batching, NLI batching. Each §7B.12 line item is annotated with a concurrency assumption ("at ≤ 2 in-flight queries") | Loosening the SLO (e.g. to 12 s) damages the vendor-contract positioning more than restructuring costs. Caching is a one-time architectural investment with compounding returns under demo concurrency | Relax NFR-005 to 12 s (rejected: weakens positioning); add a second LLM instance (rejected: doubles VRAM, breaks single-host floor) |
| DEC-055 | 2026-06-28 | 5 | User | **GroundedDocs co-locates with the ECM inside the same private network / VPC**, even when the ECM is cloud-hosted. CDC inbound webhook (DEC-051) is the primary transport because the firewall path is intra-private-network. The "outbound poll only" deployment shape is not a supported topology for MVP | GroundedDocs UX is embedded inside the ECM UI; same-private-network deployment is the natural shape; reverses no current network-form question. Documents the topology so vendor integrators do not propose split-firewall deployments | Support outbound-poll-only as a first-class shape (rejected: violates real co-location pattern + breaks DEC-051 cadence); cross-internet webhook from public ECM (rejected: security posture mismatch with on-prem positioning) |
| DEC-056 | 2026-06-28 | 5 | Architect | **Retention SLA split**: 60-second SLA on the webhook path; 30-minute SLA on the re-poll fallback path. Webhook outage triggers an ops alert (NFR-016) so the customer knows the retention SLA has temporarily degraded. NFR-014 single-number SLA is superseded | Mathematical impossibility of a 60-second SLA when the only event source is a 30-minute re-poll. Split-SLA with explicit fallback degradation is the honest framing; auditors accept this when it is documented as a known limit | Tighten re-poll to 60 s (rejected: ECM-side throughput); add a polling-style compensation job for expired-pending (rejected: adds a second consumer code path) |
| DEC-057 | 2026-06-28 | 5 | Architect | The build-approach 4-option comparison in `04-architecture` §3.1 gains two scoring dimensions: **AI auditability** and **compliance alignment difficulty**. From-scratch retains the lead. DEC-032 rationale is strengthened to reflect the dual driver (differentiation + provable compliance) | Compliance posture is GroundedDocs's commercial wedge; omitting it from the comparison hid a real advantage and weakened DEC-032's defense | Leave §3.1 unchanged (rejected: weak defense under reviewer challenge) |
| DEC-058 | 2026-06-28 | 5 | Architect | **Single source of truth for refusal taxonomy = DEC-042** (5 classes + `acl_denial_mode`). `04-architecture` §12.1 is regenerated to match. `20-agent-behavior` §6 failure table aligned. All cross-references redirected to DEC-042 | §12.1 was authored before DEC-042 and contained 3 classes; downstream readers got conflicting answers depending on which document they read first | Keep §12.1 as authoritative (rejected: DEC-042 is the canonical decision) |
| DEC-059 | 2026-06-28 | 5 | Architect | **Double-collection naming convention + `embedding_model_version` schema column are promoted to MVP**. Blue/green re-embedding automation remains V2 per REQ-034. Schema cost is near-zero at MVP; the alternative (schema migration at first LCC Tier 3 engagement) is high-friction | LCC Tier 3 (DEC-028) is foreseeable; doing the schema layer at MVP costs one column and one collection-naming convention; the automation can wait | Keep schema in V2 (rejected: forces breaking migration on first LCC event) |
| DEC-060 | 2026-06-28 | 5 | Architect | **`context_fingerprint` mandatory non-null columns on `audit_events` are promoted to MVP** (model_adapter, model_version, embedding_version, reranker_version, prompt_template_id, verify_thresholds). Fingerprint-driven LCC services remain V2 per REQ-035. Demo-period hallucination dispute forensics require the columns from day one | Without fingerprint at MVP, demo-period disputes ("which threshold caused this refusal?") cannot be traced. Schema cost is six non-null columns | Defer to V2 (rejected: blocks dispute forensics during the most-visible period) |
| DEC-061 | 2026-06-28 | 5 | Architect | JWT signature algorithm whitelist is enumerated: **RS256 / ES256 / EdDSA** allowed; **HS256, HS384, HS512, `none`** rejected. Supplements DEC-048 | DEC-048 declared "whitelist" without enumeration; implementers default to HS256 (shared-secret) which breaks the federation model | Allow HS256 with rotation (rejected: shared-secret model incompatible with federation); allow none (rejected: trivial bypass) |
| DEC-062 | 2026-06-28 | 5 | Architect | **Air-gap JWKS path**: customer pre-imports a static JWKS bundle at install time; manual rotation runbook ships in `09-deployment-ops`. Online JWKS endpoint refresh is non-air-gap only. Supplements DEC-048 | Air-gap (NFR-002) cannot reach an IdP for JWKS rotation; the gap had to be closed before first air-gap install or the only solution would be to disable signature verification | Disable signature verification in air-gap (rejected: security regression); ship JWKS proxy sidecar (rejected: scope) |
| DEC-063 | 2026-06-28 | 5 | Architect | **ECM PDP timeout / failure circuit breaker**: configurable threshold (default 500 ms); breaker tripping returns explicit `verification_unavailable` refusal — never silent skip. Supplements §7B.12 closing note that silent skip re-introduces stale-ACL leak | The "silent skip when PDP slow" failure mode was self-acknowledged but had no design response. Circuit-breaker + explicit refusal makes failure visible to ops and user without compliance regression | Silent skip (rejected: stale-ACL leak); raise PDP budget (rejected: violates p95 SLO under load) |
| DEC-064 | 2026-06-28 | 5 | Architect | **`write_audit_access` is called for both successful access AND `access_denied` refusals**, with `intent="denied"` in the denied case. Supersedes REQ-045 acceptance criterion "every query that successfully accesses..." | Compliance frameworks (SOX, HIPAA, AU Privacy Act NDB scheme) require failed-access attempts in the audit chain; the original wording silently excluded them | Keep success-only audit (rejected: compliance gap); duplicate to a separate denied-audit sink (rejected: needless dual schema) |
| DEC-065 | 2026-06-28 | 5 | Architect | **Chunking strategy pinned**: 1024-token chunks + 128-token overlap + recursive splitter primary, structural splitter (markdown headers, PDF sections) fallback. `chunk_id` is immutable per `(document_id, version_id, sequence)` | Chunking strategy was unspecified; verify/ span detection and citation granularity depend on stable bounds; spec-writer cannot proceed without a pin | Larger chunks (rejected: cuts retrieval recall on focused questions); per-customer chunking (rejected: scope) |
| DEC-066 | 2026-06-28 | 5 | Architect | **Concurrency cap declared**: MVP single-host supports **≤ 2 in-flight queries** at the NFR-005 p95 SLO; queries beyond this queue with explicit `queue_depth` metric and head-of-line latency alert. Demo documentation states the cap up front | Single-GPU vLLM + co-located TEI + NLI cannot serve N concurrent queries at sub-8s latency. Stating the cap honestly prevents demo-day overload | No cap (rejected: dishonest); auto-scale (rejected: violates single-host MVP) |
| DEC-067 | 2026-06-28 | 5 | Architect | **Offline model bundle is a first-class MVP deliverable**: a single tarball containing all model weights (generation + embedding + reranker + NLI) with a manifest of SHA-256 checksums. Air-gap install path uses the bundle; public-network install can also use it to avoid HF download latency | REQ-011 30-minute cold-start install is implausible on enterprise networks pulling ~15 GB from public Hugging Face. Bundle is the only path that satisfies both REQ-011 and REQ-012 (air-gap) | Pull from HF only (rejected: REQ-011/012 violation); per-model individual bundles (rejected: integration friction) |
| DEC-068 | 2026-06-28 | 5 | User | **DEC-021 dev budget is raised to ¥800-1,200/month** to support ≥ 4 h/day cloud-GPU usage across the 3-month DEC-026 deadline (~360 h). Original ¥200/month limited actual development to ~2 h/day and threatened the deadline. Supersedes DEC-021 budget figure | RunPod 4090 spot ~$0.45/h × 4 h/day × 30 days ≈ $54/month ≈ ¥390/month per machine; with buffer for occasional on-demand pricing, training experiments, and night-time long-running evals, ¥800-1,200 is the honest envelope | Stay at ¥200/month (rejected: deadline risk); buy a workstation GPU (rejected: ¥6-8k sunk cost not justified pre-vendor-pilot) |
| DEC-069 | 2026-06-28 | 5 | User | **`acl_denial_mode` ships in MVP with both modes first-class**: `transparent` is the default; `opaque` is config-flippable per customer with no schema change required. Reaffirms DEC-042 with explicit shipping posture | B2B2B customers with sensitive corpora (HR/exec compensation) need `opaque`; default for general B2B is `transparent`; both must be production-ready since flipping mid-deployment is a foreseeable customer request | Ship transparent only and add opaque later (rejected: schema-change friction); ship opaque only (rejected: UX dead-end for general customers) |
| DEC-070 | 2026-06-28 | 5 | User | **Audit `audit_events` is append-only and immutable** in the MVP shipping posture. GDPR right-to-erasure requests against `audit_events` are deferred to the first regulated AU/EU buyer engagement (paired with the compliance work-package per DEC-006) | Single-tenant on-prem + audit-as-product-pillar; immutability is the design promise. Erasure right is a real obligation but its first concrete trigger is the first regulated buyer; spec is honest about the deferral | Make audit deletable now (rejected: breaks the audit-pillar promise); refuse erasure right entirely (rejected: legal exposure) |
| DEC-071 | 2026-06-28 | 5 | User | **Document checkout/checkin events are out of scope**. GroundedDocs query semantics are **version-based**: only the latest committed `version_id` is queryable. Uncommitted edits in the ECM during checkout are invisible by design. When `version_added` fires after checkin, re-ingest replaces the prior version's chunks. No `checkout_started` / `checkin_completed` events are tracked | An in-flight uncommitted edit cannot be synced to RAG; 2026 best-practice (Microsoft Graph Connectors, AWS Q Business, Glean) is to index only committed versions. Documenting this as a deliberate non-goal prevents vendor evaluators from reading it as a gap | Track checkout/checkin events (rejected: cannot sync in-flight content; adds complexity for no benefit); index uncommitted drafts (rejected: out-of-product-promise) |
| DEC-072 | 2026-06-28 | 5 | User | **First-wave target market = Australia + New Zealand**. Compliance reference frame for MVP positioning: AU Privacy Act 1988 + Notifiable Data Breaches (NDB) scheme + AU AI Ethics Principles 2019; NZ Privacy Act 2020 + IPP 13 principles. On-prem + air-gap positioning maps well to AU public sector + finance + mining; OpenText/Documentum installed base in AU/NZ is substantial. Adapter priority remains as DEC-018 + DEC-050 (M-Files + Hyland strong in AU; OpenText dominant in NZ public sector) | User customer relationships are AU/NZ; sovereign-cloud + "data must not leave country" procurement clauses are common in target verticals; English-language market (consistent with DEC-052) | Target US/EU first (rejected: no customer access); target China first (rejected: language scope mismatch + market crowded) |
| DEC-073 | 2026-06-29 | 5R2 | User | **Team / timeline planning envelope expanded to 2-3 team members / 180 days.** Reality today is **still solo**; the planning envelope is for **spec sizing** (architecture choices, V2 roadmap aggressiveness, framework adoption decisions). Hard constraint paired with the envelope: **the MVP must-have subset must remain solo-partial-deliverable within 180 days** if the team does not materialize. Supersedes DEC-026 (3-month / 90-day demo deadline) and reframes the solo-only constraint that influenced DEC-032 / DEC-034 / DEC-066. Target MVP demo date shifts from **2026-09-27 → 2026-12-26** | Solo + 90 days forced too-pragmatic let-downs against 2026 RAG best practice (Stage 5 Round 2 trigger). Pragmatic let-downs were anchored on solo capacity; relaxing the envelope is the right precondition for the Round 2 re-examination. Solo-fallback clause preserves honesty: spec authors must not assume team members exist | Keep solo / 90 days (rejected: forces too many "best-practice let-downs" and contradicts Q2=c policy "V2+ must be 2026-aligned"); commit to real hire (rejected: no concrete hire identified — would be aspirational without basis) |
| DEC-074 | 2026-06-29 | 5R2 | User | **Dev budget envelope revised to ¥2,000-3,000/month** for the 2-3 team planning envelope (DEC-073), proportionally scaled from DEC-068's ¥800-1,200/month single-developer figure. While team headcount is still solo, **the actual current spend remains at the DEC-068 figure (¥800-1,200/month)** and only steps up when a second contributor onboards. Supersedes DEC-068 budget headroom (DEC-068 figure stays as the solo floor) | Spec sizing needs an honest envelope, not the current spend. Two parallel cloud rigs ≈ ¥1,600-2,400/month + occasional on-demand + experiment budget ≈ ¥2,000-3,000/month at full team. Solo floor preserved so current spend does not auto-inflate | Use single budget figure (rejected: misleads spec consumers about team-scale assumptions); cap at DEC-068 figure (rejected: prevents headroom planning) |
| DEC-075 | 2026-06-29 | 5R2 | User | **Pipeline orchestration shape = full LangGraph (R2-HL-01 option c).** Whole pipeline becomes a typed graph: nodes for `retrieve`, `rerank`, `generate`, `verify`, `audit`, with feedback edges supporting REQ-020 (V2 claim decomposition) and REQ-021/022 (V2 ReAct). LangGraph 0.2.x is adopted as the orchestration framework. **Partially supersedes DEC-032** — the build-from-scratch posture is dropped for *orchestration*, but retained for `verify/` algorithm internals (NLI + citation mechanical check), retrieval logic, ACL adapters, and audit semantics. §3.2 rationale #2 ("frameworks resist mid-flight rewriting") is retired; rationales #1 / #3 / #4 stand at the node level | 2026 senior-architect default per `92a-stage5r2-benchmark.md` Gap 2: ~70% of production agents use graph/state-machine composition; LangGraph + Workflows 1.0 are explicitly designed for mid-flight rewriting; streaming/token interception is research-grade, not production. Under DEC-073 team envelope, LangGraph onboarding is affordable. Full adoption (over partial) chosen by user; rationale per user: V2 REQ-020/021/022 + ReAct lands as node additions rather than architectural redo. Compatibility forward-pressure favors full adoption | (a) Hand-rolled FSM (rejected by user: spec-writing cost; correctness burden); (b) LangGraph for verify subgraph only (rejected by user: V2 expansion would still require graph generalization later); maintain linear pipeline (rejected: R2-HL-01 evidence) |
| DEC-076 | 2026-06-29 | 5R2 | User | **Redis is in MVP (R2-T5-02 option iii). Supersedes DEC-034 partial.** Redis (or Valkey OSS fork) is introduced as: (i) hot-path cache layer (prompt cache, answer cache, ACL cache, embedding cache), (ii) optional queue backing for high-priority paths. Postgres SKIP LOCKED queue per DEC-038 **remains for persistent jobs** (ingest, CDC re-poll); Redis-backed queue is reserved for low-latency in-request work. **DEC-034's exclusion of Celery / Kafka still holds.** Concurrency cap (DEC-066's ≤2) is now liftable to a higher figure pending hardware-floor recalculation (cascade question 1) | User wants the AU/NZ vendor-demo concurrency story to be > 2; hold-cap + demo-mode framing was rejected. Adding Redis lifts cap on single host without requiring multi-host MVP. Operational cost (one more container, one more failure mode) accepted as trade-off. Benchmark §Topic 2 ("Redis still wins" camp) endorses hybrid at higher concurrency targets. Valkey is the recommended fork given Redis license change in 2024 | (i) Hold ≤2 cap + demo-mode framing (rejected by user); (ii) V2 multi-host (rejected by user: pushes the concurrency story past MVP demo) |
| DEC-077 | 2026-06-29 | 5R2 | User | **Layered safety rails are MVP (R2-T4-02 reversed).** MVP adds: (a) input rail — `Llama Prompt Guard 2` for prompt injection / jailbreak detection, (b) output rail — `Llama Guard 3` for harmful output classification, (c) orchestration rail — `NeMo Guardrails` for declarative policy enforcement, **on top of** the existing NLI verifier + 5-class refusal taxonomy. **Demo date is accepted to push out** to accommodate added integration + VRAM budget; new demo date pending cascade question 2 | User picked aggressive 2026 alignment over MVP timeline pragmatism. Benchmark §Topic 9 (`high` confidence): layered rails are 2026 production default for AI agents handling enterprise data; vendor evaluators in AU/NZ regulated verticals expect this. Trade-off: VRAM floor lifts (cascade question 1) and demo date pushes (cascade question 2) | Confirm MVP NLI-only + V2 layered rails (rejected by user) |
| DEC-078 | 2026-06-29 | 5R2 | User | **Golden set sized to 150-200 prompts with 50-prompt smoke-test subset.** The original 50-question goldset (per `23-evals-guardrails.md §2.2`) is preserved as the CI fast-feedback inner ring; 100-150 additional prompts are constructed to reach the 150-200 target. Composition rules (refusal/grounding/edge-case mix) per `23-evals` §2.2 extended to the larger set | 2026 benchmark §Topic 5 default is 100-300 prompts. 50 is undersized and would be flagged by Stage 8 audit. Under DEC-073 envelope the larger set is feasible (manual curation ~2 weeks at 2-3 team) | Hold 50 (rejected by user: known-undersized); 300 prompts (rejected by user: curation cost not justified for demo) |
| DEC-079 | 2026-06-29 | 5R2 Cascade | User | **Customer hardware VRAM floor = 24 GB.** Supersedes DEC-041 (original 24 GB) and DEC-052 (reduced to 16 GB on English-only). VRAM allocation (approximate, int4 quantization): Llama-3.1-8B-Instruct ~5 GB + bge-large-en-v1.5 ~1.3 GB + bge-reranker-v2-m3 ~1.1 GB + deberta-v3-base-mnli ~0.7 GB + Llama Prompt Guard 2 ~1.5 GB + Llama Guard 3 8B ~5 GB + KV cache + headroom ~6-8 GB ≈ 20-22 GB; 24 GB tier covers this with margin. Customer hardware tier: **RTX 4090 / A5000 / A6000 / L40S / H100 / B40+**. RTX 3060 12GB / 4060 Ti 16GB are out of MVP support | Stack expansion from DEC-075/076/077 (Redis container excluded from VRAM calc; uses host RAM) pushes minimum VRAM above the 16 GB DEC-052 figure. 24 GB is the lowest GPU tier in 2026 customer install bases that fits the expanded stack with operational headroom | 16 GB with CPU offload (rejected: layered rails inference latency unacceptable on CPU); 32 GB (rejected: cuts customer hardware coverage too far); split-tier (rejected by user: complexity outweighs deployment-coverage gain) |
| DEC-080 | 2026-06-29 | 5R2 Cascade | User | **Target demo date = 2027-03-26 (+90 days from prior 2026-12-26 target per DEC-073).** Supersedes DEC-026 (original 2026-09-27) and DEC-073 timeline portion. The +90-day buffer accommodates: ~30 d full-LangGraph adoption + retrieval/verify subgraph design + checkpoint/replay; ~15 d Redis integration + cache-invalidation policy; ~15 d three guard-model integration + VRAM/latency tuning; ~15 d golden set curation (150-200 prompts) + RAGAS threshold calibration; ~15 d B2B2B customer co-deployment dry-runs + slack | User picked +90d (conservative) over +60d (recommended) and +30d (aggressive). Rationale: full-LangGraph + Redis + 3 guard models is the longest critical path GroundedDocs has yet taken; +60d underestimates RAGAS threshold calibration + customer connectivity (DEC-055 ECM co-location) dry-run requirements; +30d is unrealistic and would force quality concessions | +60d (rejected by user: high risk of quality concessions); +30d (rejected by user: unrealistic critical path); no push (rejected: aggressive R2 gate choices make 2026-12-26 unachievable) |
| DEC-081 | 2026-06-29 | 5R2 Cascade | User | **Solo-fallback subset = full team scope (no scope cut on the solo path).** Supersedes DEC-073 solo-fallback clause: the original promise "MVP must-have subset must remain solo-deliverable within 180 days" is **withdrawn**. Under DEC-075/076/077/079's aggressive 2026 alignment, the MVP scope is no longer solo-deliverable within 180 days. The new posture: **solo path delivers the same scope, on a longer timeline (estimated 2027-05 or later)**; the team-path date of 2027-03-26 (DEC-080) applies only if the 2-3 team materializes by 2026-08 (60 d ramp window). **RISK-007 severity is raised to HIGH** to reflect the bet on team materialization | User chose explicit honesty over preserving the solo-fallback contract. Cutting scope on the solo path (drop LangGraph or layered rails or 150-200 goldset) would create two materially different MVPs and confuse vendor evaluators. Trade-off: timetable is at the mercy of team materialization. Mitigated by: pre-committing to dropping team-path date and announcing solo extension transparently if no team by 2026-08 | Drop LangGraph on solo path (rejected by user: bifurcates spec); drop layered rails on solo path (rejected by user: bifurcates spec); preserve 180-day promise by cutting scope (rejected by user: forces dual MVPs) |
| DEC-082 | 2026-06-29 | 5R3 | User | **Round 3 consolidated architecture refinement (Llama Guard 3 int4 AWQ + SafetyRailAdapter protocol + parallel `safety_input ∥ retrieve` graph edge + `verify/` mechanical-fast / NLI-slow explicit bifurcation).** Llama Guard 3 8B switches from standard int4 to **int4 AWQ** quantization (~2.5 GB VRAM, was ~5 GB). New §4.3 introduces **`SafetyRailAdapter` Protocol** (`classify(text) -> SafetyVerdict`, `health()`, `model_version()`) mirroring REQ-033's adapter pattern; default MVP wraps Llama Prompt Guard 2 (input) and Llama Guard 3 8B int4 AWQ (output); customer-side swap to alternatives (ShieldGemma 2B, BERT-class fine-tune) is supported subject to RAGAS-validated regression. LangGraph node design shifts from `safety_input → retrieve` to **`[safety_input ∥ retrieve]` parallel fan-out → `acl/` join** (`safety_input` latency masked under retrieval; on flag, retrieval result discarded and routed to `policy_blocked`). `verify/` is explicitly bifurcated into **`mechanical_fast_path` (≤1 ms regex/dict)** and **`nli_slow_path` (≤600 ms warm)** with early-exit short-circuit. **Supersedes DEC-077 partial** (LG3 quantization detail) **and DEC-079 partial** (VRAM allocation refinement) | External senior-architect feedback (`feedback.txt` S1.1 + S2.1 + S4.2 + S6.1) flagged: (a) LG3 8B is the largest single VRAM consumer; AWQ int4 saves ~2.5 GB without quality loss per benchmark; (b) safety-rail vendor lock-in is unhealthy — adapter pattern preserves swap freedom; (c) sequential `safety_input → retrieve` wastes the ~10-30 ms safety-rail latency that could mask under ~250 ms retrieval; (d) NLI cost should not be paid on obviously-broken mechanical-check failures. All four optimizations preserve the contract-grade citation SLO positioning | Keep DEC-077 as-is (rejected by user: 3 GB VRAM saving + adapter freedom worth the spec churn); replace LG3 with ShieldGemma 2B (rejected by user choice (a): ecosystem maturity argument); sequential pipeline (rejected: feedback evidence of latency waste); merge mechanical + NLI into a single check (rejected: mechanical fail is deterministic, NLI is probabilistic — separate by design) |
| DEC-083 | 2026-06-29 | 5R3 | User | **vLLM optimization flags pinned in MVP shipping config.** `--enable-chunked-prefill` is **mandatory** (smooths TTFT for long-context RAG prompts; no VRAM cost). `--speculative-decoding` is **conditional on measured VRAM headroom**: default `false` for the 24 GB floor tier; default `true` for the 32 GB comfort tier; controlled by `vllm.speculative_decoding.enabled` config flag. Draft model when enabled = `Llama-3.2-1B-Instruct` int4 (~1 GB VRAM). `09-deployment-ops` documents the validation procedure. Supplements DEC-012 (vLLM as serving engine) | External feedback (`feedback.txt` S2.3) and `92a-stage5r2-benchmark.md` §Topic 7 align: chunked prefill is 2026 default-on; speculative decoding offers 1.5-2× token throughput improvement when VRAM allows. Conditional default protects the floor tier where the +1 GB draft model would consume headroom needed for KV cache | Speculative decoding always-on (rejected: conflicts with DEC-079 24 GB floor headroom); always-off (rejected: leaves measurable performance on the table at comfort tier); customer-chosen with no recommended default (rejected: configuration paralysis) |
| DEC-084 | 2026-06-29 | 5R3 | User | **V2 admin-configurable intent classifier may downgrade NLI to advisory mode for low-risk intents** (e.g., chitchat / general FAQ / lookup-style queries), gated by **per-customer SLA waiver** captured in `audit_events.policy_waiver_id`. **Hard discipline (cannot be overridden):** mechanical citation check + audit logging remain synchronous on every query, regardless of intent class; audit always records `nli_performed: bool` + `intent_class`; the "contract-grade citation SLO" positioning is preserved by the explicit waiver gate. Supplements DEC-005 (verify is the hot-path differentiator) by introducing V2 customisation discipline | External feedback (`feedback.txt` S3.1) noted that uniform NLI on chitchat traffic wastes latency budget. User chose option (b): allow the downgrade in V2 with strict gating rather than reject outright. Preserves positioning: vendor evaluators see "your contract still applies; you signed a waiver to opt out for specific intent classes" — not "NLI is sometimes skipped silently" | Reject outright and keep MVP NLI uniform (rejected by user choice (b)); allow without waiver (rejected: positioning damage); ship in MVP (rejected by user: V2 is the right phase for customer-configurable depth) |
| DEC-085 | 2026-06-29 | 5R3 | User | **V3 federated retrieval path for `legal_hold` / `sensitivity: high` document classes.** For these classes, GroundedDocs **bypasses pre-ingestion entirely**: at query time, an `ECMSearchAPIAdapter` (new V3 adapter class extending the existing ECM/CDC adapter contract) queries the customer's ECM Search API with the user's security context; ECM enforces ACL natively; returned passages are run through TEI rerank in-memory then handed to `generate/`. **No pre-ingested copies retained** — RAG-side TTL 0 for these documents. CDC sync path (DEC-051) is unchanged for normally-classified documents. Supplements DEC-046 (two-layer auth) and DEC-051 (CDC) by introducing the alternative retrieval topology | External feedback (`feedback.txt` S7.3) named federated search as the canonical answer to vendor evaluators asking "what about documents I can't risk replicating?". User chose option (a): V3 placement (post Helm + multi-host per REQ-026) keeps MVP/V2 critical paths focused. Documented as named V3 capability with explicit positioning rationale | V2-late placement (rejected by user choice (a): would stretch V2 critical path); MVP placement (rejected: out-of-scope for demo); reject as out-of-scope (rejected by user: legitimate AU/NZ regulated-vertical need) |
| DEC-086 | 2026-07-03 | 5R4 | User | **Reference embedding reverted to `bge-m3`.** Supersedes the embedding portion of DEC-052 (which had moved to `bge-large-en-v1.5` for the English-only stack) and reinstates the embedding portion of DEC-014. `bge-large-en-v1.5` is dense-only and produces no sparse vector, which silently broke REQ-003's "hybrid retrieval (dense + sparse + rerank)" requirement — no sparse-vector source existed anywhere in the English-only stack. `bge-m3` natively emits dense + sparse (lexical) + ColBERT-style multi-vector output in one forward pass, restoring hybrid retrieval without adding a separate sparse-encoder component. DEC-052's other English-only provisions (generation model, NLI model, golden-set language scope, NFR-003) are **unaffected and remain in force** — this DEC narrows DEC-052 to exclude the embedding row only. Hardware floor VRAM budget is recalculated: `bge-m3` (568M params) is heavier than `bge-large-en-v1.5` (335M params); TEI-served footprint estimated at ~2.2 GB (was ~1.3 GB), a ~0.9 GB increase absorbed against the DEC-079/082 24 GB floor's remaining headroom | Independent architecture review (2026-07-03) identified that the DEC-052 embedding swap had no sparse-vector replacement, making REQ-003's "hybrid" claim technically false for the shipped MVP stack. User chose to fix this by reverting the embedding model rather than bolting on a separate sparse encoder (e.g. SPLADE, Qdrant/BM25 via FastEmbed), since `bge-m3` already provides the needed capability natively and was the original DEC-014 pin before the English-only narrowing | Add a standalone sparse encoder alongside `bge-large-en-v1.5` (rejected by user: extra component, extra VRAM, extra failure mode for a capability `bge-m3` already provides); accept dense-only retrieval and reword REQ-003 (rejected by user: weakens a core competitive claim) |
| DEC-087 | 2026-07-03 | 5R4 | User | **`audit_events.citations` stores a verbatim snapshot of the cited span text at answer time**, not merely a `chunk_id` pointer. Supplements DEC-046 (retention-driven physical deletion, no soft-flag) and DEC-070 (audit append-only, immutable forever). Because `chunks` rows are subject to mandatory physical deletion on `retention_expired` or `version_deleted` (DEC-046), a pointer-only citation record becomes unreconstructable once the source chunk is legitimately deleted — the audit trail would retain a `chunk_id` that resolves to nothing. Storing the cited text verbatim inside the (separately retained, already-immutable) `audit_events` row makes each audit record self-contained and evidentiary-complete independent of the `chunks` table's lifecycle | Independent architecture review (2026-07-03) identified that "audit-ready by default" (a named product differentiator) and LCC Tier 3 dispute-forensics ("which threshold caused this refusal, reconstructible from MVP day one" per DEC-060) both implicitly require the ability to verify what a historical citation actually said — which is impossible with pointer-only storage after physical deletion. User chose the verbatim-snapshot approach over documenting the gap as an accepted limitation | Keep pointer-only citations + document the reconstruction gap as a known limitation (rejected by user: undermines the audit-ready differentiator and the LCC forensics promise); snapshot only on retention-expiry event, not at write time (rejected: adds a second code path and a race window between answer time and the eventual deletion event) |
| DEC-088 | 2026-07-03 | 5R4 | Architect (per user-approved review) | **`verify/.mechanical_fast_path` validates citations against `reranked_set`** (the Layer-2-authorized, reranked chunk set actually passed into `generate/`'s prompt), **not** the raw `retrieval_set` (the pre-Layer-2, over-fetched top-K=50 candidate pool). Supplements DEC-005 (`verify/` as the hot-path differentiator) and DEC-046 (two-layer authorization) | Independent architecture review (2026-07-03) found that `04-architecture.md §8.1`'s mechanical-check pseudocode and `§5.1.1`'s typed state schema disagreed on which set citations should be checked against. As written, a chunk present in the raw `retrieval_set` but removed by Layer 2 ACL trim (i.e. content the user is not authorized to see) would still pass the mechanical fabrication check if the model hallucinated a citation to it — an ACL bypass via citation fabrication, and a hole directly in the NFR-004 100%-hit-rate hard gate. This is a correctness/security fix, not a scope change | Leave the ambiguous parameter name and rely on implementer judgment (rejected: the exact failure mode — ACL-bypass via citation — is not something to leave to interpretation); check against `acl_trimmed_set` instead of `reranked_set` (rejected: `reranked_set` is what the LLM actually saw in its prompt, so it is the correct ground truth for "could this citation possibly be genuine") |
| DEC-089 | 2026-07-03 | 5R4 | Architect (per user-approved review) | **`context_fingerprint` schema extended with safety-rail forensic fields**: `safety_input_adapter`, `safety_input_version`, `safety_output_adapter`, `safety_output_version`, `policy_ruleset_version` (NeMo Guardrails declarative ruleset version). Supplements DEC-060 (context fingerprint promoted to MVP) and REQ-050 (`SafetyRailAdapter.model_version()`) | Independent architecture review (2026-07-03) found that DEC-060's fingerprint field list (written before DEC-077/082 made layered safety rails MVP) never absorbed the safety-rail dimension, even though REQ-050 explicitly defines `model_version()` for this purpose. Without these fields, a `policy_blocked` refusal cannot be traced to the specific rail model/version/ruleset that produced it — the exact scenario DEC-060 was written to make traceable | Leave safety-rail verdicts in the audit record without version fingerprinting (rejected: `policy_blocked` is the refusal class most likely to be disputed by a customer, and it would be the one class DEC-060's forensics promise does not cover) |
| DEC-090 | 2026-07-03 | 5R4 | Architect (per user-approved review) | **Periodic full reconciliation crawl added between ECM and RAG document inventories**, MVP scope = detect + ops alert only (no auto-remediation). Runs at a low frequency (recommended weekly, admin-configurable). Supplements DEC-051 (CDC webhook + 30-min re-poll fallback) | Independent architecture review (2026-07-03) found that CDC (event-driven, differential) and re-poll (differential, 30-min window) both assume no gap longer than the re-poll window; neither detects drift caused by extended RAG-service downtime or a missed event outside any poll window. AU/NZ first-wave verticals (public sector, finance, mining, per DEC-072) have elevated ACL-churn profiles (reorgs, offboarding) where undetected drift is a compliance risk, not just a data-quality one | No reconciliation mechanism, rely solely on CDC + re-poll (rejected: no way to detect silent drift after an extended outage); auto-remediate on reconciliation mismatch (rejected for MVP: auto-deleting or auto-reingesting based on a reconciliation diff is a destructive action better left to an ops-reviewed alert at this stage) |
| DEC-091 | 2026-07-03 | 5R4 | Architect (per user-approved review) | **`legal_hold_added` invalidates the vLLM KV-cache for any active conversation whose recent-turn context (per `conversation_id`, last N=5 turns) includes the newly frozen document.** Supplements DEC-046 (legal hold freeze semantics) and DEC-063 (PDP circuit breaker) | Independent architecture review (2026-07-03) found that §7B.6's legal-hold freeze (not deletable/updatable/re-indexable) did not address content already materialized inside a live conversation's KV-cache (§9.4 cross-turn KV-cache reuse, DEC-054). A document placed under legal hold mid-conversation could still influence generation in later turns of the same session via cached attention state — a more severe compliance exposure than the already-documented generic mid-session ACL-change limitation (D7-12), because legal hold implies an active litigation-hold obligation | Leave as a documented known limitation like the generic ACL-change case (rejected: legal hold carries materially higher compliance stakes than an ordinary ACL tightening, and a targeted fix — cache invalidation scoped to the affected `conversation_id` — is cheap relative to the risk) |
| DEC-092 | 2026-07-03 | 5R4 | Architect (per user-approved review) | **Quantization changes to safety-rail models require a hazard-detection accuracy-preservation check before shipping as the MVP default.** Applies retroactively to DEC-082's Llama Guard 3 8B → int4 AWQ change: before that quantized adapter ships as the MVP default, a documented before/after comparison of hazard-classification F1/recall on a red-team test set (e.g. a HarmBench-derived subset) must be recorded in `09-deployment-ops` or `23-evals-guardrails.md`. Supplements DEC-082 and REQ-050 | Independent architecture review (2026-07-03) found that DEC-082 justified the AWQ switch entirely on VRAM savings with no cited data on whether int4 AWQ quantization preserves Llama Guard 3's hazard-detection accuracy — an omission that matters more for a safety-classifier than for a generation model, since a quantization-induced accuracy regression here directly weakens the last line of defense in the layered-rail stack | Ship the quantized adapter without a validation gate (rejected: safety-critical component; VRAM savings alone is not sufficient justification) |
| DEC-093 | 2026-07-03 | 5R4 | Architect (per user-approved review) | **ONNX Runtime is the MVP-default TEI rerank backend** (promoted from "recommended for AU/NZ public-sector deployments" to the shipped default for all MVP installs). Supersedes the backend-choice framing in NFR-027 | Independent architecture review (2026-07-03) found that NFR-027 documented the TEI default backend as an acceptable "fallback" at 150-200 ms p95, which individually exceeds the ≤150 ms rerank line-item budgeted in `§7B.12`'s latency SLO calculation — meaning the simplest, most likely first-install path (TEI default) risked silently violating the demo latency promise the SLO table is built on. Making ONNX Runtime the default (not an opt-in tuning step) keeps the documented install path and the documented latency budget consistent | Leave TEI default as the base install and ONNX as an optional tuning step (rejected: the "default path" and the "SLO-compliant path" must be the same path, or the SLO table is misleading) |
| DEC-094 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **§1 plain-English summary corrected: `verify/` is the differentiator for claim/citation grounding, but not the system's only refusal point.** `safety_input/` and `safety_output/` (DEC-077 layered rails, DEC-082 parallel fan-out) also produce `policy_blocked` refusals, before retrieval and before verification respectively. No behavior change — documentation-only fix. Supplements DEC-077, DEC-082 | Independent architecture review (2026-07-05) found that `04-architecture.md §1` (not revised in the Round 2/3 supersede notes, unlike §2 which was) still claimed `verify/` was "the only place where the system can reject an answer mid-flight" — directly contradicted by §4.3's verdict-to-refusal mapping added in Round 3. §1 is read first by any reviewer or engineer onboarding to the spec, so a stale claim there risks propagating a wrong mental model of the refusal architecture | Leave §1 as a simplified/non-literal summary (rejected: the sentence makes a specific, falsifiable claim — "only place" — not a simplification, and it is factually wrong against §4.3) |
| DEC-095 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **§5.1 module call-direction table extended with governance rows for `safety_input/`, `safety_output/`, `policy/`, and `cache/`** — the four nodes/services introduced by DEC-075/076/077/082. Supplements DEC-075, DEC-076, DEC-077, DEC-082 | Independent architecture review (2026-07-05) found that §5.1's call-direction table only covered the 13 pre-Round-2 modules; the four nodes added in Round 2/3 had no documented call-direction rule, leaving the `tests/architecture/` CI import-graph enforcement (§5.1, closing line) with nothing to check them against | Leave the 4 new nodes ungoverned until V2-grade enforcement lands (rejected: the MVP-grade CI check described in the same section is supposed to cover exactly this, and silently exempting the newest, safety-critical nodes defeats its purpose) |
| DEC-096 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **Retrieval-rail indirect-injection scan (§12.2 point 2, DEC-077) is confirmed to run inside `acl/`'s Layer 2 join step** (after PDP trim, before `rerank/`) — matching the ordering §8.1's pipeline pseudocode already assumed, **not** a new standalone graph node. Module map (§5.0), call-direction table (§5.1: `acl/` row extended), typed state schema (§5.1.1: new `retrieval_safety_verdicts` field), verdict-to-refusal mapping (§4.3), and the Mermaid/ASCII diagrams are now aligned to §8.1. Extends DEC-042's `verification_unavailable` trigger set to include "retrieval-rail flagged-chunk drop rate exceeds threshold." Supplements DEC-077, DEC-082, DEC-088 | Independent architecture review (2026-07-05, Security dimension pass) found that the retrieval-rail scan was asserted as MVP functionality in §12.2 and the §12.5 STRIDE table, and §8.1's pipeline pseudocode already specified where it runs — but §5.0, §5.1, §5.1.1, and the Mermaid diagram (§7B.11) had no representation of it at all, meaning an implementer following only the module map/call-direction table/typed-state contract would never build it. A first-draft fix introduced a standalone `safety_retrieval/` node before noticing it duplicated and contradicted §8.1's existing acl/-embedded design; corrected to align with §8.1 rather than add a second, conflicting mechanism | Add `safety_retrieval/` as a new standalone LangGraph node (rejected: contradicts §8.1's already-specified "runs after PDP trim inside acl/" ordering, and would require re-deriving latency/state-machine semantics that §8.1 already defines correctly) |
| DEC-097 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **§7B.12 latency budget gains an explicit line item for the retrieval-rail scan** (≤ 80 ms warm / +30 ms cold, batched Llama Prompt Guard 2 call inside `acl/`). Revised totals: warm-cache p95 ≤ 7,240 ms (760 ms headroom, was 840 ms); cold-cache lower bound ≤ 7,890 ms (110 ms headroom, was 140 ms). Supplements DEC-096 | The retrieval-rail scan was always in scope per §8.1 but had no budgeted latency cost, meaning its true cost was invisible in the SLO accounting against NFR-005's 8 s cap. Cold-cache headroom is now thin (110 ms) — flagged as a risk to revisit if per-chunk scan cost grows with authorized-chunk count, rather than silently absorbed | Leave the scan unbudgeted (rejected: same "default path ≠ SLO-compliant path" risk DEC-093 was written to close, just for a different line item) |
| DEC-098 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **Three reliability findings fixed**: (a) `20-agent-behavior.md` §6's failure table wrongly attributed the retrieval-rail scan to `safety_input/`; corrected to `acl/` per DEC-096/§8.1. (b) Two named-but-untracked reliability risks — VRAM admission-control gap and cold-cache burst/demo-day scenario (both `04-architecture.md` §9.4, dated 2026-07-03 as prose-only "review findings" with no tracking id) — promoted to **NFR-030** and **NFR-031** in `02-requirements.md`. (c) `20-agent-behavior.md` §6's failure table had no row for TEI embedding/rerank service unavailability, despite TEI being a separately-failable container per §4.1/§9.1; added two rows (503, same pattern as the existing vLLM row) | Independent architecture review (2026-07-05, Reliability dimension pass) found the retrieval-rail attribution bug as a direct consequence of the Security pass's DEC-096 fix — the two "single source of truth" documents for refusal/failure behavior (`04-architecture.md` and `20-agent-behavior.md`) had drifted apart the moment one was corrected and the other wasn't. The two untracked risks were flagged as a traceability gap: prose-only findings deferred to a not-yet-written `09-deployment-ops.md` have no owner and can silently disappear; assigning NFR numbers now prevents that. The TEI gap was found by cross-checking the failure table against the docker-compose service list (§9.1) rather than against the table's own internal logic | Leave the two risks as prose pending `09-deployment-ops` (rejected: same untracked-risk pattern already causing drift elsewhere in this log — see DEC-093's "default path ≠ SLO path" precedent); leave TEI failure implicit under "vLLM unhealthy" (rejected: TEI and vLLM are separate containers per §9.1 and fail independently — conflating them hides a real failure mode from whoever builds the health-check logic) |
| DEC-099 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **Two Performance-dimension fixes to `23-evals-guardrails.md`**: (a) §7.2's tuning-lever table said "Increase top-K from 10 → 20," contradicting the documented retrieval top-K default of 50 (NFR-027, `04-architecture.md` §7B.3/§7B.4) — corrected to "increase above the documented default of 50 — e.g. to 75 or 100." (b) The file had two sections both numbered "§6" (`Threshold-tuning protocol` and `Customer onboarding runbook`) — the runbook was added later and deliberately labeled "§6" to match RC-T6-01's tracking note, colliding with the pre-existing §6. Renumbered the runbook and its subsections to §7 (7.1–7.3), cascading the following two sections to §8 (Open questions) and §9 (Cross-references) | Independent architecture review (2026-07-05, Performance dimension pass) found the top-K mismatch would mislead an LCC/ops engineer following the onboarding runbook during a live customer tuning session — the runbook implies a starting point (10) that does not match what the system actually ships with (50), risking either a no-op "fix" or confusion when the real config doesn't match the doc. The duplicate heading was found by cross-referencing `92-stage5-review-memos.md`'s RC-T6-01 tracking note, which explains *why* the collision exists (the runbook's number was chosen to satisfy the tracking note, not derived from the surrounding document structure) | Leave top-K number as-is on the theory it might refer to a distinct, undocumented "chunks passed to generate/" parameter (rejected: no such parameter is defined anywhere in `04-architecture.md`; the more likely explanation is the number is stale/illustrative and was never reconciled against NFR-027); leave the duplicate `§6` and rely on readers using titles instead of numbers (rejected: the whole point of numbered sections is unambiguous cross-referencing, and other specs already cite these sections by number) |
| DEC-100 | 2026-07-05 | 5R5 | User + Architect (per user-approved review) | **The GPU sub-matrix's (§4.2.1) explicit rejection of 16-20 GB GPUs (Tesla T4, A2, RTX A4000) is authoritative, overriding the main hardware matrix's (§4.2) prior "Degraded" tier claim of GPU-based degraded-mode support at that VRAM range.** The "Degraded (GPU)" row is retired; 24 GB is now a hard GPU floor with no supported sub-24GB GPU tier. The CPU-only degraded-edge tier remains the sole below-floor fallback, already explicitly flagged as NFR-005-noncompliant. `NFR-001` corrected to match | Independent architecture review (2026-07-05, Deployment & Cost dimension pass) found the two tables gave opposite answers to the same customer-facing question ("will it run on my hardware") — §4.2 said 16-20 GB is supported in degraded mode, §4.2.1 said the same VRAM range's actual GPUs are rejected outright. Checking for a GPU that is in the 16-20 GB window but *not* on §4.2.1's rejection list found none — every cataloged card in that range is explicitly rejected, so the "Degraded (GPU)" row had no valid hardware to describe. User chose to resolve by trusting the sub-matrix (finer-grained, DEC-079-dated) over reconciling/preserving the coarser tier | Keep the Degraded (GPU) tier and remove the sub-matrix rejection instead (rejected by user: sub-matrix's per-model detail is more authoritative than the coarse tier table); try to find replacement 16-20GB hardware not on the rejection list to keep the tier alive (rejected: no such cataloged GPU exists; would require inventing a supported card not grounded in the sub-matrix's research) |
| DEC-101 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **docker-compose remains the MVP orchestration choice; Kubernetes is not adopted at MVP.** Formalizes a previously-implicit assumption. Revisit trigger: if/when V3 multi-host (REQ-026) is scheduled, K8s (or a simpler multi-host tool) becomes the natural point to introduce orchestration | User asked whether docker-compose meets MVP performance requirements or whether K8s should be adopted instead. Analysis: the performance model (§9.4 concurrency, §7B.12 latency) is bounded by single-GPU compute/VRAM, not by container orchestration choice — K8s does not make one GPU faster. K8s's real value (multi-node scheduling, cross-host resilience) matches V3 multi-host (REQ-026), which is out of MVP scope. Adopting K8s now would work against REQ-011's ≤30-min install target and the DEC-021/DEC-068 low-cost, low-headcount-deliverable positioning | Adopt K8s now for operational robustness even at single-node (rejected: marginal benefit at single-host scale does not justify the install-time and IT-literacy cost against REQ-011/DEC-072 target customers) |
| DEC-102 | 2026-07-05 | 5R5 | User + Architect (per user-approved review) | **Poll-only CDC transport mode is a first-class supported topology** (not an unplanned webhook-failure fallback) for customers whose network segmentation prohibits an inbound webhook listener reachable from the ECM (per §7B.0's procurement risk note). `cdc/` runs exclusively as an outbound poller against the ECM's event/change API; interval is admin-configurable (default 30 min, recommended 5 min where ECM API limits allow). Adds REQ-057 + NFR-032. No new infrastructure — reuses the existing re-poll code path; **does not** introduce an event bus, so DEC-034's Kafka exclusion is unaffected | User asked whether MVP should replace webhook with an event-bus ("Event方式") approach for customers where GroundedDocs and the ECM sit in different subnets/domains. Analysis found the premise incomplete: an event bus only helps if it inverts the connection direction (GroundedDocs subscribes/pulls) — if the ECM (or a broker in its zone) must still reach into GroundedDocs's zone to publish, the same segmentation problem recurs, just on a different port. It would also conflict with DEC-034's standing Kafka exclusion. The mechanism that actually solves the stated problem — GroundedDocs always initiates the connection — already exists as the re-poll fallback (DEC-051/DEC-056); the fix is to promote it to a deliberately-selected, configurably-fast topology with its own SLA, not to add new infrastructure | Add Kafka/EventGrid/Pub-Sub for MVP to formalize an "event" transport (rejected: conflicts with DEC-034's exclusion, adds infrastructure cost, and does not by itself solve the connection-direction problem unless GroundedDocs is the subscriber — which the existing poll mechanism already achieves without new infrastructure); leave the segmentation risk as an unresolved procurement question (rejected: same untracked-risk pattern flagged elsewhere in this log — DEC-098's precedent) |
| DEC-103 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **Two Maintainability fixes**: (a) `cdc/`'s call-direction row (§5.1) extended to permit invalidation-only calls into `cache/` (force-refresh ACL cache entries on `acl_changed`, per §7B.5); `cache/`'s row correspondingly notes `cdc/` as an invalidation-only caller, explicitly barred from business-data cache entries (prompt/answer/embedding). (b) `QueryGraphState` (§5.1.1) gains three V2-reserved, MVP-unset fields — `intent_class`, `nli_performed`, `policy_waiver_id` — matching REQ-051/DEC-084's V2 intent-classifier acceptance criteria, mirroring the existing `revision_count`/`failed_claims` reservation pattern already used for REQ-020 | Independent architecture review (2026-07-05, Maintainability dimension pass) found (a) as a real governance/functionality contradiction: §7B.5 requires `cdc/` to force-refresh the Redis ACL cache on `acl_changed`, but §5.1's CI-enforced call-direction table gave `cdc/` no permission to call `cache/` at all — implementing the documented CI check as written would break the documented cache-invalidation requirement. Found (b) as an asymmetry: the typed state schema already reserves fields for one V2 feature (REQ-020) to support this section's "graph-native extension, no schema migration" claim, but not for another equally-specified V2 feature (REQ-051) that needs schema fields per its own acceptance criteria — user chose to close the asymmetry now rather than defer it | For (a): leave `cdc/` without cache access and treat §7B.5's requirement as aspirational until V2 (rejected: §7B.5 states this as an MVP requirement, not V2 — the governance table was simply wrong); for (b): defer to V2 design time (user's initial suggestion, but user then changed course and asked to fix now for consistency with the already-reserved REQ-020 fields) |
| DEC-104 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **Two Evaluability fixes**: (a) `04-architecture.md` §8.1's `refusal_decision()` pseudocode corrected — "mechanical fail rate > X%" (implying a tunable percentage) changed to explicit "> 0%, hardcoded, not tunable — NFR-004," matching §8.5's threshold table and the earlier early-exit logic in the same section. (b) `23-evals-guardrails.md`'s golden set gains a new **Retrieval-rail refusal prompts** category (+5), testing that `acl/`'s retrieval-rail scan (DEC-096) correctly produces `verification_unavailable` — distinct from `safety_input/`'s `policy_blocked` — which had no goldset coverage until now | Independent architecture review (2026-07-05, Evaluability dimension pass) found (a) as a genuine internal inconsistency within §8.1 itself: the early-exit logic a few lines above already treats mechanical failure as zero-tolerance, but `refusal_decision()`'s own restatement used "X%" phrasing indistinguishable from the legitimately-tunable NLI "Y%" on the next line — a reader could reasonably conclude mechanical fail rate is configurable, which would violate NFR-004's 100% citation-hit-rate hard gate if implemented that way. Found (b) as a direct consequence of DEC-096 (Security pass, same session): a new refusal pathway was clarified in the architecture but never propagated into eval coverage, so no automated test would catch a regression in that specific path | For (a): leave as "X%" on the theory implementers will infer non-tunability from context (rejected: the exact reason to write "0%, hardcoded" instead of "X%" is to remove the need for that inference); for (b): treat as already covered by the existing "Layered-rail trigger prompts" category (rejected: that category's stated acceptance criterion is `policy_blocked` only, which is the wrong refusal class for this path per DEC-096 — it would not actually catch a regression) |
| DEC-105 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **`audit_events`'s safety-rail forensics capture (§4.3) explicitly extended to cover the per-chunk `retrieval_safety_verdicts` list** (DEC-096), not just the singular `safety_input_verdict`/`safety_output_verdict` fields the sentence previously named | Independent architecture review (2026-07-05, Traceability dimension pass) found that §4.3's audit-capture sentence ("`audit_events` captures the full `SafetyVerdict.raw_response` for forensics") was written before DEC-096 introduced the plural, per-chunk `retrieval_safety_verdicts` field, and was never updated to confirm that list is captured too. Left as-is, a disputed chunk-drop decision (why was this chunk excluded from an answer) would have no forensic trail, undermining the same DEC-060/DEC-089 forensics guarantee this section otherwise upholds | None considered — this is a direct, low-risk propagation of an already-decided fix (DEC-096) into a sentence that predates it, not a new design choice |
| DEC-106 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **KV-cache invalidation triggered by `legal_hold_added` (DEC-091) must itself be written to `audit_events`** (`conversation_id`, triggering `doc_id`, event timestamp, invalidation timestamp) — not just the underlying chunk freeze in Qdrant/Postgres. Supplements DEC-091 | Independent architecture review (2026-07-05, Traceability dimension pass) found that DEC-091's KV-cache invalidation requirement (§7B.6) had no corresponding audit-write requirement, even though DEC-091 itself frames legal hold as carrying "an active litigation-hold obligation" — the highest-stakes compliance case in the document. Without an audit record of the invalidation action, the system could prove a document was frozen but not that the conversation-level remediation (the actual mechanism preventing further disclosure via cached context) ran, which is precisely the evidence a litigation-hold dispute would require | Treat the chunk-freeze audit trail (already covered by DEC-070's immutable audit_events) as sufficient (rejected: freezing the chunk in the vector store and invalidating a live conversation's cache are two distinct actions with two distinct compliance claims — "the document is no longer retrievable" vs "this specific conversation stopped being influenced by it" — and only the first was previously provable from audit) |
| DEC-107 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **§7's API surface listing corrected from "three surfaces" to four (Query/Ingest/Admin/Operational)**, adding four endpoints that were already referenced elsewhere in `04-architecture.md` but never listed in §7 itself: `POST /v1/admin/eval` (from §5.0's api/ box), `POST /v1/admin/acl/refresh_user/{user_id}` and `POST /v1/admin/acl/refresh_doc/{doc_id}` (from §7B.5), and `GET /ready` (from §9.1). Also adds `PUT /v1/admin/config/cdc` for REQ-057/DEC-102's transport-mode selection. **Created `specs/06-api-contracts.md` as a stub** consolidating all endpoints into one canonical enumeration, explicitly marked as placeholder shapes pending Stage 6 | Independent architecture review (2026-07-05, API-contract dimension pass) found `06-api-contracts.md` did not exist, and that §7 — the interim canonical API listing until that file is written — had drifted: several endpoints were introduced in other sections (§5.0, §7B.5) and never reconciled back into §7's own enumeration. Since the matrix review plan (`00-index.md`) specifically calls for this pass to produce the missing file's stub rather than only findings, both the §7 correction and the new stub were done together | Leave `06-api-contracts.md` unwritten until Stage 6 (rejected: per the review matrix's own instruction, and because leaving four live endpoints undocumented in any canonical API listing is the same untracked-risk pattern flagged elsewhere in this log) |
| DEC-108 | 2026-07-05 | 5R5 | Architect (per user-approved review) | **Two ECM-integration fixes**: (a) §7B.5's opening sentence corrected — it framed webhook as the only MVP transport with an event bus as the only alternative (V2), not mentioning poll-only mode (DEC-102) even though poll-only was already made MVP-available three passes earlier in this same review. (b) `ECMAdapter`'s CDC interface (§7B.10) gains `poll_changes(cursor) -> tuple[list[Event], str]` — a pull-style method — alongside the existing push-style `subscribe_changes(handler)`; the reference-implementations table now notes `poll_changes()` is mandatory for any adapter offered to a poll-only customer | Independent architecture review (2026-07-05, ECM Integration dimension pass) found that DEC-102 (Deployment & Cost pass, same session) made poll-only mode a first-class MVP topology, but never propagated into (a) the plain-English description customers/integrators would read first, or (b) the actual adapter interface contract vendor-specific adapters must implement — meaning poll-only mode had a product decision and an SLA (NFR-032) but no corresponding method in the interface a real adapter would need to implement it. This is the same "fix doesn't propagate to every touched document" pattern already seen with DEC-096 (Security→Reliability, Security→Evaluability, Security→Traceability) — recurring across this review, now caught in the adapter contract before an implementer would have hit it | Leave `subscribe_changes` as the only CDC method and assume adapters can internally poll and self-invoke the handler (rejected: this hides the transport distinction from the interface, which defeats the point of `cdc_transport_mode` being admin-selectable per install — the interface should make explicit which method a poll-only adapter must implement) |
| DEC-109 | 2026-07-06 | R6.FixAudit | Architect (per user-approved review) | **NLI VRAM/CPU accounting corrected**: `04-architecture.md` §4.2.2's VRAM allocation table wrongly counted `deberta-v3-base-mnli`'s ~0.7 GB in "Subtotal (all GPU-resident)" despite §4.1/§9.1 both documenting NLI as CPU-resident by design. Table corrected to exclude NLI from the GPU subtotal; §4.1's NLI row annotated to confirm CPU residency explicitly. Headroom recalculated: ~5.0→~5.7 GB cold-cache, ~1.0→~1.7 GB warm-cache. §7B.12's DEC-097 latency-headroom figures (760 ms warm / 110 ms cold) are a *time-budget* headroom, unrelated to this *VRAM* headroom correction, and are unaffected | Round 6 independent review (2026-07-06, D1) found the two documented facts (CPU-resident vs counted-as-GPU-resident) directly contradicted each other, with the erroneous version feeding the already-thin, DEC-097-flagged VRAM headroom figure. NLI's ≤600 ms warm latency budget (§7B.12) is achievable CPU-side with batching for a base-class DeBERTa model, so CPU residency is retained (not moved to GPU) as the resolution | Move NLI to GPU-resident and keep the tighter headroom (rejected: no latency evidence requires GPU residency for a base-class NLI model, and moving it would further erode already-thin VRAM headroom for no measured benefit) |
| DEC-110 | 2026-07-06 | R6.FixAudit | Architect (per user-approved review) | **V2 `intent_classifier/` (REQ-051/DEC-084) latency-budget gap flagged in `04-architecture.md` §8.2**: the node is documented as running strictly sequentially before the `[safety_input ∥ retrieve]` parallel fan-out (DEC-082), with no latency-budget line item anywhere in §7B.12/NFR-029. Flagged as a gap to close before REQ-051 ships, with two named resolution directions (explicit budget line item, or folding the classification call into the existing parallel fan-out) — deliberately left as an open V2 design question, not resolved here, since REQ-051 is V2-scoped and MVP has no such node | Round 6 independent review (2026-07-06, D1) found that DEC-082's parallelization rationale ("mask cheap rail checks under expensive retrieval") is silently undermined by a new, unbudgeted sequential node introduced in the same document's V2 section. Deferred to V2 spec-writing per Round 6's own "deferred items" classification — no MVP behavior changes | Resolve the placement now by mandating a specific fix (rejected: this is a V2-scoped design trade-off between two credible directions, not a factual error correctable without further V2 design work; recording the gap explicitly is sufficient at this stage) |
| DEC-111 | 2026-07-06 | R6.FixAudit | User + Architect (per user-approved review) | **`01-product-brief.md` §9.3 stale golden-set figure corrected**: "100% on 50-question hand-curated set" updated to "100% on the 150-200 prompt full golden set (50-prompt smoke subset preserved as the CI fast-feedback ring)," matching DEC-078's Round 2 golden-set expansion | Round 6 independent review (2026-07-06, D2) found this row was never updated after DEC-078 (2026-06-29) expanded the golden set; `23-evals-guardrails.md` §2.2 and `02-requirements.md` REQ-049 already reflected the expansion, but `01-product-brief.md` §9.3 did not, creating a stale acceptance-criteria reference | None — this is a mechanical reconciliation of an already-decided figure (DEC-078), not a new decision |
| DEC-112 | 2026-07-06 | R6.FixAudit | User + Architect (per user-approved review) | **DEC-092's safety-rail quantization/adapter-change accuracy gate extended to REQ-033 (generation model swap) and REQ-034 (embedding model swap)**. Both REQs' acceptance criteria in `02-requirements.md` now require that a swap not be promoted to production unless post-swap RAGAS metrics hold at or above the DEC-017 MVP floor — reusing the RAGAS delta report both REQs already produce, not a new red-team test set (RAGAS is the right quality signal for generation/embedding, unlike for safety-rail detection accuracy which RAGAS cannot measure) | User decision (2026-07-06, resolving Round 6 open question 3): previously only safety-rail model changes carried an explicit pass/fail quality gate (DEC-092); generation and embedding swaps only required "producing comparable structured deltas" with no stated blocking threshold, leaving room for an unnoticed quality regression to ship during an LCC Tier 3 migration | Leave REQ-033/034 without an explicit gate (rejected by user: DEC-092's rationale — a swap needs a documented pass/fail check, not just a delta report — applies equally to generation and embedding models) |
| DEC-113 | 2026-07-06 | R6.FixAudit | Architect (per user-approved review) | **`acl_changed(doc_id)` event contract clarified to cover standalone `security_label` reclassification** (e.g. `confidential` → `internal`) as well as principal-set changes and folder-move-driven inheritance changes already covered. No new event type introduced — `get_effective_acl(doc_id)`'s response payload already includes `security_label`; the ECM-side adapter is now explicitly required to fire `acl_changed(doc_id)` on a label-only change too | Round 6 independent review (2026-07-06, D3) found the CDC event table's `acl_changed` row only explicitly named principal-set and folder-move triggers, leaving standalone label reclassification (which changes Layer 1's `security_label` filter field without changing `allow_principals[]`/`deny_principals[]`) as an undocumented gap in the event contract that a real ECM integration could miss | Introduce a dedicated `security_label_changed` event type (rejected: `acl_changed(doc_id)`'s existing re-call-and-refresh mechanism already re-fetches the full effective ACL including `security_label`; a new event type would duplicate that mechanism for no functional gain) |
| DEC-114 | 2026-07-06 | R6.FixAudit | Architect (per user-approved review) | **`ECMAdapter.get_effective_acl(leaf_doc_id)` contract clarified for compound/virtual documents**: the adapter implementation (not the caller) is responsible for resolving to the correct ACL-authority node (e.g. a Documentum virtual-document root, if that is where the ECM stores the ACL) and returning the leaf's fully-resolved effective ACL. Callers in `ingest/` and `acl/` always pass a leaf `doc_id` and always receive an already-resolved ACL; no compound-document tree-walking logic is required outside the adapter | Round 6 independent review (2026-07-06, D3) found that §7B.9's MVP leaf-only indexing decision addressed retrieval/citation granularity but left unstated where ACL-resolution responsibility sits when a compound document's ACL lives only at the root node — a contract ambiguity that would otherwise surface for the first time during V2-α (`DocumentumAdapter`/`OpenTextAdapter`) implementation, the highest technical-priority V2 adapters per DEC-050 | Defer clarification to V2-α adapter design time (rejected: cheaper to settle in writing now, before any adapter code depends on an assumed answer, than to discover the ambiguity as a blocking question mid-V2-α implementation) |
| DEC-115 | 2026-07-06 | R6.FixAudit | Architect (per user-approved review) | **Two Maintainability-adjacent process notes added**: (a) `confirmed-context.md` §6 gains a Stage 6/7 instruction to generate a one-time "currently-effective decisions" materialized view over `13-decision-log.md` (grouped by topic, latest-effective value only, linking back to full supersede history) given the log's now-115+-entry size and dense supersede chains; (b) `04-architecture.md` §12.3 gains a note that `09-deployment-ops` (Stage 7) must include an `audit_events` capacity-growth estimate (record size × QPS × retention years) and a minimum single-host storage/backup planning commitment, given DEC-070's append-only-forever posture and AU/NZ regulated-vertical multi-year retention obligations (DEC-072) | Round 6 independent review (2026-07-06, D4) found both as documentation/tooling gaps rather than functional defects: (a) the append-only decision log's own convention makes supersede-chain tracing an increasing cognitive burden with no materialized "current state" view; (b) `audit_events`'s immutable-forever posture (DEC-070) has never been paired with any capacity-planning estimate, despite being the one table in the system explicitly designed never to shrink | Change the decision log's format or `audit_events`'s retention policy (rejected: both would be scope creep beyond what the findings call for — these are planning/tooling gaps, not architecture or convention defects) |
| DEC-116 | 2026-07-06 | R6.FixAudit | User + Architect (per user-approved review) | **Redis answer cache gains a `doc_id → cache-key` reverse index, invalidated (targeted, not full-flush) on `legal_hold_added(doc_id)`**, extending DEC-091's KV-cache invalidation requirement to the second cache layer capable of re-serving frozen-document content. The invalidation action is written to `audit_events` alongside the existing DEC-106 KV-cache invalidation record (same evidentiary shape, `query_hash` set in place of `conversation_id`). Additionally records the explicit design conclusion that the answer-cache key does **not** need a "serving_config_hash" dimension covering `--speculative-decoding` (DEC-083) or the Prompt Guard 2 eviction policy (§4.2.2) — both are lossless/output-invariant serving parameters, not output-distribution-altering ones | User decision (2026-07-06, resolving Round 6 open question 1): Round 6 (D5) found that DEC-091's KV-cache fix for legal hold had no counterpart for the Redis answer cache, which could continue serving a cached answer citing frozen-document content for up to its 600 s TTL — the same compliance-risk class DEC-091 was written to close, uncovered in a second cache layer. The serving-config-hash question (a separate Round 6 D5 finding) was resolved by confirming neither named parameter affects output content, only latency/throughput or upstream-node availability | Full-flush the answer cache on every `legal_hold_added` event (rejected: unnecessarily destroys unrelated cache value; targeted eviction via reverse index achieves the same compliance outcome at lower cost); add a serving_config_hash dimension to the cache key regardless (rejected: no evidence either parameter changes output content; adding the dimension would needlessly fragment cache hit rates) |
| DEC-117 | 2026-07-06 | R6.FixAudit | User + Architect (per user-approved review) | **`/ready` health-check contract defined as a full-dependency aggregate check**: `GET /ready` returns HTTP 200 only after every backend service (`vllm`, `tei-embed`, `tei-rerank`, `nli`, `safety-input`, `safety-output`, `policy`, `qdrant`, `postgres`, `redis`) independently reports healthy — not merely after the `api/` process starts. Updated in `04-architecture.md` §9.1, `06-api-contracts.md`'s `/ready` entry, and `02-requirements.md` REQ-011's acceptance criterion. Does not change REQ-011's ≤30-minute install target — it changes what "ready" means at the end of that window, from a potentially-premature signal to an honest one | User decision (2026-07-06, resolving Round 6 open question 2): Round 6 (D5) found `/ready`'s dependency-check scope was previously undefined, risking an implementation that returns 200 once `api/` starts while model-serving containers are still loading weights — producing a false-green install and a first-query 503 immediately after a vendor demo facilitator sees "ready" | Liveness-only check on `api/` alone (rejected by user: exactly the false-green risk the finding identified); partial dependency check (e.g. only vLLM) (rejected by user: every listed service is on some query's hot path, so partial coverage still permits a false-green for the uncovered services) |
| DEC-118 | 2026-07-06 | R6.FixAudit | Architect (per user-approved review) | **D1 "no material issues" confirmation formally logged**: Round 6's D1 pass confirmed hybrid retrieval + rerank + two-layer ACL + observability + eval harness + LangGraph orchestration + layered safety rails match 2026 enterprise RAG best practice with no architectural-soundness defect requiring a fix, distinct from the two D1 findings (DEC-109, DEC-110) that did require action | Recorded per this repository's convention that a reviewed dimension's outcome — including "no issues found" — is logged, not left implicit, so a future reader of `13-decision-log.md` can distinguish "not reviewed" from "reviewed, no defect" for D1's overall architectural-soundness verdict | None — bookkeeping entry, no design alternative was in play |
| DEC-119 | 2026-07-06 | Stage 6→7 | Architect (per user-approved Stage 6 plan) | **`specs/93-stage5r2-benchmark.md` renamed to `specs/92a-stage5r2-benchmark.md`** (git history preserved via `git mv`). Every in-repo reference to the old filename (`00-index.md`, `04-architecture.md`, `13-decision-log.md`, `23-evals-guardrails.md`, `92-stage5-review-memos.md`, `confirmed-context.md`) updated to the new filename | Stage 6 spec-set selection confirmed `00-index.md` as this project's stable slot-00 filename (not the catalog's generic `00-spec-index.md`) and identified that slot 93 is catalog-reserved for the Stage 7 `spec-writer` process's own final `confirmed-context.md` snapshot step (per `spec-writer.md` process step 9). Freeing slot 93 by renaming the Round 2 benchmark memo (which is Group E background evidence, not a numbered catalog deliverable in its own right) avoids a filename collision at Stage 7 | Leave `93-stage5r2-benchmark.md` in place and pick a different slot number for the Stage 7 snapshot (rejected: 93 is the catalog-designated slot for that snapshot per the skill's own process; renaming the non-canonical file is the smaller change) |
| DEC-120 | 2026-07-06 | Stage 7 (final sweep) | Architect (per user-approved Stage 7 plan) | **Two mechanical golden-set-figure corrections found during the final Stage 7 consistency sweep**: `04-architecture.md` §11.1 ("Golden set: 50-question hand-curated CCM-style corpus") and `23-evals-guardrails.md`'s file-header summary line (both predating DEC-078's Round 2 expansion to 150-200 prompts) corrected to reflect the two-ring (150-200 full / 50 smoke) structure already correctly detailed in `23-evals-guardrails.md` §2.2 | The Stage 7 final consistency sweep (grepping all stale "50-question" references across `specs/`) found these two lines had escaped every prior reconciliation pass — Round 1-5's own sweeps, and the Round 6 D2 fix (DEC-111) that corrected the same stale figure in `01-product-brief.md` §9.3 — because the DEC-111 fix and its cross-check were scoped to the specific finding location (`01-product-brief.md`) and did not re-grep the whole repo for the same stale pattern elsewhere | Leave as-is on the theory these are cosmetic/summary lines not load-bearing (rejected: the `04-architecture.md` §11.1 line in particular is under a "## 11. Eval direction" heading a new implementer would read as authoritative, and stating a wrong number there risks the same confusion DEC-111 was written to close) |
| DEC-121 | 2026-07-06 | Stage 8 (post-audit fix, Gate 8) | `spec-writer` (per Stage 8 audit report routing) | **`REQ-005a`/`REQ-005b` orphaned-reference resolved**: `23-evals-guardrails.md` §3.2's Output Guardrails table previously cited `REQ-005a`/`REQ-005b` as if they were independently minted requirement IDs; `02-requirements.md` never defined them. Corrected to `REQ-005(a)`/`REQ-005(b)` with an explicit note that these are shorthand for the two sub-parts already described in prose inside REQ-005's own acceptance criterion ("(a) reference a chunk... and (b) pass an NLI entailment check"), not separate minted IDs | Stage 8 quality audit (2026-07-06) independently re-verified the full REQ/NFR/DEC/RISK/TASK/TEST/VG ID-resolution sweep and found this as the one cosmetic orphan in an otherwise clean result. Rewording the citation (rather than minting new IDs) was chosen because REQ-005's existing acceptance criterion already fully specifies both sub-parts; minting `REQ-005a`/`REQ-005b` as first-class IDs would create redundant, never-independently-referenced entries for content REQ-005 already owns | Mint `REQ-005a`/`REQ-005b` as new IDs in `02-requirements.md` (rejected: no distinct acceptance criterion, test, or traceability need exists for these as separate IDs beyond what REQ-005 already provides — minting would be bureaucratic overhead, not a real gap-fill) |
| DEC-122 | 2026-07-06 | Stage 8 (post-audit fix, Gate 8) | `spec-writer` (per Stage 8 audit report routing) | **Ten orphaned MVP-tagged REQs in `10-build-plan.md` resolved.** Cross-reference-only fixes (functionally already covered, citation was missing): REQ-037 (retention capture at ingest) added to TASK-009's Related Requirements; REQ-006a/REQ-006d (refusal-taxonomy specifics) added to TASK-023's Related Requirements. New tasks added (genuinely unbuilt work): **TASK-033** (HTTP API surface + admin API, REQ-008/REQ-010), **TASK-034** (embeddable widget, REQ-009), **TASK-035** (audit-pull API + ECM audit write-back, REQ-043/REQ-045), **TASK-036** (`SafetyRailAdapter` Protocol — the swappable interface itself, distinct from TASK-020's hardcoded adapter, REQ-050), **TASK-037** (periodic full-reconciliation crawl, REQ-056), **TASK-038** (generation-model adapter abstraction, REQ-033 — V2-tagged, added for Gate-8 traceability completeness even though not required by the MVP-scoped Definition of Done). `10-build-plan.md`'s "Definition of Done" claim ("every REQ-### tagged MVP has at least one TASK-### tracing to it") is re-verified true end-to-end (REQ→TASK→TEST→VG) for all 30 MVP-tagged REQs after this fix | Stage 8 quality audit (2026-07-06) found the "Definition of Done" claim directly falsifiable by grep: several `VG-###`/`TEST-###` entries referenced work no `TASK-###` scheduled the implementation for (most severely REQ-043, which had zero coverage anywhere despite being fully specified in `06-api-contracts.md`). Root cause per the audit's own diagnosis: Phase structure was organized around the hot-path query pipeline and cross-cutting infra, leaving surface-level MVP deliverables (the HTTP API itself, the widget itself, the admin API itself, the audit-pull endpoint) implicitly assumed as "obviously covered" rather than separately scheduled | Leave REQ-033 uncovered on the theory it is V2-tagged and therefore out of the MVP DoD's scope (rejected: TEST-017 already exercises this capability with no task backing it, which is the exact Gate-8 failure pattern regardless of MVP/V2 tagging — recording TASK-038 now prevents the same gap from being rediscovered when V2 work begins) |
| DEC-123 | 2026-07-06 | Stage 8 (post-audit fix, Gate 7) | `spec-writer` (per Stage 8 audit report routing) | **TDD-Exempt template compliance completed across all 12 pre-existing exempt tasks plus the 1 newly-added exempt task (TASK-037).** A full pass (not just the 6 tasks the audit named) found 8 tasks missing at least one mandatory section: TASK-003 and TASK-010 were each missing "Why TDD-Exempt" (not named by the audit's initial 6, found by this fix round's own complete re-pass); TASK-004, TASK-007, TASK-028, TASK-030, TASK-031, TASK-032 were each missing both "Why TDD-Exempt" and "Rollback Plan" (matching the audit's named list). All 8 corrected; a script-verified pass over all 12 exempt tasks (TASK-001, 002, 003, 004, 006, 007, 010, 028, 030, 031, 032, 037) now confirms both mandatory sections present on every one | Stage 8 quality audit (2026-07-06) named 6 tasks as definitely missing at least one section but noted its own count (3 of 12 have "Why", 5 of 12 have "Rollback") implied broader gaps than the 6 named — this round's instruction was explicit not to trust the named list alone. A full automated pass (regex-based section-presence check per task) found 2 additional tasks (TASK-003, TASK-010) each missing "Why TDD-Exempt" that the audit's prose summary had not individually named, though its own aggregate count was consistent with their existence | Patch only the 6 named tasks and assume the rest are fine (rejected: the coordinator's own instruction and the audit's aggregate-count discrepancy both required a full independent re-verification, not a trust-the-summary shortcut) |
| DEC-124 | 2026-07-06 | Stage 8 (post-audit fix, close-out) | `spec-writer` (per Stage 8 audit report routing) | **Post-audit fix round closed out.** Confirms: (a) DEC-121/122/123 above are the complete set of substantive fixes for the Stage 8 audit's Gate 7 and Gate 8 failures; (b) `10-build-plan.md`'s Definition of Done claim is re-verified true via an automated end-to-end sweep (every MVP-tagged REQ in `02-requirements.md` resolves to at least one `TASK-###`'s Related Requirements field); (c) new max ids after this round: TASK-038, DEC-124 (TEST/VG namespaces unchanged — no new TEST/VG ids were needed, since existing TEST-017/020 and VG-016/020/021 etc. already existed and merely needed a backing task, not new test/verification content) | Per this repo's established convention (every round logs its fixes as DEC entries, with a matching `confirmed-context.md` Drift Log row), this closing entry exists so a future reader can find the complete Stage-8-fix-round DEC range (DEC-121..124) from a single anchor rather than needing to infer the round's boundary from timestamps alone | None — bookkeeping entry, no design alternative was in play |
| DEC-125 | 2026-07-06 | Stage 8 (re-audit fix, Gate 7) | `spec-writer` (per independent re-auditor's findings, relayed via coordinator) | **Duplicate/misplaced content bug fixed in `10-build-plan.md`**: TASK-037's own "Acceptance Criteria" + "Verification Evidence" pair (covering the reconciliation crawl's orphan-document detection, REQ-056/TEST-020) had been appended after TASK-038's block instead of inside TASK-037's own block, leaving TASK-037 without its own evidence-bearing acceptance criteria (a real Gate 7 gap for a TDD-exempt task) and TASK-038 with a trailing block of content that belonged to a different task. Corrected: the misplaced pair moved to immediately follow TASK-037's "Rollback Plan" section; TASK-038 now ends cleanly at its own "Verification Evidence" section with no duplication | Independent re-audit of Gates 7+8 (post the DEC-121..124 fix round) found this copy-paste displacement, most likely introduced when TASK-037 and TASK-038 were drafted back-to-back in the same editing pass during that round. A full automated duplicate-section scan across every task in `10-build-plan.md` (not just TASK-037/038) confirmed no other task has more than one "Acceptance Criteria" or "Verification Evidence" section | Leave the content where it was and merely add new sections to TASK-037 (rejected: this would duplicate the reconciliation-crawl acceptance criteria rather than fixing the actual placement error, and would leave TASK-038 carrying content that isn't about it) |
| DEC-126 | 2026-07-06 | Stage 8 (re-audit fix, Gate 8) | `spec-writer` (per independent re-auditor's findings, relayed via coordinator) | **Three remaining REQ→TASK→TEST→VG chain gaps closed.** (a) REQ-050 (`SafetyRailAdapter` Protocol, TASK-036): `12-verification.md`'s pre-existing VG-020 referenced a generic "adapter-swap integration test" with no backing `TEST-###`; added **TEST-036** in `11-test-plan.md` and corrected VG-020 to cite it by ID. (b) REQ-037 (retention-state capture at ingest, TASK-009): had zero `TEST-###`/`VG-###` coverage; added **TEST-035** and **VG-031**. (c) REQ-043 (audit-pull NDJSON endpoint, TASK-035): had zero `TEST-###`/`VG-###` coverage despite being fully specified in `06-api-contracts.md` API-A-04 and TASK-035's own inline test description; added **TEST-037** and **VG-032**. All three REQ→TASK→TEST→VG chains independently re-traced end-to-end (opening each of `02-requirements.md`, `10-build-plan.md`, `11-test-plan.md`, `12-verification.md` directly, not just counting rows) and confirmed closed | Independent re-audit of Gates 7+8 found that the DEC-122 fix round (which added TASK-033..038 to close the ten-orphaned-REQ Gate 8 finding) had not been mirrored forward into `11-test-plan.md`/`12-verification.md` for these three specific requirements — the new tasks existed but the test/verification layer for them didn't, which is the same "verification references work the build layer doesn't produce" pattern in reverse (build layer existing with no verification layer to close the loop) | Treat VG-020's existing generic test description as sufficient without a formal TEST-### (rejected: every other VG row in the file cites a specific TEST-### or an equivalent concrete evidence source; leaving REQ-050 as the sole exception would reintroduce the inconsistency this fix round exists to close) |
| DEC-127 | 2026-07-08 | Post-Stage-8 (Gate 7 batch fix) | `spec-writer` (per independent Gate 7 re-audit, relayed via coordinator) | **"TDD Refactor" subsection added to all 14 strict-TDD tasks in `10-build-plan.md` that were missing it: TASK-012, TASK-014, TASK-015, TASK-016, TASK-017, TASK-019, TASK-021, TASK-022, TASK-023, TASK-024, TASK-025, TASK-026, TASK-027, TASK-029.** A programmatic sweep (parsing every task with "Verification Pattern: TDD" and confirming it has Red/Green/Refactor/Acceptance Criteria/Verification Evidence, per `quality-gates.md`'s Gate 7 requirement) had been reported as finding 13 tasks missing "TDD Refactor," with TASK-027 separately reported as already fixed in a prior round. Opening `10-build-plan.md` directly to apply the fix found that claim false: TASK-027 was **not** already fixed — it had the same Red/Green/Acceptance-Criteria/Verification-Evidence-but-no-Refactor shape as the other 13. No prior commit, branch, or decision-log entry (max prior ID was DEC-126) supports the claimed prior fix or a DEC-127 having already existed. TASK-027 is folded into this batch rather than left inconsistent with a false paper trail. Each of the 14 additions is a genuine, task-specific refactor opportunity (e.g. TASK-012/TASK-013 sharing one ACL-filter-clause translation instead of two; TASK-015/TASK-016 sharing one event-routing function across webhook and poll transports; TASK-023's `refusal_decision()` kept as the single typed-refusal source other rails call into) — not placeholder "clean up code" lines. Post-fix, a full re-parse of `10-build-plan.md` confirms all 26 strict-TDD tasks have exactly one TDD Refactor section each, 0 gaps **(reconciliation note, added during merge with the concurrent DEC-128 observability round below): this round ran in an isolated git worktree with no visibility into DEC-128's session, which had already added TASK-027's Refactor section as uncommitted, not-yet-pushed work in the main working tree at the time this round started — hence this round's accurate-from-its-own-vantage-point finding that "no prior commit... supports the claimed prior fix." Both rounds' TASK-027 Refactor content were merged into one combined section covering both the generic span-lifecycle wrapper (this round) and the domain-specific attribute-collection helper (DEC-128) — see `10-build-plan.md` TASK-027)** | An independent Gate 7 re-audit surfaced this defect and reported it as a 13-task gap with TASK-027 already resolved; verifying that claim against the actual file (rather than trusting the count) found TASK-027 still broken and no supporting history for the claimed prior fix or for DEC-127 already existing. Per this repo's own established discipline (DEC-123's "don't trust the named list alone," DEC-125/126's independent re-tracing rather than counting rows), the fix proceeds against the file's actual state — 14 tasks, not 13 — with the discrepancy recorded here rather than silently absorbed into a narrative that didn't happen | Fix only the 13 named tasks and separately flag TASK-027 for a future round (rejected: TASK-027's Gate 7 gap is identical in kind to the other 13 and already identified — deferring a known, already-diagnosed defect for a second pass would be pure process overhead); take the claimed prior TASK-027 fix and DEC-127 existence at face value without checking (rejected: directly contradicted by the file and the decision log, and this repo's convention is independent verification over trusting a summary) |
| DEC-128 | 2026-07-08 | Post-Stage-8 maintenance (`update-specs` skill) | User (design-gap finding) + Architect (resolution) | **Observability trace enrichment bundle, closing the gap between "diagnose latency" and "diagnose accuracy" via traces.** Four sub-decisions: **(a) Domain-specific span attributes** — `08-observability-logs.md`'s Span Structure gains the attributes now specified in NFR-033 (candidate counts, rerank score deltas, per-claim NLI scores, mechanical/nli_slow_path verdict, and five version identifiers already in `audit_events.context_fingerprint` but previously absent from traces). **(b) NLI-score histogram metric added** (`nli_entailment_score`, alongside the existing `citation_hit_rate` gauge) so `23-evals-guardrails.md` §7's threshold-tuning runbook can see the score *distribution* behind a pass/fail rate, not just the rate itself. **(c) Trace sampling reconciled, not newly invented**: `23-evals-guardrails.md` §2.2 already stated "5-10% of production queries are sampled into the trace store," which read as if `otel_spans` persistence itself were sampled — this would break `08-observability-logs.md`'s Incident Investigation examples, which assume any query's trace is pullable by `request_id`. Resolved: MVP persists 100% of query traces into `otel_spans` (matches the low sustained-concurrency assumption, DEC-066/NFR-005); the pre-existing 5-10% figure is re-scoped to mean "of already-persisted traces, this fraction is pulled into weekly human quality review" — a downstream review sample, not a persistence-time sample. Re-evaluation trigger: if sustained concurrency exceeds the DEC-066 cold-cache floor / NFR-005 warm-cache range for a sustained period, revisit whether 100% persistence remains viable. **(d) Trace-to-regression-test path, MVP-scoped**: `23-evals-guardrails.md` §2.2 already promotes human-reviewed traces into the golden set, but only via the V2-scoped customer-specific set (REQ-023). Added an MVP-scoped manual path reusing existing infrastructure: §6's "Quarterly LCC review" row (sample 50 production answers) now also directs that a sampled answer revealing a genuine golden-set gap gets added as a new smoke/full-ring prompt, tagged with its source `request_id` for provenance — no new build task, no new V2 dependency. **Fifth item, added at merge time**: DEC-128 originally minted the id "DEC-127" for this bundle before discovering, at merge time, that the concurrently-running Gate 7 batch-fix round (an `update-specs`-spawned background task, isolated in a separate git worktree) had independently and legitimately minted the same id first for a different decision. Renumbered to DEC-128 and reconciled against that round's row above — see `confirmed-context.md`'s 2026-07-08 Drift Log rows for the full reconciliation note; recorded here as a concrete example of why `update-specs`'s Step 1 (map blast radius) and Step 2 (mint new IDs) must be re-run immediately before commit, not just at the start of a session, when work spans multiple concurrent sessions | User identified (with supporting evidence from this project's own `92a-stage5r2-benchmark.md` Topic 6 research) that the current span structure's shape (node tree) was correct but its per-node attributes only supported latency diagnosis, not accuracy diagnosis — an operator seeing a quality regression currently has no way to localize it without manually opening `audit_events` for every affected query. Grepping the underlying entity (`otel_spans`) rather than just the requirement ID (NFR-026) during blast-radius mapping (`update-specs` skill Step 1) additionally surfaced the sampling-wording ambiguity in (c), which was not part of the original report but is a real latent inconsistency between two files' implicit assumptions about the same data store | For (a): a dedicated LLM-as-judge trace annotator instead of raw NLI scores on the span (rejected: NLI already runs synchronously in `verify/`, so surfacing its existing output is free; a separate judge pass would be new latency and cost for data already computed). For (c): leaving the 5-10% wording as literal persistence-time sampling (rejected: would silently break the Incident Investigation runbook's core promise of "any query's trace is pullable"). For (d): building V2's customer-specific golden-set promotion tooling early to also cover MVP (rejected: REQ-023 is explicitly V2-scoped and building its admin-UI/tooling early is scope creep; the quarterly-review-row extension achieves the same outcome with a manual step, not new infrastructure) |
| DEC-129 | 2026-07-08 | Post-Stage-8 maintenance (`update-specs` skill) | User (design-gap finding) + Architect (resolution) | **NFR→TASK traceability sweep: 6 gaps closed.** Every prior Gate 8 audit pass (DEC-121..126) checked REQ→TASK coverage but never checked NFR→TASK coverage — this round is the first to do so, independently re-verified twice. Three were **traceability-only gaps** (capability already built and tested, the NFR id itself never cross-cited): **NFR-021** (layered safety rails) is a near-duplicate of REQ-048, already fully implemented by TASK-018/019/020/021 — added NFR-021 to each task's Related Requirements. **NFR-020** (AU/NZ data-residency, "reviewer can trace every model/config/external call") is substantially satisfied in practice by TASK-030's air-gap verification (zero outbound calls under a blocked network namespace) — added NFR-020 to TASK-030's Related Requirements. **NFR-003** (English-only schema neutrality) — its "golden set passes" half is covered by TASK-029; its "schema review confirms no hardcoded English-only field names" half had no task — added NFR-003 to TASK-029's Related Requirements and a new acceptance-criteria bullet to TASK-007 (Qdrant `chunks` payload is the most language-sensitive schema surface). Three were **real functional gaps** (zero task, test, or verification-gate coverage at all): **NFR-024** (cost-per-turn) — the ceiling (¥0.10/turn) had existed since Stage 4 (RC-R2-T8-04) but no computation methodology was ever defined; this round defines one (GPU-hour-amortized rate × generation wall-clock duration, grounded in DEC-068's already-established ~$0.45/h RunPod-4090-spot reference rate and NFR-026's already-captured `gen_ai.request.duration`), documents it in `08-observability-logs.md`'s Metrics section, and extends TASK-027 (metric emission) + TASK-029 (eval-harness reporting, per NFR-024's own acceptance seed) to implement it — new **TEST-039**, extends VG-013. **NFR-028** (mandatory `--enable-chunked-prefill`, conditional `--speculative-decoding`) — the flag values and enablement condition were already fully specified in NFR-028's own text, just never wired into a task; extended TASK-014 (`generate/`'s vLLM call, the natural implementation site) — new **TEST-040**, new **VG-035**. **NFR-007** (durable state survives container restart) — the backup/restore mechanics already existed in `09-deployment-ops.md`, but no task verified the restart-survival property itself, distinct from TASK-003's dev-rig model-cache-only check and TASK-010's ingest-job-resume check; added new **TASK-039** (Phase 6, TDD-Exempt, alongside TASK-030/031/032's "verify the whole system does X" pattern) — new **VG-036**. `specs/14-spec-audit-report.md`'s "Required Fixes" section, which claimed "None outstanding," is corrected to record these 6 as a fourth audit pass rather than leaving a stale completeness claim standing | A user-requested re-audit of `14-spec-audit-report.md` and the codebase (independent of any specific spec change) surfaced that Gate 8's REQ-only scope had never been extended to NFR-###, unlike the parallel, more thorough Gate-7-vs-TDD-Refactor sweep. Re-verified twice across two separate sessions with identical results before treating the 6 gaps as confirmed rather than provisional | For NFR-024: leaving the cost ceiling undefined-by-formula indefinitely (rejected: `08-observability-logs.md`'s own `cost_per_turn` metric and alert cannot be implemented without one, and Gate 4's "testable acceptance criteria" standard requires it); inventing a novel cost-rate figure from scratch (rejected: DEC-068 already established a defensible, cited reference rate for the same reference-hardware tier — reusing it avoids introducing an unsourced number). For the 3 real gaps generally: bundling all 3 into one new mega-task (rejected: NFR-024/028/007 are unrelated concerns in unrelated phases/modules; forcing them into one task would violate this build plan's own vertical-slice-per-task discipline) |
| DEC-130 | 2026-07-08 | Post-Stage-8 maintenance (`update-specs` skill) | User (design-gap finding) + Architect (resolution) | **RAGAS judge model specified: `Qwen2.5-14B-Instruct` int4 (GGUF), CPU inference via `llama.cpp`, invoked only by the eval runner (`cli eval run`) — not a persistent docker-compose service.** Closes a gap where `92a-stage5r2-benchmark.md` §Topic 5 named "LLM-as-judge with a *distinct* judge model" as a `high`-confidence non-negotiable practice, but no spec file ever named which model scores RAGAS's faithfulness/answer_relevancy metrics — `23-evals-guardrails.md` §2.1's threshold table lists thresholds, not a judge; `04-architecture.md` §9.1's MVP docker-compose service list has no judge/eval model container. Qwen (Alibaba) is a different vendor/family from both MVP generation-model candidates (`Llama-3.1-8B-Instruct`/Meta, `Mistral-Small-24B-Instruct`/Mistral AI, per DEC-052/DEC-079) — the same distinct-family bar the NLI/generation split already meets (DEC-037), applied here to the separate RAGAS-judge mechanism the benchmark actually calls non-negotiable. CPU inference (not vLLM/GPU-resident) is deliberate: the eval runner is offline/manual-triggered (weekly regression, pre-demo gate, CI, Stage 8 audit acceptance — `23-evals-guardrails.md` §2.2/§2.3), never on the query hot path, so it carries no query-latency SLO and does not need to compete for §4.2.2's GPU-resident budget, which is already down to ~1.7 GB warm-cache headroom at the 24 GB floor tier. A GPU-resident 14B-class int4 judge would need roughly 8-9 GB VRAM — more than the entire remaining headroom — forcing either a floor-tier hardware bump or evicting an online-serving component during eval runs, neither acceptable for a background batch job. **Also corrects `92-stage5-review-memos.md` R2-T6-02**, which had marked "LLM-as-judge with distinct judge model" `[CONFIRMED]` on the strength of the NLI verifier's decoupling (DEC-037) — a different mechanism from the RAGAS LLM-as-judge that 92a Topic 5 actually discusses. Split into R2-T6-02a (NLI decoupling — remains CONFIRMED, DEC-037) and R2-T6-02b (RAGAS judge decoupling — was the true, previously-unflagged gap; now resolved by this DEC). Also corrects `14-spec-audit-report.md` Gate 6's evidence for "judge-model decoupling," which cited the same NLI-only justification | 92a Topic 5 (`high` confidence) explicitly distinguishes mature teams by using a distinct-family LLM-as-judge for RAGAS scoring, not merely a decoupled grounding verifier — conflating the two was a real Round 2 review-memo error, not a stylistic quibble, because it caused a genuine MUST-ALIGN gap to read as already closed. NFR-002 (air-gap, no required outbound calls) rules out a hosted GPT-4-class judge, the common non-air-gapped default — an explicit local design was required, or an implementation team would likely default to same-model self-evaluation (the generation model judging its own output), a known self-evaluation-bias anti-pattern that would inflate DEC-017's faithfulness/answer_relevancy pass rates without the underlying grounding actually improving, undermining the "verified citations / audit-ready" differentiator this product is positioned on. Qwen reuses a model family already vetted in this project's own Stage 1 research (DEC-013's original Qwen3 generation-model pick, superseded for the *generation* role only by DEC-052's English-only pivot) rather than introducing an unresearched new vendor purely for this purpose | Same-family self-judge, e.g. `Llama-3.1-8B-Instruct` judging its own output (rejected: self-evaluation bias is the exact anti-pattern the benchmark's non-negotiable exists to prevent); hosted GPT-4-class judge (rejected: violates NFR-002 air-gap); GPU-resident judge sharing the floor-tier GPU (rejected: §4.2.2's warm-cache headroom is already ~1.7 GB, far short of a 14B-class int4 model's ~8-9 GB footprint, and evicting an online-serving component to make room during eval runs is a worse tradeoff than accepting CPU inference latency for a non-latency-sensitive batch job); smaller judge model, e.g. `Qwen2.5-7B-Instruct` (rejected: judge accuracy/human-correlation matters more than judge speed here, since eval runs are already batch/offline with no latency requirement to trade against) |
| DEC-131 | 2026-07-08 | Post-Stage-8 maintenance (`update-specs` skill) | User (design-gap finding) + Architect (resolution) | **Corrects a stale version claim: LangGraph's pinned version updated from "0.2.x" to "1.2.x"** across `02-requirements.md` (REQ-046), `01-product-brief.md`, `04-architecture.md` (all tech-stack/module-map/pipeline citations), `10-build-plan.md` (TASK-002's dependency pin, TASK-011's Green step), `20-agent-behavior.md`, and the published Phase 1 issue (`.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md`) that cites the same pin. **DEC-075's and DEC-082's own rows above are left unedited** — append-only, they accurately record what was decided and reasoned on 2026-06-29 — this entry corrects only the living spec files' *current-state* assertions, which were already wrong the day DEC-075 was written. Verified directly (not re-derived from training data): `gh api repos/langchain-ai/langgraph/releases` shows `1.2.5` published 2026-06-12 — seventeen days *before* DEC-075's 2026-06-29 date — and `1.2.8` published 2026-07-06. LangGraph's official reference docs (`reference.langchain.com/python/langgraph`) additionally show deprecation classes for v0.5/v1.0/v1.1, confirming a real 0.x→1.x transition occurred. The choice of LangGraph as the orchestration framework, and DEC-082's "typed execution graph; runtime is a swappable detail" framing — established specifically so the architecture survives exactly this kind of version drift — are unaffected; this entry corrects only the version number those documents assert as current, not the architectural reasoning | User cross-checked `92a-stage5r2-benchmark.md`'s "2026 senior-architect default" framing for LangGraph against LangGraph's official docs and GitHub release history directly, rather than trusting the spec's own claim. The Stage 5 research reflected the researching agent's training-data knowledge of LangGraph's version state rather than a live check, and that staleness existed at decision time (DEC-075, 2026-06-29), not merely from time passing since — the most-recent pre-DEC-075 release (1.2.5) had already shipped 17 days earlier. Scope of this fix is deliberately narrow, per explicit user decision: correct the version number only. Whether the 0.x→1.x API changes (deprecation classes exist for v0.5/v1.0/v1.1) affect the specific typed-state/feedback-edge usage `04-architecture.md` describes is **not** verified by this entry — flagged as a separate, not-yet-done follow-up rather than silently assumed fine | Re-verify the full `92a-stage5r2-benchmark.md` research pass for other version-sensitive claims now that one has been found stale (deferred — user explicitly scoped this round to the version number only; a broader benchmark re-audit is a legitimate follow-up but separate work, not folded into this entry); rewrite DEC-075/DEC-082's original rows to reflect the corrected version (rejected: violates this log's append-only convention — those rows accurately record what was decided and reasoned on 2026-06-29, and rewriting them would erase the fact that the original research was already stale the day it was used); leave the 0.x→1.x API-compatibility question unresolved without flagging it (rejected: silently leaving it implicit risks a future reader mistaking "version number fixed" for "API compatibility confirmed") |
| DEC-132 | 2026-07-08 | Post-Stage-8 maintenance (`update-specs` skill) | User (called it) + Architect (verified) | **Closes DEC-131's flagged follow-up: LangGraph's 0.x→1.x transition does not affect `04-architecture.md`'s typed-state/feedback-edge design.** Searched LangGraph's GitHub PR/issue history directly for breaking-change reports around and since the 1.0 release (published 2025-10-17): every hit found clusters in peripheral packages (`langgraph-prebuilt`, `langgraph-checkpoint-postgres`) or narrow behaviors (`aiosqlite` upstream break, `stream_mode='values'` interrupt semantics) — none touch `StateGraph`, `add_node`, `add_edge`, `add_conditional_edges`, typed state schemas (TypedDict/Pydantic), or cyclic/feedback edges, the exact primitives DEC-075/DEC-082's design relies on. A pre-1.0 PR ("add backwards compat utils imports to make v0.6 migration easier... so things don't fully break") indicates a deliberate stabilization effort rather than a core redesign. No spec content changes as a result — this entry records the verification, not a design change | User's own read ("我觉得不影响") was checked rather than accepted at face value, per this project's consistent practice of independent verification over trusting a summary (DEC-123, DEC-125/126, DEC-129's own self-correction). Caveat carried forward honestly: no single authoritative "LangGraph v1 migration guide" document was located (two guessed URLs 404'd / redirected to an unrelated LangSmith changelog) — this is strong evidence from direct PR/issue search, not confirmation from an official migration document, and is reported as such rather than overstated | Treat the user's assessment as sufficient without independent verification (rejected: this project's own established discipline is to verify claims directly rather than trust them, including the user's own judgment calls — the same standard applied to DEC-075's original research); keep searching until an official migration guide is found (rejected: diminishing returns — the PR/issue-history evidence is already strong and directly on-point for the specific primitives in question) |
| DEC-133 | 2026-07-08 | Post-Stage-8 maintenance (`update-specs` skill, general re-audit) | User (requested general re-audit) + Architect (found + fixed) | **Closes a coverage gap: `llama-cpp-python` — the runtime DEC-130 introduced for the RAGAS judge model — was never added to `TASK-002`'s dependency-pinning list.** DEC-130 (2026-07-08) specified the RAGAS judge model runs via `llama.cpp` CPU inference; `TASK-029`'s Green step (`10-build-plan.md`) correctly references this, but `TASK-002` ("Dependency Verification + Install," Phase 1 — the task that pins and verifies every core dependency) was never updated to include the package, and `TASK-029` itself had no acceptance criterion for the judge model's GGUF weights actually being present/loadable. Fixed: `llama-cpp-python` added to `TASK-002`'s pin list and to `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` (not yet implemented, updated in place); a new `TASK-029` acceptance criterion + verification-evidence line added for judge-model weight presence, failing fast rather than silently falling back to a different model if the weights are missing (which would quietly reintroduce the self-evaluation-bias anti-pattern DEC-130 exists to prevent). No new `REQ`/`NFR`/`TEST`/`VG` id needed — existing `TASK-029` acceptance-criteria/verification-evidence structure absorbed the addition, same pattern DEC-129 used for its traceability-only NFR gaps | Found via a user-requested general re-audit (not tied to any specific known change), modeled on DEC-129's NFR→TASK sweep: checked whether any other recently-added DEC (127-132) introduced a capability without matching build-plan coverage, the same failure shape the LangGraph version gap (DEC-131/132) surfaced. `llama.cpp` appears in `04-architecture.md`'s docker-compose comment and `TASK-029`'s Green step but nowhere in `TASK-002`'s pin list — grepping the underlying entity (`llama.cpp`/`llama_cpp`) rather than just `DEC-130`'s id is what surfaced this, per this skill's own Step 1 discipline | Add the pin only to `TASK-029` since that's the only consumer (rejected: `TASK-002` is this build plan's single owner of "pin and verify every core dependency," regardless of which phase first consumes a given package — vLLM/TEI/Qdrant client are pinned there too, well ahead of their own later-phase use; splitting dependency-pinning responsibility across tasks by first-use phase would fragment the one place a contributor should look to know the full pinned set); leave the GGU-weight-presence check unstated and assume it's implied by "judge model is Qwen2.5-14B-Instruct" (rejected: an assumed-present model weight is exactly the kind of unverifiable acceptance criterion this project's `verifiable-acceptance-criteria` skill exists to catch — an explicit fail-fast check costs one bullet point and prevents a silent wrong-model fallback) |
