# Dev Environment Capabilities

> Generated by `/dev-environment-capabilities`. This is ground truth for the `verifiable-acceptance-criteria` gate — every row must be a directly-observed probe result or a user-confirmed answer, never a guess. Re-probe rather than trust a stale date.

Last probed: YYYY-MM-DD

## Local tooling

| Capability | Probe command | Result | Notes |
|---|---|---|---|
| Container runtime (Docker) | `docker version` | not tested / available / unavailable | |
| Docker Compose | `docker compose version` | not tested / available / unavailable | |
| GPU | `nvidia-smi` | not tested / available / unavailable | |

## Cloud / infra access

| Capability | How confirmed | Result | Notes |
|---|---|---|---|
| e.g. RunPod API access | user-confirmed / probed | yes / no / unknown | Credentials configured for *this agent's* use, not just existing elsewhere |
| e.g. Terraform / IaC CLI | `terraform version` | not tested / available / unavailable | |

## CI/CD access

| Capability | How confirmed | Result | Notes |
|---|---|---|---|
| CI provider connected to this repo | `.github/workflows/` presence + `gh workflow list` | not tested / connected / not connected | |
| Agent can query live run status | `gh run list` | not tested / yes / no | |

## Reference docs (fast-moving / version-sensitive libraries)

| Library | Pinned version | Official docs URL | Why it's listed here |
|---|---|---|---|
| | | | e.g. pre-1.0, frequent breaking changes, training-data recall risk |

## Human-verification-only items on record

List anything the `verifiable-acceptance-criteria` gate has split off as `[manual-verify]` across active issues, so this file also serves as a standing reminder of what no agent can currently close alone.

| Item | Owner | Evidence required | Source issue |
|---|---|---|---|
