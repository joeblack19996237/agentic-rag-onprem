# Dev Environment Capabilities

> Generated by `/dev-environment-capabilities`. This is ground truth for the `verifiable-acceptance-criteria` gate — every row must be a directly-observed probe result or a user-confirmed answer, never a guess. Re-probe rather than trust a stale date.

Last probed: 2026-07-11 (Docker/GPU/toolchain rows re-confirmed unchanged 2026-07-14 while gating TASK-006/007's draft acceptance criteria; Alembic/qdrant-client rows in the Reference docs table below are new findings from that same session)

The no-Docker/no-GPU/no-C++-toolchain ceiling recorded below drives `specs/13-decision-log.md` DEC-135's Phase 2+ verification-tiering strategy (fake-backed tests as the agent-executable default; live-service checks are `[manual-verify]`). Re-check both this file and DEC-135 together if this environment's capabilities change. **Qdrant is a partial exception (DEC-140, corrected by DEC-141)**: `qdrant-client`'s embedded local mode runs the real client library in-process with no Docker needed for vector-config and filter-semantics checks — but not for payload-index existence, which the local client silently no-ops. See its row in the Reference docs table below.

## Local tooling

| Capability | Probe command | Result | Notes |
|---|---|---|---|
| Container runtime (Docker) | `docker version` | unavailable | `docker: command not found` |
| Docker Compose | `docker compose version` | unavailable | Same binary as above; not installed |
| GPU | `nvidia-smi` | unavailable | `nvidia-smi: command not found` — no CUDA-capable host in this environment |
| C/C++ build toolchain (cmake) | `cmake --version` | unavailable | `cmake: command not found` |
| C/C++ compiler (MSVC / gcc) | `cl.exe` on PATH, `vswhere.exe`, `gcc --version` | unavailable | No `cl.exe`, no Visual Studio Installer (`vswhere.exe` not found), no `gcc`. Blocks any package that ships only an sdist (e.g. `llama-cpp-python` — see manual-verify table below) |
| Python | `python --version` | available, matches pin | Installed: 3.14.3. `specs/13-decision-log.md` DEC-134 (2026-07-11) re-pins the project target to **3.14**, superseding DEC-033's original 3.12 (a deliberate re-decision matching this environment, not a correction of a stale claim). No 3.12 runtime is installed (`py -3.12 --version` still fails) but that no longer matters — the spec now targets what's actually here |

## Cloud / infra access

| Capability | How confirmed | Result | Notes |
|---|---|---|---|
| RunPod CLI (`runpodctl`) | `runpodctl --version` | unavailable | Referenced by `specs/10-build-plan.md` TASK-003 (Dev Rig Verification); not installed here |
| Terraform / IaC CLI | `terraform version` | unavailable | Not currently named as required by any spec'd task, probed anyway for completeness |
| `aws` / `gcloud` CLIs | `aws --version` / `gcloud --version` | unavailable | Product is on-prem/no-cloud-egress by design (`specs/01-product-brief.md`); not expected to be needed, probed for completeness |

## CI/CD access

| Capability | How confirmed | Result | Notes |
|---|---|---|---|
| CI provider connected to this repo | `git remote -v` + `gh run list --branch master --limit 3` | **connected** (updated 2026-07-11, was "not connected" earlier the same day) | `origin` is `https://github.com/joeblack19996237/agentic-rag-onprem` (public). User connected it and pushed after Issue 02 landed. `.github/workflows/ci.yml` exists and Actions is enabled. |
| Agent can query live run status | `gh run list --branch master --limit 3` | **yes** | `gh` CLI (v2.88.1), authenticated as `joeblack19996237`. Confirmed run `29143498710` (Issue 02's commit): `completed` / `success`. Pushing itself still needs per-instance user confirmation (this session's operating convention, not a tool/credential limit) — but once pushed, querying the result is fully agent-drivable, e.g. `gh run list --branch master --limit 1` / `gh run view <id> --log`. |

## Reference docs (fast-moving / version-sensitive libraries)

| Library | Pinned version | Official docs URL | Why it's listed here |
|---|---|---|---|
| LangGraph | 1.2.9 (DEC-131 corrected the stale "0.2.x" pin) | `https://reference.langchain.com/python/langgraph` | Crossed a 0.x→1.x major boundary in 2026; DEC-131/132 already found and fixed one stale-version claim in this repo's own specs — training-data recall is not trustworthy here |
| vLLM | 0.24.0 (verified live via PyPI JSON API, 2026-07-11, as part of DEC-134's research — not yet formally pinned in a requirements file since it's GPU-only and can't be installed/smoke-tested in this environment) | `https://docs.vllm.ai/` | Fast release cadence; DEC-083 pins specific serving flags (`--enable-chunked-prefill`, conditional `--speculative-decoding`) that are version-sensitive. GPU-only — can't be smoke-tested in this environment (see manual-verify table) |
| TEI (text-embeddings-inference) | Unpinned exact version as of this writing (DEC-035) | `https://huggingface.co/docs/text-embeddings-inference` | HuggingFace-maintained, evolving; no dedicated Python client package on PyPI — calls go over its REST API (assumed via `httpx`, flagged in `requirements.txt`) |
| qdrant-client | 1.18.0 (verified live via PyPI JSON API, 2026-07-11) | `https://python-client.qdrant.tech/` | Verified live rather than from memory, per this repo's dependency-version discipline. **Also ships an embedded local mode** (`QdrantClient(":memory:")` or `path=...`) that runs a real, in-process implementation with no server/Docker needed (DEC-140, 2026-07-14) — but narrower than first recorded: **confirmed by actually running it** (not just the GitHub repo/Discussion #683 summary) that collection vector config (dense+sparse) and Layer 1 filter/query semantics work correctly in local mode, while `create_payload_index()` silently no-ops (real `UserWarning`, empty `payload_schema` on introspection) — payload-index *existence* checks still need a real server (DEC-141, same day). Production-scale HNSW-recall/performance also still needs a real server regardless |
| psycopg | 3.3.4 (verified live via PyPI JSON API, 2026-07-11) | `https://www.psycopg.org/psycopg3/docs/` | Major-version-3 rewrite of psycopg2; API differs meaningfully from the older `psycopg2` most training data assumes |
| Alembic | 1.18.5 + SQLAlchemy 2.0.51 (both confirmed live-installable, `cp314` wheels exist; not yet pinned in `requirements.txt` — TASK-006's own job) | `https://alembic.sqlalchemy.org/` | **Empirically confirmed twice independently (main session + a fresh re-check agent), not just read from docs**: `alembic upgrade head --sql` / `alembic downgrade <rev>:base --sql` (offline mode, dialect-targeted DDL rendering) work with **zero live DB connection** — proven against a hand-written migration containing a FK, a CHECK constraint, a partial unique index, and a raw `REVOKE` statement, all rendered correctly. But `alembic revision --autogenerate` **hard-requires a live connection** (hangs or throws `OperationalError` against an unreachable URL) — it cannot run in this environment at all. Implication for any TASK-006-adjacent work: migrations must be hand-written using Alembic's `op.*` DSL, not machine-generated here; `--sql` offline mode is the Tier-2 verification mechanism, actually running `upgrade`/`--autogenerate` against a live Postgres stays `[manual-verify]`. Note: offline `downgrade` needs the explicit `<fromrev>:<torev>` form — bare `alembic downgrade base --sql` fails |
| redis (redis-py) | 8.0.1 (verified live via PyPI JSON API, 2026-07-11) | `https://redis.readthedocs.io/` | Recent major-version jump (from the 4.x/5.x line); dropped Python 2 support and shifted some client defaults |
| llama-cpp-python | 0.3.33 (verified live via PyPI JSON API, 2026-07-11) | `https://llama-cpp-python.readthedocs.io/` | Ships only an sdist on PyPI as of this version — no prebuilt wheel; tightly coupled to whatever `llama.cpp` commit it vendors |

Doc URLs above other than LangGraph's were not individually re-verified via a live fetch this session — they are the well-established canonical doc domains for each project, not training-data guesses about API behavior. Re-verify via WebFetch before relying on a specific API claim from any of them, per `implement`'s dependency-version discipline.

## Human-verification-only items on record

| Item | Owner | Evidence required | Source issue |
|---|---|---|---|
| `docker compose config` validates the full compose file against a live Docker daemon | DevOps (or whoever has Docker available) | Command output showing resolved config, or the error if it fails | `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` |
| vLLM installs and imports cleanly | DevOps/AI (needs a CUDA-capable host) | Install log + `python -c "import vllm"` output from that host | `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` |
| `llama-cpp-python==0.3.33` (or the then-current pin) installs and imports cleanly | DevOps/Backend (needs a host with a C/C++ build toolchain) | Install log + `python -c "import llama_cpp"` output from that host | `.scratch/phase-1-bootstrap/issues/01-project-scaffold-dependencies.md` |
