# Testing Guide

> How to write a test in this repo, what to avoid, and a live inventory of every test that actually exists (as of 2026-07-16, 22 test modules / 28 files, 189 test functions). This is a *practice* doc — for the spec-level catalog of every test planned across all build phases (`TEST-001`..`TEST-040`), see `specs/11-test-plan.md` instead; this file only documents what's implemented today. Keep the inventory table below in sync whenever a test is added, removed, or changes what it covers — it's the whole point of this file.

## How to write a test here

The test modules that exist today (see inventory below) all follow one of two shapes. Match one of them rather than inventing a new one:

1. **Check-style tests** (a rule you assert holds across the repo, not a single component's behavior) get an implementation module plus a test module, both under `tests/<domain>/` — e.g. `tests/architecture/import_graph.py` + `test_import_graph.py`, `tests/docs/doc_drift.py` + `test_doc_drift.py`. The implementation module exposes plain functions that return a list of human-readable violation strings (`[]` means clean). The test module imports it bare (`from doc_drift import ...`) — pytest's default import mode puts the test file's own directory on `sys.path` when there's no `__init__.py` there, so this resolves without packaging tricks. **Do not add `__init__.py` to `tests/<domain>/` directories** — it would break this.
2. **Component tests** (a single piece of real application code, like a FastAPI route) test it directly against its real module — no separate implementation module needed. `tests/api/test_ready.py` imports `api.main` directly (`from api.main import ServiceHealth, app`); `tests/ingest/test_qdrant_setup.py` imports `ingest.qdrant_setup` directly and runs it against a real `QdrantClient(":memory:")`. `tests/db/test_migration.py` is the same idea one level out — the "component" under test is a CLI tool's real output (`alembic upgrade head --sql` via `subprocess`), since Alembic migrations aren't invoked through a Python import at all in normal use.
3. Every check-style test module includes one test that runs the check against the real repo (`REPO_ROOT = Path(__file__).resolve().parents[2]`) and asserts zero violations. This is the test that actually gates CI — the synthetic cases exist to prove the check works, not to replace this one.
4. Cover both directions for any check: a synthetic case that should stay clean (no false positives) and one that should trip it (no false negatives). See `test_doc_drift.py`'s `test_ignores_dec_ids_that_do_resolve` paired with `test_detects_a_dangling_dec_reference`.
5. Use pytest's `tmp_path` fixture for anything that needs its own throwaway directory tree. Never write scratch files into the real repo tree from a test.
6. Follow this project's TDD discipline (`.claude/skills/tdd/`): write the test first, run it, confirm it fails for the *right* reason (an assertion mismatch, not a collection/import error), then implement until it passes. `.scratch/phase-1-bootstrap/issues/03-first-failing-smoke-test.md` records a real worked example, including a red run that failed for the *wrong* reason (a `sys.path` bug) that had to be fixed before the red-for-the-right-reason state was reached.
7. Before calling a test "passing" or "done," actually run it (`pytest <file> -v`) and read the real output. A test you wrote but never executed isn't verified — see `CLAUDE.md`'s "Running and verifying this repo."

## What to avoid

- **Don't assume Docker, a GPU, or any live backend service is reachable.** This agent's execution environment has none of those (`docs/agents/dev-environment.md`). Follow `specs/13-decision-log.md` DEC-135's tiering: fake/stub the external client as the default (the `SafetyRailAdapter`/`ECMAdapter` Protocol-substitution pattern this spec already establishes), reach for a real live-service integration test only when one can actually be run here, and split anything that can't into an agent-checkable proxy plus a named `[manual-verify]` item. **Partial exception: Qdrant (DEC-140, corrected by DEC-141)** — `qdrant-client`'s embedded local mode (`QdrantClient(":memory:")` or `path=...`) runs the real client library in-process, so collection vector-config and filter-correctness tests should use it directly rather than a hand-rolled fake. **Payload-index existence does not follow this exception** — local mode silently no-ops `create_payload_index()` (confirmed: real `UserWarning`, empty `payload_schema` on introspection), so that check needs either a static call-inspection proxy or `[manual-verify]` against a real server, same as production-scale HNSW-recall/performance.
- **Don't let a test pass only because of an accidentally-correct `sys.path`.** Both bare `pytest` and `python -m pytest` must resolve first-party imports identically. This repo hit a real CI failure from this once — `ModuleNotFoundError: No module named 'api'` in CI while the exact same test passed locally, because only `python -m pytest` had been run locally. `pyproject.toml`'s `pythonpath = ["."]` fixes this at the root — don't remove it, and don't paper over a future regression here with a per-file `sys.path.insert` workaround instead of fixing the root cause.
- **Don't "fix" a deliberately-red test by weakening its assertion.** `tests/api/test_ready.py::test_ready_reports_every_service_healthy` is red on purpose — Phase 1 has no backend services to check yet, so asserting `ready: true` would be dishonest. If a test is red because the underlying capability genuinely isn't built yet, leave it red with a comment explaining why (as that test already does) rather than loosening the assertion to make CI green.
- **Don't test private implementation details a caller can't observe.** Assert on the public surface — an HTTP response body, a function's returned violation list, a CLI's stdout — not on private internal state. None of the existing test modules reach into private attributes; keep it that way.
- **Don't trust a new check-style test until you've proven it catches the real bug it's for.** Before relying on a drift/lint-style check, reproduce the actual incident it's meant to prevent (in a `tmp_path` or a temp copy of the repo) and confirm the check flags it. `tests/docs/test_doc_drift.py` was verified this way against the real README-staleness bug this session hand-fixed earlier — not just against its own synthetic fixtures.
- **Don't leave a new or changed test uncommitted alongside the code it covers.** A test isn't real project memory — reusable by the next session or the next agent — until it's committed with the change it protects, per `CLAUDE.md`'s documentation-conventions rule.
- **Don't exclude a whole directory from the gated run to dodge one known-red test.** `tools/verify.sh` and `.github/workflows/ci.yml` used to `--ignore=tests/api` entirely (there was only `test_ready.py` in there at the time, with one deliberately-red test). By the time `tests/api/` grew to include `test_auth.py`, `test_openapi_contract.py`, and `test_query_stub.py` (TASK-033 Issue 01, 2026-07-15), all of them were silently running only in the "expected red, not gated" step and were never actually enforced by CI — caught only by noticing `gh run list` showing `failure` on every recent push. Deselect the exact known-red node (`--deselect path::test_name`) instead of ignoring its directory, so anything else added there stays gated by default.
- **A second `conftest.py` anywhere under `tests/` needs a `pyproject.toml` `[tool.mypy] exclude` entry, or `mypy .` breaks repo-wide.** Neither `tests/` nor any `tests/<domain>/` has `__init__.py` (this file's own rule, above), so mypy's default (no `--namespace-packages`) resolution names every rootless file by bare basename alone — two files both named `conftest.py` anywhere in that tree collide as "Duplicate module named conftest", confirmed when `tests/admin/conftest.py` (TASK-033 Issue 03, 2026-07-16) was added alongside the pre-existing `tests/api/conftest.py`. `pyproject.toml`'s `[tool.mypy] exclude` keeps `tests/api/conftest.py` (the original) walk-discovered and type-checked, and excludes every later `tests/<domain>/conftest.py` from the walk instead — those still run fine under pytest (which discovers `conftest.py` by directory tree, not basename) and are still linted by ruff, they just lose static type coverage, accepted as a narrower trade-off than the alternative (`--namespace-packages`/`--explicit-package-bases`, which fixes this cleanly but was tried and empirically breaks 4 existing, unrelated check-style test modules' bare `from doc_drift import ...`-style imports repo-wide — see that config's own comment). Fixtures meant to be shared across more than one `tests/<domain>/` package (like auth setup) go in a plain, non-`conftest.py`-named file instead (`tests/_shared_auth_fixtures.py`, imported explicitly by each consuming `conftest.py`) — the same "plain, non-magic filename" fix `tests/ingest/pdf_docx_fixtures.py` already used for a related but not identical collision (see that module's own docstring), generalized here to a second collision shape (bare-vs-dotted import identity, not just conftest.py-vs-conftest.py).

## Current test inventory

| Module | Type | Covers | Run it |
|---|---|---|---|
| `tests/architecture/import_graph.py` + `test_import_graph.py` | Static/AST check | Forbidden module-to-module import edges per `specs/04-architecture.md` §5.1 (e.g. `retrieve/` may never import `generate/`, `acl/`, or `verify/`) | `pytest tests/architecture/test_import_graph.py -q` |
| `tests/api/test_ready.py` | Component (FastAPI `TestClient`) | `GET /ready`'s response shape (DEC-117) and its honest not-ready state — Phase 1 has no backend services wired, so `ready: false` is the only correct value right now | `pytest tests/api/test_ready.py -v` |
| `tests/docs/doc_drift.py` + `test_doc_drift.py` | Static/text check | `DEC-###` reference integrity (no dangling or duplicate ids across `specs/`, `docs/`, `.scratch/`, `CLAUDE.md`, `README.md`) and `README.md` ↔ `.scratch/<feature-slug>/issues/*.md` issue-status sync, scoped per feature slug (fixed 2026-07-14 — a bare issue number collided across features, since every feature's numbering restarts at 01 by convention). Both directions of the cross-feature-collision fix are now covered (test-audit, 2026-07-15): no false positive when two colliding-numbered issues each independently agree, and a real mismatch inside one of them is still caught and correctly attributed, not masked by the other's clean state — the gap the pre-fix bug actually had, confirmed via mutation testing against the old bare-number-keyed logic | `pytest tests/docs/test_doc_drift.py -v` |
| `tests/db/test_migration.py` | Component (subprocess against the real Alembic CLI) | TASK-006's hand-authored migration (`alembic/versions/0001_initial_schema.py`) renders correct offline DDL for all 9 Postgres tables — table presence, the `document_versions` FK's `RESTRICT` (no cascade), `job_queue`'s status `CHECK`, `model_versions`' partial unique active-row index, `audit_events`' `REVOKE`, and structural reversibility via `alembic downgrade <rev>:base --sql` | `pytest tests/db/test_migration.py -v` |
| `tests/ingest/test_qdrant_setup.py` | Component (real `QdrantClient(":memory:")`, DEC-140/141; `pytest-mock` for `resolve_index_target`'s SQLAlchemy `Session`) | TASK-007's Qdrant collection setup (`ingest/qdrant_setup.py`) — dense+sparse vector config, Layer 1 ACL/retention filter semantics (a real seeded-points query, not a mock), a call-inspection proxy confirming all 5 mandatory payload indexes are requested (existence itself is unobservable in local mode, per DEC-141), and `resolve_index_target()` (added during Issue 01 code review, 2026-07-15 — wires `config/active_model_version.py`'s query into `build_collection_name`, closing a "built but never called" gap) | `pytest tests/ingest/test_qdrant_setup.py -v` |
| `tests/ingest/test_tokenizer.py` | Component (`tokenizers` library; one test needs outbound network access on first run to fetch `BAAI/bge-m3`'s tokenizer config, then cached) | `ingest/tokenizer.py`'s real bge-m3 (XLM-RoBERTa/SentencePiece) tokenizer wrapper and its deterministic offline `FakeTokenizer` — including a regression guard against silently reverting to character-count-as-token-count | `pytest tests/ingest/test_tokenizer.py -v` |
| `tests/ingest/test_chunking.py` | Component | `ingest/chunking.py`'s token-windowed splitter (1024/128 overlap, DEC-065) and `chunk_id` identity — deterministic via `uuid5`, stable across repeated calls, differs by document/version/sequence, and is a valid UUID (Qdrant point-ID requirement) | `pytest tests/ingest/test_chunking.py -v` |
| `tests/acl/test_ingest_stub.py` | Component (`pytest-mock` for the SQLAlchemy `Session`) | `acl/ingest_stub.py`'s disposable Layer 1 ACL/retention lookup — the in-memory `FakeACLLookup` and the real `SqlAlchemyACLLookup`'s row-to-`EffectiveACL` mapping and fail-closed behavior on a missing `document_versions` row | `pytest tests/acl/test_ingest_stub.py -v` |
| `tests/ingest/test_checkpoints.py` | Component (`tmp_path`) | `ingest/checkpoints.py`'s local-filesystem checkpoint read/write for bulk step output (kept out of `job_queue.payload` per the 2026-07-15 risk review) | `pytest tests/ingest/test_checkpoints.py -v` |
| `tests/ingest/test_job_store.py` | Component (`pytest-mock` for the SQLAlchemy `Session`) | `ingest/job_store.py`'s job-store port — the in-memory `InMemoryJobStore` and the real `SqlAlchemyJobStore`'s payload-merge/status-transition logic (structural correctness only; round-tripping against a live Postgres is `[manual-verify]`, no live server in this sandbox) | `pytest tests/ingest/test_job_store.py -v` |
| `tests/config/test_active_model_version.py` | Component (`pytest-mock` for the SQLAlchemy `Session`) | `config/active_model_version.py`'s runtime lookup of the active `model_versions` row for a role, and its fail-closed error when none is active. `test_selects_model_version_column_of_model_version_table` rewritten (test-audit, 2026-07-15) — it previously only asserted `ModelVersion.__tablename__ == "model_versions"`, never actually calling the function under test or inspecting the query's SELECT clause; now compiles the real query and asserts it selects `model_versions.model_version`, confirmed load-bearing via mutation testing (a wrong-column mutation now fails it) | `pytest tests/config/test_active_model_version.py -v` |
| `tests/ingest/test_embedding.py` | Component (`httpx.MockTransport` for the real HTTP client, no live TEI server) | `ingest/embedding.py`'s split dense/sparse design (DEC-142, 2026-07-15 — bge-m3's own sparse output isn't servable through TEI, confirmed via TEI's own GitHub issue #141): `TEIDenseEmbeddingClient`'s real `POST /embed` (bge-m3's confirmed-working half), `TEISparseEmbeddingClient`'s real `POST /embed_sparse` (a dedicated SPLADE model's own TEI deployment, response schema verified against TEI's Rust source), `HybridTEIEmbeddingClient` composing both into the `EmbeddingClient` Protocol (including a result-count-mismatch guard), and the deterministic `FakeEmbeddingClient`. `test_hybrid_client_recomputes_dense_on_each_retry_when_only_sparse_fails` (external peer review, 2026-07-15, LOW severity, not fixed) pins a known trade-off: a caller retrying the whole `embed()` call on a sparse-only failure redundantly recomputes an already-succeeded dense result | `pytest tests/ingest/test_embedding.py -v` |
| `tests/ingest/pdf_docx_fixtures.py` | Shared test helper (not itself a test module) | `build_pdf_bytes`/`build_docx_bytes` — real in-memory PDF/DOCX bytes built with PyMuPDF/`python-docx` themselves, shared by `test_parsing.py` and `test_pipeline.py` rather than duplicated or committed as binary fixture files. Deliberately **not** named `conftest.py` — pytest auto-imports `conftest.py` as a bare top-level module, which collided with an explicit `tests.ingest.conftest` import and made mypy see the same file under two module names (`Source file found twice`); a plain, non-magic filename avoids the collision, imported bare the same way `tests/docs/doc_drift.py` already is | n/a (no tests of its own) |
| `tests/ingest/test_parsing.py` | Component (real `pdfminer.six`/PyMuPDF/`python-docx` against in-memory-built fixtures, `pytest-mock` for the primary-parser-failure case; no live service) | `ingest/parsing.py`'s format-dispatched parser (DEC-143, 2026-07-15) — `.md`/`.txt` UTF-8 decode, PDF via `pdfminer.six` primary + PyMuPDF rescue (both the happy path and the rescue-path activation, plus the double-failure case), DOCX via `python-docx`, and `UnsupportedFormatError` for anything else | `pytest tests/ingest/test_parsing.py -v` |
| `tests/ingest/test_retry.py` | Pure unit (no I/O) | `ingest/retry.py`'s Full Jitter backoff (`compute_backoff_delay`) — non-negative, respects its cap, ceiling grows with attempt count, and — the property Issue 02's risk review specifically called for — not deterministic across repeated calls at the same attempt number (rules out synchronized thundering-herd retries) | `pytest tests/ingest/test_retry.py -v` |
| `tests/ingest/test_pipeline.py` | Component (real `QdrantClient(":memory:")` + all the fakes above) | `ingest/pipeline.py` — the full parse→chunk→embed→ACL-enrich→index orchestration, covering all 8 acceptance criteria in `.scratch/document-ingest-pipeline/issues/01-plaintext-markdown-ingest-pipeline.md` (happy path, unsupported-format rejection, NFR-012, chunk immutability, checkpoint idempotency, ACL-lookup fail-closed via mutation testing, NFR-003 field-name exactness, lightweight `job_queue.payload`) plus all 4 acceptance criteria in `.scratch/document-ingest-pipeline/issues/02-pdf-word-parsing-retry-resilience.md` (2026-07-15): real PDF/DOCX happy paths, the PDF rescue path activating end-to-end and setting `parser_fallback` correctly (`true`/`false`/absent depending on format and outcome), and the embedding-service retry loop — both recovering after a transient outage with an incrementing `retry_count` and giving up cleanly (job `failed`, no chunk written) after `MAX_EMBEDDING_RETRY_ATTEMPTS`, never hanging indefinitely | `pytest tests/ingest/test_pipeline.py -v` |
| `tests/api/test_auth.py` | Component (FastAPI `TestClient`; local RSA/EC/Ed25519 keys generated per test run, no live JWKS endpoint) | `api/auth.py`'s JWT bearer (DEC-061 algorithm whitelist, `kid`-based key selection across a 3-key-type JWKS bundle) and admin API key (`hmac.compare_digest`) authentication, against `POST /v1/query` as the first protected route: missing credentials, HS256/`none` rejection (including a hand-forged alg-confusion token), all 3 algorithm/key-type pairs accepted, `kid` edge cases (missing/unknown/type-mismatch — the last one a bare `TypeError` PyJWT raises, not one of its own exception types, confirmed empirically) rejected `401` not `500`, admin API key accept/reject, and `GET /ready` staying reachable with no credentials | `pytest tests/api/test_auth.py -v` |
| `tests/api/test_openapi_contract.py` | Component (in-process `app.openapi()`, no live server) | OpenAPI schema-drift: the generated schema matches a committed snapshot (`tests/api/openapi_snapshot.json`, hand-verified once against `specs/06-api-contracts.md` at authoring time, 2026-07-15) and an intentional mutation is actually caught (proves the comparison isn't vacuously true) | `pytest tests/api/test_openapi_contract.py -v` |
| `tests/api/test_query_stub.py` | Component (FastAPI `TestClient`) | `POST /v1/query`'s stub contract — registered, auth-enforced, returns `501` with the standard structured error body (not a bare `501`, not `404`) | `pytest tests/api/test_query_stub.py -v` |
| `tests/api/conftest.py` | Shared pytest fixtures (not itself a test module) | `ingest_client`/`fake_pipeline_deps`/`fake_scheduler`/`job_store`/`mock_session` for `test_ingest_routes.py` (2026-07-15); re-exports `client`/`keys`/`admin_api_key`/`jwt_headers` from `tests/_shared_auth_fixtures.py` (moved out 2026-07-16 once `tests/admin/` needed the identical setup — see that module's own docstring, and this file's "What to avoid" note on the `conftest.py`-basename collision that motivated the move) rather than redefining them | n/a (no tests of its own) |
| `tests/api/test_ingest_routes.py` | Component (FastAPI `TestClient`; `app.dependency_overrides` swaps in fake pipeline deps/scheduler/session — no live Postgres/Qdrant/TEI) | `api/ingest_routes.py`'s `POST /v1/ingest` + `GET /v1/ingest/{document_id}` — background scheduling via the `FakeTaskScheduler` (not response timing, not a spy on `BackgroundTasks`), the crash-recovery warning log, response shape, MIME-vs-`SUPPORTED_EXTENSIONS` rejection, `acl_override` bootstrap (missing/invalid → `400`), all 6 real `JobPhase` values mapped to the 5 API `status` values, raw-error redaction on a `failed` job, `404` for an unknown id, and both credential types on both routes | `pytest tests/api/test_ingest_routes.py -v` |
| `tests/_shared_auth_fixtures.py` | Shared pytest fixtures (not itself a test module) | `client`/`keys`/`admin_api_key`/`jwt_headers` -- JWT/admin-API-key auth setup shared across `tests/api/` and `tests/admin/`. Deliberately not named `conftest.py` (see its own docstring and this file's "What to avoid" note); imported explicitly by each consuming `conftest.py` rather than auto-discovered | n/a (no tests of its own) |
| `tests/admin/conftest.py` | Shared pytest fixtures (not itself a test module) | `document_store`/`audit_store` (`FakeDocumentStore`/`FakeAuditEventStore`) and `admin_client` (`TestClient` with both `get_document_store` and `get_audit_event_store` overridden, TASK-033 Issue 03 + Issue 04) for `test_documents.py`/`test_audit.py`'s HTTP-route tests; `mock_config_session`/`config_client` (`get_session` overridden with a bare mock, Issue 04) for `test_config_models.py`'s -- `/v1/admin/config/models` depends on `get_session` directly rather than a Protocol-typed store, since `list_active_model_versions` has one implementation, not a real/fake pair. Re-exports the four shared auth fixtures from `tests/_shared_auth_fixtures.py`. Excluded from mypy's directory walk (`pyproject.toml`'s `[tool.mypy] exclude`) -- see this file's "What to avoid" note | n/a (no tests of its own) |
| `tests/admin/test_documents.py` | Store-level (bare `mocker.MagicMock()` Session, and a stateful `FakeDocumentStore` -- no FastAPI) + Component (FastAPI `TestClient` via `admin_client`) | `admin/document_store.py` (`GET`/`PUT /v1/admin/documents`'s backing store) and `api/admin_routes.py` (the routes themselves), TASK-033 Issue 03. Two non-interchangeable store-level tiers (`.scratch/api-surface/issues/03-admin-document-management.md`'s "Testing tiers" section): Tier A -- `SqlAlchemyDocumentStore` against a mocked `Session`, real compiled `select`/`update` statements asserted (cursor-pagination `ORDER BY`, soft-delete's `UPDATE ... SET lifecycle_state='deleted'` with the real bound values inspected via `.compile().params`, not just `update()` vs `delete()` -- confirmed load-bearing via mutation testing, 2026-07-16: a wrong `SET` value passed while the returned object still looked correct); Tier B -- `FakeDocumentStore`, cursor pagination proven stable when a new row is seeded between two simulated page fetches (also mutation-tested), and an ACL edit's persistence proven via a *separate* subsequent `get_document()` call, not just the mutating call's own return value. Route-level: cursor-page/PUT response wiring, unknown-`document_id` → `404`, empty-body / invalid-`lifecycle_state` / invalid-`authority_state` → `400` (not `422` -- see `api/admin_routes.py`'s module docstring), and both credential types × both routes for auth (also mutation-tested: removing `Depends(require_auth)` from one route is caught) | `pytest tests/admin/test_documents.py -v` |
| `tests/admin/test_audit.py` | Store-level (bare `mocker.MagicMock()` Session, and a stateful `FakeAuditEventStore` -- no FastAPI) + Component (FastAPI `TestClient` via `admin_client`) | `audit/event_store.py` (`GET /v1/admin/audit`'s backing store) and `api/audit_routes.py` (the route itself), TASK-033 Issue 04. Same Tier A/B split as `test_documents.py`, deliberately *not* sharing pagination code with it (`.scratch/api-surface/issues/04-admin-audit-model-version-read.md`'s own risk-review: two fakes isn't yet a pattern worth abstracting) -- Tier A: real compiled `WHERE` clause asserted (`from`/`to` range, exact-string `user_id` match, correctly omitted when not filtering) and `ORDER BY timestamp DESC, audit_id DESC` (mutation-tested); Tier B: cursor pagination stable under an interleaved insert (mutation-tested) plus a `from`/`to`/`user_id` combined-filter check. Route-level: required `from`/`to` missing/malformed → `400` (not `422` -- same convention as `api/admin_routes.py`, extended here to query params since `06-api-contracts.md`'s API-A-03 row marks them required), and both credential types for auth | `pytest tests/admin/test_audit.py -v` |
| `tests/admin/test_config_models.py` | Store-level (bare `mocker.MagicMock()` Session -- no FastAPI, no Tier B: a fixed 7-role loop has no pagination/persistence algorithm to prove against known data) + Component (FastAPI `TestClient` via `config_client`) | `config/active_model_version.py`'s `list_active_model_versions` (extends the existing single-role `get_active_model_version` into a loop over `KNOWN_ROLES`, TASK-033 Issue 04) and `api/config_routes.py`'s `GET /v1/admin/config/models`. A role with no active `model_versions` row returns `{role, model_version: null}` plus a logged warning rather than failing the whole request (mutation-tested: removing the per-role `except NoActiveModelVersionError` makes one broken role fail every role). Route-level: all 7 roles present in the response, both credential types for auth | `pytest tests/admin/test_config_models.py -v` |

All twenty-one run in CI on every push/PR (`.github/workflows/ci.yml`) and, except the intentionally-red `/ready` target-state test, as a local pre-commit gate too (`.pre-commit-config.yaml` — `pre-commit install` once per clone). Run everything locally with `pytest` from the repo root — see `CLAUDE.md`'s "Running and verifying this repo" for the full local-verification command list (`ruff check .`, `mypy .`, `pytest`).

This inventory — and whether each test's assertions actually verify what they claim to, not just whether they pass — is periodically re-audited by `.claude/skills/test-audit/SKILL.md`. See `CLAUDE.md`'s "Test-quality audits" section for when to run it. `.scratch/review-reports/test-audit-2026-07-14.md` was the first run (clean, but scoped to Phase 1's 14-function scaffold suite only); `.scratch/review-reports/test-audit-2026-07-15.md` covered Phase 2's `data-foundation`/`document-ingest-pipeline` suite (94 new functions), which found and fixed two real gaps; `.scratch/review-reports/test-audit-2026-07-16.md` covered `api-surface`'s HTTP layer (81 new functions across Issues 01-04, never previously audited), which found and fixed two more (an overly broad `pytest.raises(Exception)`, and a test mutating shared `app.state` without restoring it).

## Planned but not yet implemented

`specs/11-test-plan.md` is the canonical, spec-level catalog of every test planned across Phase 2+ (`TEST-001`..`TEST-040`: unit, integration, contract, and E2E layers, each traced to a `REQ-###`/`NFR-###`). Its Test Environments table (amended by DEC-135) records which environment tier each planned test targets — check it before assuming a planned test can run in this agent's own sandbox. When a `TEST-###` gets implemented, add its module to the inventory table above; this file tracks what exists, `11-test-plan.md` tracks what's planned.
